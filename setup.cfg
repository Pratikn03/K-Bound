[metadata]
license_files = LICENSE

# NOTE: linting (ruff), import sorting (ruff "I"), and pytest configuration are
# defined once in pyproject.toml. The former [flake8]/[isort]/[tool:pytest]
# blocks here were duplicates and have been removed (single source of truth).
