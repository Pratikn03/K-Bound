# K-Bound paper/core input profile (CPU only).
#
# Python is fixed by .python-version.  The numerical versions below are the
# sealed direct requirements for the 2026-08-27 paper profile. This profile
# intentionally excludes Torch, WILDS, the API, and OpenCV.
# Install requirements-paper.lock.txt with --require-hashes for a reproducible
# environment; edit this input file only when intentionally refreshing the lock.
numpy==2.4.4
scipy==1.17.1
scikit-learn==1.8.0
pandas==3.0.3
matplotlib==3.10.9
PyYAML==6.0.3
joblib==1.5.3
pytest==9.0.3
