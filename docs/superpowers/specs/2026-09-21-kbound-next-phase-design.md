# K-Bound next research phase

The user explicitly approves all six improvements in the academic audit, implementation, experiments, and manuscript revisions. The intended outcome is stronger original research and measured practical value. A numeric quality target is aspirational, not an acceptance criterion or license to select favorable outcomes. The physical-device camera study remains future work.

## Design and authority

Work in /Users/pratik_n/.codex/worktrees/kbound-nextphase-20260921 on codex/kbound-nextphase-20260921, based on artifact commit 102ceecc04d8c6a9272059277bb4f215cf495ec6. The Desktop release is an immutable reference. Never overwrite its source, result authorities, raw data, or reviewer ZIP. New outputs are under this worktree's output/next_phase or /Volumes/T9/kbound_next_phase_20260921. The tested Python is /Users/pratik_n/.cache/kbound-release-verify.MsziU5/venv/bin/python; MPS is available; Docker engine 28.5.1 responds.

Three independent tracks feed a revised manuscript: (1) calibration-value comparisons with honest group hold-outs; (2) a constructive paired-benefit uncertainty model with explicit structural assumptions; (3) a newly locked natural-shift evaluation and measured deployment behavior. Existing immutable histories retain their original scientific status.

## Calibration-value study

Use only the authenticated CIFAR-10-C/ImageNet-C compact source manifest and the recovered mixed-sign three-checkpoint panel. All are retrospective. Fit a common predictor per outer split; compare always-adapt/freeze, point prediction, tune-selected fixed margins, exact split-conformal, locally scaled conformal, and a finite-family calibrated risk-selection arm. No target-label feedback is permitted in these matched-information arms. ACI is excluded from the matched arms because it consumes delayed labels.

Outer evaluation holds out whole corruption types; checkpoint panels additionally use held-out models and joint model/corruption exclusions. Related semantic conditions are indivisible in fit/tune/cal roles. A separate environment-level calibration sensitivity uses one worst-case residual per environment and returns infinity whenever the exact rank is unavailable. Finite condition-level radii do not claim distribution-free new-environment protection.

Protocol version, input hashes, split rules, model settings, margin grid, budgets, tie rules, costs, and inference units must be frozen before executing the new study. Primary alpha is .10; sensitivities .05/.20 are separate. Primary utility is paired-oracle regret. Harm-weighted diagnostics use a fixed grid and cannot select the reported winner. Decision generation rejects outcome fields and precedes scoring. Each run has fresh output directories, strict JSON, command/runtime receipts, and hashes of fitted artifacts and decisions.

Constant-radius gates have the same acceptance ranking as point scores within each fold. Matched-exposure comparisons must test this identity. Any improvement of a constant-radius gate concerns selecting an operating point before scoring. Varying radii can change ranking and need their own comparative evidence.

## Constructive theory

Investigate a finite-sample paired-benefit feasible set from source class-conditional prediction-pair information and target unlabeled pair frequencies, with explicit transport/sensitivity assumptions. Derive sharp optimization within the declared feasible set, construct counterexamples, report empty sets as assumption/model incompatibility, and expose any unidentifiable case as unresolved. Existing label-shift and new confidence-polytope literature are mandatory comparisons; standard LP and concentration machinery cannot be advertised as newly invented. A break-even sensitivity budget may make the assumption cost operationally interpretable. Publish only proved scoped statements; failed conjectures remain recorded.

## Natural-shift evaluation

First assess the existing So2Sat v2 design, five independently trained source checkpoints, 9 opened development cities, 19 reserved calibration cities, and 10 reserved target cities. Inspect access/protocol metadata before reading payloads. Bind the actual source weights and schemas and reconstruct missing public authorities from authenticated metadata. Lock configuration and read permissions before calibration. Apply the predeclared city-level screen honestly; a development/calibration stop is a completed negative study, not permission to open target or weaken thresholds. Verify exact-rank city aggregation and independent model identities.

If So2Sat's histories or prerequisite authorities prevent this design, record the exact obstruction and use a separately locked public-data design rather than claiming its target fresh. CLEAR-10 is a small verified candidate, but its few temporal environments cannot deliver a finite alpha=.10 environment-level radius with two calibration buckets. Any CLEAR study must state this limitation and distinguish episode-level empirical evaluation from environment-level guarantees. No dataset is selected by inspecting reserved outcomes.

## Deployment measurements

Implement a local shadow-serving harness with an explicit checkpoint/adapter/schema/calibration identity, frozen fallback, fresh decision records, atomic state transitions, malformed-input rejection, and repeated-use expiration. Benchmark real candidate creation, paired inference, feature extraction, policy decision, rollback/state storage and peak memory separately. Synthetic service tests are engineering evidence; replay timing is not full adaptation cost. Use existing real checkpoint/data interfaces where authorized, synchronize accelerator timings, preserve warmup/repetition counts, and report all failures. External public serving and safety-critical deployment are not implied by local production-style validation.

## Success criteria

- Source and outcome isolation, valid statistical calculations, faithful comparisons, measured costs, and reproducible receipts.
- Positive or negative results reported under their frozen rule; no post-target rescue search.
- Closest-work table includes newly verified September 2026 identifiability and label-shift work and MORPHEUS.
- Manuscript revisions follow observed evidence; no guaranteed 9+ score, novelty, population coverage, or universal utility.
- Final documents and new research package are rebuilt only after checks and independent review; original sealed release stays available.
