# Next-phase execution ledger

Plan: docs/superpowers/plans/2026-09-21-kbound-next-phase.md
Spec: docs/superpowers/specs/2026-09-21-kbound-next-phase-design.md
Base: 102ceecc04d8c6a9272059277bb4f215cf495ec6

The user explicitly approved all six audit recommendations, implementation and experiments. Proceed continuously with reasonable technical choices; no repeated scope-approval request is needed. Publication/production quality is an evidence outcome, not a promised score.

Preflight: dedicated Git worktree created from the correct Desktop repository. The app's native worktree tool addresses the different Projects checkout, so manual Git was necessary to isolate the correct source. Original Desktop release and untracked natural-calibration audit files remain untouched. T9 has 426 GiB free; internal disk 29 GiB. Pinned release Python has torch/torchvision/sklearn/scipy/pytest and MPS; Docker responds at version28.5.1.

| Tasks | Shared interface | Ruling |
|---|---|---|
| 1 and 3 | Aggregate feature/decision semantics | Natural runner uses actual image/model identities; never invent them for historical compact cells. |
| 1 and 4 | Shared fitted predictions and gate outputs | Runtime measurements must distinguish replay and true candidate execution. |
| 2 and 3 | Class-conditional transport assumption | Theory diagnostic cannot certify So2Sat unless the assumption is justified; default separate scopes. |
| 1–4 and 5 | Machine-readable summaries and claims | Only reviewed empirical outputs and proved scope enter maintained text. |
| 5 and 6 | Generated documents and seal | Freeze after all edits; old seal remains reference only. |

Task1: completed retrospective calibration comparison, including group-risk reporting. All 160 cohort/arm summaries and 1,140 fold/arm group summaries are retained. Independent arithmetic/data-separation review passed. CIFAR Tent improves, but the full comparison does not show general superiority to point prediction or simple margins. Corrected reports do not overwrite original execution authorities.
Task2: completed paired-transport implementation, proof note and 4,860 synthetic configurations. Independent review found no central mathematical error. The useful paired-cancellation example and harmful same-observable-world negative control are both retained. Floating-point feasibility/quantile limits are explicit.
Task3: locked So2Sat v2 calibration completed 95 cells and failed the unchanged 7-direct-ADAPT-city/7-direct-FREEZE-city screen (one ADAPT city, zero direct-FREEZE cities). Target access remains forbidden by this result. Original executed bytes are archived; verified-document read and prior-access discovery corrections were applied only after execution, with regression evidence and explicit source lineage. An already-opened-record posthoc diagnosis explains the wide radius without refitting or changing the screen.
Task4: 46 lifecycle tests now pass, including expiry, journal failure, bounded receipt/journal retention and concurrency. Capacity exhaustion permanently closes the session and returns all prior selections to source fallback. Fresh deployment_v4 and Docker v2 measurements are complete. All 54 image code/lock hashes match current source; the loopback local service is running. Neural stage profiling uses only already-opened Cairo pixels across all five checkpoints and three repetitions; its final report records workload and measurement limitations.
Task5: completed close-comparison and new numerical/theory integration. Preliminary 64-page compact and 73-page TMLR builds had no overfull boxes or unresolved references. Full/main-only Word derivation preserves native equations and source-bound figure media; early visual review found and prompted correction of inherited Word table and equation-label layout issues. Final generated page counts and all-page inspection are recorded separately in the release QA receipt. The main paper is a page-prefix ending with references, not the historically named full short_final_draft.
Task6: source-seal inventory and create-only evidence/archive plumbing implemented and tested (124 cases). Both Lean caches APFS-cloned into independent worktree directories. Pinned current CIFAR upstream audit materialized and arithmetic replay passed. The committed evidence manifest binds completed selected outputs, including original attempts and executed sources. Fresh all-gates verification and generated-document/archive acceptance follow this source freeze; their final machine-readable receipts, rather than this source-stage ledger, determine release acceptance.

Initial baseline invocation named two nonexistent tests and ran no tests; corrected invocation uses actual test_controlled_grid_crossfit.py, test_kga_population_transfer.py, test_kga_unavailable_runtime.py. Preserve its real result separately.

Corrected fresh baseline: 109 passed in 2.45 seconds with pinned release Python. Docker 28.5.1 responds and MPS is available. Existing natural source checkpoints avoid five unnecessary retrainings.

Natural calibration launched after current-run lock 0f6ecd5c8ad2abb4f0d4045f38b4e94eff969a13accd853854bc9786b5fd8f9a and pre-calibration seal d2d0a9d1a78d36027ec3ba1a04a7dfd03e0fe4f90e2781733b68e171eeeb3610. Small authorities use APFS because T9 rejected atomic create-only publication. Opaque decompressed containers remain on T9. The failed preparation is preserved. Runtime source bytes are archived before postrun formatting. Original 7-ADAPT-city/7-FREEZE-city criterion is unchanged.

First held-corruption results show severe interval undercoverage and no general advantage over tuned margins. They remain negative transfer evidence, not closure. Reporting review corrected false-FREEZE counts that had included ABSTAIN; sealed decisions stay unchanged. ImageNet group-schema correction completed under unchanged scientific settings, with its old conservative grouping preserved. A reporting-only group-risk supplement recomputed the existing sealed decisions without refitting.

A Bandit import warning for pickle was scoped to the exact import with an explanatory serialization-only comment: the module calls dumps on locally fitted models and never deserializes pickle. Runtime behavior and experiment outputs are unchanged; executed study source snapshots remain authoritative.
