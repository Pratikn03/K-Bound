# Final publication decisions

**State: FINAL SOURCE EDITS FROZEN FOR APPROVED BUILD AND RELEASE.**

Date: 2026-09-23. This is the single decision record for the two supplied final
publication plans. It records source edits and the approved release scope, not a new
scientific verification or release receipt.

The user subsequently authorized rebuilding, supplied the final publication
checklist, and explicitly requested pushing a frozen release with checksums and
SHA identities. The release receipts generated after this source freeze are the
authority for execution and publication completion; the initial source-only
restrictions below describe the earlier phase.

## Final checklist closure and export scope

The five requested corrections are applied: Protocol A/B spacing, main-text JPEG
influence interpretation, the fibre-wise audit limitation, scoped SAR numerical
perturbation checks, and the descriptive Table 23 caption with incomplete local
execution provenance. Additional consistency fixes narrow the 250-tree settings
to principal CIFAR Protocol B, replace historical "official SAR" with "published
settings", distinguish accuracy percentages from percentage-point differences,
and label the withheld iWildCam record as a metric-mismatched diagnostic.
Stable artifact identifiers replace the remaining full experimental paths.

The exact theorem statements, proofs, equations and principal numerical rows are
unchanged. The claim register retains its scientific statuses and refreshes only
approved manuscript-source bindings and the fixed-fold finite-radius anchor.
The reference anchor for study-specific estimator settings now attaches to its
numbered subsection. A protected path macro fixes LaTeX caption compilation.

The Word converter replaces a repeated exact caption for adjacent parts of one
table with a continued label, preserving the first full caption and table XML.
Its 84 targeted tests passed before the final source freeze. All initial PDF and
LibreOffice-rendered Word pages were visually checked; final byte identities,
changed-page checks and appendix boundaries belong to the final document QA
receipt. Native Microsoft Word inspection was attempted through both Word and
Finder but repeatedly returned timeout/no-window errors. Do not represent the
LibreOffice check as native Word verification or claim that native equations
were repaired: all 88 display equations and five figures were already retained.

The approved final operations are source-bound software/artifact checks,
compiler-only PDF builds, Word conversion/rendering, appendix-free derivation,
source/evidence manifests and archives, checksums, a new immutable release tag,
GitHub publication and download/hash verification. Prior releases and archival
scientific records remain intact. No new training, refitting, protected target
access, new statistical study or physical-camera work is included. The frozen
manuscript is reopened only for a factual/mathematical error, broken citation,
inconsistent result, substantive reviewer feedback or venue requirement.

## Authority and permissions

- Working source: `/Users/pratik_n/.codex/worktrees/kbound-nextphase-20260921`,
  branch `codex/kbound-journal-20260922`, base commit
  `16f705a0c1f8658b8e5e760daf2c37f2ee91e716`.
- Published baseline: `Pratikn03/K-Bound`, release
  `kbound-journal-2026-09-22`. The plans' `7f02a14` / `kbound-2026-09-22`
  reference is older. The newer abstract, introduction and Appendix R are retained.
- Before editing, the maintained scientific inputs matched the existing source
  seal. The existing compact PDF matched its recorded SHA-256
  `8a933732a4c5ef6f6882c9bc5b1d6c0f8ea776f73a4bf86b4e0f5f0b6ed49d2f`;
  its existing QA records describe 67 pages, with appendices beginning on page 24.
  The existing full DOCX matched
  `9df3ad5eac838eca0857ddd365af6609eecfd4ca5db42609f356093cd0b62b3c`.
  The renamed PDF/DOCX mentioned in the plans were not attached as document
  bytes here, so their identity with these local files is not asserted.
- The shared sources remain `kbound_abstract_core.tex`,
  `kbound_submission_body.tex`, `kbound_submission_supplement.tex` and
  `journal_revision/journal_synthesis.tex`. Compact and TMLR drivers continue
  to use the same scientific text. No title, proof, equation, result authority,
  numerical table row, decision threshold or experiment record was changed.
- Authorized here: read-only source/record inspection and bounded source-text
  editing. The supplied plan explicitly says not to run tests, compile, render,
  convert Word or invoke the multi-operation wrapper without specific permission.
  None of those operations, nor training, refitting, new statistics, protected
  target access, downloads, installs, commits, pushes or publication, occurred.
- Four generated files were already modified at entry and remain outside this
  pass: `KBOUND_RELEASE_SHA256SUMS.txt`, `KBOUND_POST_CHECKSUM_SHA256SUMS.txt`,
  `audits/release_source_seal_2026_08_29.json`, and
  `audits/repository_test_inventory.json`. The preceding exports and receipts
  remain the preceding release's records; they do not certify these text edits.

The final source diff was reviewed for theorem/proof preservation, estimator
settings, evidence scope and citation grammar. The review corrected the fitting
unit to include stream seed and distinguished executed code from saved fitting
receipts. This is source inspection, not an executed verification pass. The
existing PDF and DOCX hashes were re-read after editing and remain unchanged.

## Initial source-only edit decisions

All paths in this table are relative to `docs/research/kbound`, except root README.

| ID | Source and decision | Status |
| --- | --- | --- |
| E01 | `kbound_submission_body.tex`: spaces after both Protocol A/B colons. | Source edited; exported typography pending. |
| E02 | Shared abstract, main shared-predictor interpretation, conclusion: retain lower observed Tent loss, add smaller matched-exposure advantage and corruption-mix sensitivity. Main text now describes the existing JPEG influence diagnostic and retains all six types in the primary estimate. | Source edited; no new sensitivity calculation. |
| E03 | Main estimator paragraph and supplement `app:estimator-settings`: separate principal CIFAR 250-tree replay from the 100-tree shared-predictor study, its rescoring history, CCT ridge and So2Sat ridge. Historical Protocol-A defaults are not asserted as recovered execution settings. | Source edited from existing configurations and executed records. |
| E04 | Main results and Appendix R: constant finite radius preserves ordering for the same predictions within a fixed scoring fold; matched exposure uses the same tie rule. No global ranking assertion across fits. | Source edited. |
| E05 | Main audit interpretation, Appendix A and sampled-frontier discussion: scope the audit obstruction to a uniformly tighter bound on the same evidence fibre and declared class; allow conservative bounds or justified added information/structure. | Prose edited; theorems and equations preserved. |
| E06 | Supplement: one SAR intervention supports the tested replay cell, not every cell; retain the separate 81-cell checks. Shared-predictor preparation reads historical outcomes; distinguish feature-only decision inputs from later scoring and identify the tested fields rather than full receipt bytes. | Source edited; no tests rerun. |
| E07 | Historical Tent seed-0 Figure 4 illustration remains explicitly separate from the current Protocol-B aggregate; its short artifact identity already suffices. | Already adequate; no figure or caption change. |
| E08 | Compound-bound tables retain sample-size sensitivity and no-fresh-sampling qualification, including the separate synthetic diagnostic. | Scientific text already adequate; Word Table 16 caption/native equation display pending inspection. |
| E09 | Impact and conclusion: application-specific fallback and domain-expert review where appropriate; retained model is not certified safe. Keep explicit absence of clinical/autonomous validation. | Source edited. |
| S01 | Supplement: diagnostic heading and study headings replace reader-facing task labels. Table 23 caption/Arm-2 label changed in its maintained generator to a qualified local SAR comparison. | Generator source edited only; generated Table 23 remains unchanged until approved targeted regeneration. |
| S02 | Supplement: replace repeated printed hashes, random-suffix path and CLI blocks with stable artifact identities. Existing `next_phase/RELEASE_CHECKLIST.md` retains exact locations and wrapper side effects. | Source edited; original integrity records untouched. |
| S03 | Root README points to the published journal release, distinguishes pending source edits, and describes CCT as locked retention. Five citation sentences use prose compatible with numeric and author-year drivers. | Source edited; bibliography and CITATION software version/date retained. |
| S04 | Preserve Table 27's B6/helpful-only/native-component qualifications and corrected-BN versus matched-SAR 400/eight configured-update distinction. | Already adequate; numerical evidence and captions preserved. |

## Retained claim-to-evidence map

This is an index to the existing
[`journal_revision/claim_evidence_register.json`](journal_revision/claim_evidence_register.json)
and [`next_phase/evidence_manifest.json`](next_phase/evidence_manifest.json), not
a replacement register. `R` means `experiments/kbound/results/`; `E` means
`output/next_phase/`, restored from the existing evidence archive where needed.
`J` means `docs/research/kbound/journal_revision/`.

| Finding / manuscript label | Evidence type and unit | Existing authority / procedure identity | Retained limit |
| --- | --- | --- | --- |
| Binary frontier / `sec:compact-theory`, `thm:compact-beta-minimax` | Mathematical result under assumptions; population paired zero-one benefit | Maintained theorems/proofs; fixed pair, evidence law and rich residual class; register T1–T3 | Scoped formal correspondence does not certify empirical KGA or unrestricted multiclass claims. |
| Principal CIFAR / `tab:primary-cifar` | Saved-feature replay of measured cells; equal-cell accuracy and regret | `R/reconciled_panels_v1/{canonical_panel_results.json,source_manifest.json}`; Protocol B, GBRT 250/2/.05/.8/0 | Five stream seeds reuse an archived checkpoint; current replay does not recover original neural execution identity. |
| Shared predictor / `sec:nextphase-calibration` | Saved-feature replay; weighted loss, exposure, cell/group inclusion | `E/calibration_value_v1/FINAL_RESULTS_SUMMARY.json`; GBRT 100/2/.05/1/20260921; `report_03` rescores unchanged `attempt_02` decisions and retains fitting receipts, corrected ImageNet has its own `attempt_04_imagenet_schema_corrected` execution snapshot, group report rescoring only | Receipts and executed code are not serialized fitted models. Retrospective and dependent; poor inclusion remains, conditional error undefined at zero exposure. |
| Appendix R / `app:journal-empirical` | Numerical reconstruction and sensitivity analysis; original cell means and six-corruption paired sensitivity | `J/empirical_synthesis_v1.json` and its existing input-bound manifest/receipt | Adds interpretation and reproducible arithmetic, not independent deployments or confirmatory inference. |
| B6 / `tab:task1_independent_models` | Historical provenance-limited replay; checkpoint-condition cells | `R/task1_independent_models_replication/task1_mixed_sign_records_checkpoint.json`; `release/recovered_gemini_evidence_v1/` | Rank nine clipped to eight; helpful-only execution authority missing; native estimator is not the whole published learner. |
| SAR controls / `tab:sar-corrected-fiveseed`, `tab:task4_matched_sar`, `tab:sar-27cell-audit` | Measured records and saved-feature diagnostics, with historical provenance limits; 27 conditions | Separate corrected-BN records/replays; `R/task4_matched_sar_v1/TASK4_MATCHED_SAR_MANIFEST.json` and `TASK4_MATCHED_SAR_KGA_SUMMARY.json`; historical pre-repair audit separate again | Different candidate executions and configured budgets; matched execution identity incomplete. Historical 60-cell raw authority unavailable; no causal BN attribution. |
| CCT / `sec:cct20-result`, `app:cct20-ledger` | Measured experiment; checkpoint-location cells, set-membership accuracy | `paper/generated/cct20_release_manifest.json`, portable public bundle; standardized ridge penalty 10, unpenalized intercept | Retention with zero ADAPT; nine locations crossed with five checkpoints, and aggregate target metadata previously inspected. |
| So2Sat / natural-shift calibration section | Calibration-only measured study; city/checkpoint cells | `E/natural_v2/{calibration_summary.json,gate_screen_v2.json,precalibration_seal_v2.json}`; locked ridge penalty 1 and runner snapshot | Failed eligibility; no target scoring. Access chronology is not universal proof of historical nonaccess. |
| Paired transport / `app:nextphase-paired-proof` | Mathematical result under assumptions plus simulation; paired population contrast and synthetic trials | `E/paired_transport_v1/receipt.json`, counts, witnesses and executed snapshots; fixed pair with supplied transport budget | Violated-assumption failure retained; numerical inverse-CDF and feasible-set qualifications remain; no native MaC-LP or field validation claim. |
| Costs / `app:deployment-measurements` | Measured workload costs and local software behavior | `E/natural_cost_v1/{summary.json,cost_arithmetic_audit.json}`, `E/deployment_v4/summary.json`, executed snapshots and Docker receipts | One opened-city workload and local fault tests do not establish independent deployments, maintenance economics or production net benefit. |

## Items recorded at the end of the source-only phase

1. **Output phase requires permission.** Generated Table 23, PDFs and Word still
   show their preceding versions. Native Word inspection remains outstanding;
   the plan's 59-page preview is not evidence that native Word loses equations.
   Inspect the exact Word file's abstract, frontier table, Theorems 1–3,
   Equation 11, Algorithm 1, Tables 14/16 and paired-transport mathematics before
   considering converter repair.
2. **Historical authority stays limited.** Original Protocol-A fitted settings,
   the historical 60-cell SAR raw authority and complete matched-SAR execution
   identities remain unavailable. The new text narrows claims; it does not
   recreate these records or request new experiments to replace them.
3. **No new research phase.** Phase 7, protected target scoring, renewed So2Sat
   eligibility, new datasets/checkpoints and production claims remain outside
   this pass. No acceptance or exceptional-doctoral rating is asserted.

## Build operations subsequently approved by the user

The immediate proposed scope is **Table 23 presentation regeneration, candidate
PDF/Word exports, native Word inspection and rendered-page QA**, with no research
rerun, source commit, resealing or publication. Use a fresh candidate directory;
do not overwrite the preceding delivery. The commands below record that approved scope.

1. Read the two unchanged `TASK4_MATCHED_SAR_*.json` authorities named above and
   invoke only `generate_latex_table(manifest, kga_summary, candidate_tex)` in
   `scripts/generate_task4_matched_sar_artifacts.py`. Compare the candidate with
   `paper/generated/tab_task4_matched_sar.tex`; replace the maintained generated
   input only if every numerical row is unchanged and differences are limited
   to the caption/Arm-2 display label. This formats existing summary numbers;
   it does not refit or execute SAR. **Do not invoke the generator CLI:** it also
   rewrites an archival Markdown summary whose template retains older overclaims.
2. From `docs/research/kbound`, initialize empty `.aux`, `.out`, `.toc`, `.lof`,
   `.lot`, `.loa`, `.lol` and `.bbl` files for each job in the new absolute
   `$KBOUND_PDF_CANDIDATE` directory to prevent old sidecars being reused. Then:

   ```bash
   latexmk -g -pdf -interaction=nonstopmode -halt-on-error -file-line-error \
     -pdflatex='pdflatex -no-shell-escape %O %S' \
     "-outdir=$KBOUND_PDF_CANDIDATE" "-auxdir=$KBOUND_PDF_CANDIDATE" \
     -jobname=kbound_short_final_draft kbound_submission.tex
   latexmk -g -pdf -interaction=nonstopmode -halt-on-error -file-line-error \
     -pdflatex='pdflatex -no-shell-escape %O %S' \
     "-outdir=$KBOUND_PDF_CANDIDATE" "-auxdir=$KBOUND_PDF_CANDIDATE" kbound_tmlr.tex
   ```

   These write candidate PDFs and compiler intermediates/logs, not result
   authorities. Inspect logs and references. Do not invoke `build_pdfs.sh`.
3. From the repository root, convert the same maintained source:

   ```bash
   "$KBOUND_DOC_PYTHON" docs/research/kbound/scripts/build_docx.py \
     --output "$KBOUND_CANDIDATE/kbound_short_final_draft.docx"
   ```

   This invokes latexpand, Pandoc, figure rasterization and internal conversion
   checks; it writes DOCX plus `.figures.json` and temporary files. It is not a
   copy operation. Open the exact candidate in Microsoft Word without saving
   edits; native inspection may create temporary lock/recent-file state.
   If native math fails, identify the defect before proposing converter edits.
4. Render PDFs with `pdftoppm -png -r 144 INPUT.pdf OUTPUT_PREFIX` and render
   Word using the existing document runtime and renderer:

   ```bash
   "$KBOUND_DOC_PYTHON" "$KBOUND_DOC_RENDERER" \
     "$KBOUND_CANDIDATE/kbound_short_final_draft.docx" \
     --output_dir "$KBOUND_CANDIDATE/full_word_render" --emit_pdf
   "$KBOUND_DOC_PYTHON" docs/research/kbound/scripts/derive_main_only_documents.py \
     --full-pdf "$KBOUND_PDF_CANDIDATE/kbound_short_final_draft.pdf" \
     --full-docx "$KBOUND_CANDIDATE/kbound_short_final_draft.docx" \
     --output-dir "$KBOUND_CANDIDATE/main_only"
   ```

   The renderer uses LibreOffice, not native Word, and writes temporary
   profiles, PDFs and images. Main-only derivation writes PDF/DOCX and a JSON
   receipt and performs temporary raster comparisons. Inspect every delivered
   page and rediscover the appendix boundary; do not reuse the old cutoff.

Fresh source-bound verification, source commits, seals/checksums and reviewer
archives are later separately approved operations following the existing release
checklist. All-gates verification runs tests/validators, formal builds and receipt
writes, package/dashboard builds, a Docker build and container checks; it is not
read-only. Its protected-data flag concerns already-opened So2Sat development
authority, not target access. The Docker build may use network. Publication is
a further separate approval for a named commit, branch/tag, notes and asset list
on `Pratikn03/K-Bound`; do not overwrite the existing release.
