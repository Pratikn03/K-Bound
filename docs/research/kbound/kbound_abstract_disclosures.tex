% =====================================================================
% kbound_abstract_disclosures.tex -- the weak-zeros caveat, verbatim, shared.
% This is a mid-sentence CLAUSE: no leading conjunction, no trailing period.
% Both closing paragraphs (kbound_abstract.tex and kbound.tex) \input it, so
% the natural-track zeros cannot be reported as strong in one build and weak
% in the other. See kbound_abstract_core.tex for why.
% =====================================================================
three of those zeros are weak---Camelyon17's promoted subset contains no harmful cell, and
RxRx1 and iWildCam make $0$ and $1$ \textsc{adapt} decisions---and the held-out record file
behind the promoted iWildCam triple is absent from the release, so that row is a sealed summary
rather than a reproduced result%
