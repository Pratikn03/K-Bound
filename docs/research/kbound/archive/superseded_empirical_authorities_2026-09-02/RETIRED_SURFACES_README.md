# Retired active surfaces (2026-09-02)

This companion inventory preserves 43 runnable sources, stale claim/lock surfaces, obsolete
reproduction notebooks, readiness guides, and superseded publication entry points. They are not
current execution, claim, seal, reproduction, or publication authorities.

`RETIRED_SURFACES_MANIFEST.json` records exact bytes from source commit
`660d893caede49c3b7daa8c18e43bb6cbbce5480`. Files with disposition `archived_only` no longer exist
at their original active paths. Files with disposition `retired_or_rerouted_active_stub` retain a
fail-closed launcher, explicit retired notice, or canonical-routing revision at the original path;
their pre-retirement bytes are preserved below `retired_tree/`.

The duplicate legacy reproduction notebooks were removed from the active tree in favor of
`notebooks/00_KBound_Master_Guide.ipynb`. Historical G5, showcase, full-panel, and subjective
readiness launchers now exit 2 without running; the unbannered WIN_HUNT v4/v5 copy-paste runsheets
are explicit retired notices. The current topic notebook and maintained indexes route only to
`runbooks/release_candidate.sh` for release verification.

The historical reviewer reproduction packet and June findings analysis are archive-only because
they contain superseded promotions. The June headline JSON retains a non-authoritative stub at its
old path so older supersession links resolve safely; its original bytes are archived and the stub
routes current claim status to `claim_ledger.json`.

The historical nine-track receipt remains byte-for-byte unchanged in this archive. Its verifier is
read-only and resolves the one moved findings narrative through the primary archive manifest. It
does not regenerate an active seal or recreate archived authorities.
