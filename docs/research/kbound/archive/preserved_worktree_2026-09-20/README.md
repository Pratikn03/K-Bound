# Preserved local variants — 2026-09-20

These seven files were relocated byte-for-byte after user approval and a bounded
independent dependency review. Six text/script files differ from the older
September 2 archive. The Word file is identical to its older archived copy.
Both sets are retained; neither was overwritten.

`MANIFEST.json` binds each original path, new path, byte count, SHA-256 and older
archive identity. To recover a file, copy its recorded `tree/` member to a fresh
location and verify the manifest hash. Do not overwrite a newer active file.

These are historical records, **not release deliverables or current authority**.
Their original wording, including obsolete closure claims and local commands,
is deliberately unchanged. Do not execute the archived scripts. The unique
`manuscript/theory_spine/` sources remain at their live locations.

Current status: [living execution ledger](../../ACTIVE_NATURAL_STUDY_PLAN.md).
Current release route: [release checklist](../../RELEASE_CHECKLIST.md), subject to
its source, runtime and verification gates. This archive does not seal a release.
