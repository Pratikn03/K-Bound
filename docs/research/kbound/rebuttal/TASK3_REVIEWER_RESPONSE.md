# Task 3 reviewer response: current verified scope

Updated September 13, 2026. Status: **internal comparison verified; synchronized official external-method benchmark OPEN**.

The preceding response claimed full resolution, independent source-model replication, and universal superiority without the required evidence. It is superseded, not a current scientific authority. Its exact pre-edit bytes remain at `experiments/kbound/results/task_closure_20260911/manuscript_before_20260913/TASK3_REVIEWER_RESPONSE.md`. Historical numerical artifacts are unchanged.

## 1. Official POEM, AETTA, and TTA/ALine comparison

The current paper does not claim this requirement is complete.

- **POEM:** two reproduced real-image CPU engineering runs using the full pretrained ResNet-50-GN and authenticated native components. The reduced source-reference sample, fixed engineering stream, and disclosed CPU harness are not the complete published protocol.
- **AETTA:** two reproduced real-image CPU runs of its native dropout estimator and error update on the full frozen ResNet-50 ImageNet V2 model. No target-accuracy result or adaptation-controller comparison was measured. Saturated estimates do not establish estimator usefulness.
- **Baek Agreement-on-the-Line:** authenticated native computational functions passed conformance checks. A real aligned multi-model prediction bank is still required.
- **Kim TTA/ALine:** the advertised official source was unavailable in the recorded source investigation. Baek ALine is a distinct method, not a replacement for the Kim source requirement. No substitute is presented as official Kim execution.

The POEM and AETTA engineering streams share selected target image identities, but use different checkpoints and method roles. They are not a fair head-to-head benchmark. No official benchmark result is inferred from successful imports, source authentication, or smoke tests.

## 2. Synchronization and calibration

The maintained CIFAR comparison derives internal fixed and gating policies from the same authenticated paired candidate records. Its current fit/calibrate/score construction separates the scored outcomes from their decision path; point-benefit gating and KGA use the same fitted predictions. The retained historical pooled leave-one-out procedure is explicitly separate and does not establish outcome-disjoint evaluation.

Five stream/adaptation repetitions around one CIFAR source checkpoint are not five independently trained source models. Cell-level outcome exclusion also does not establish unseen-environment independence or prospective validity.

The older explanation that different candidate trajectories account for the conflicting synchronized-table oracle regrets was not supported by the inspected paired arrays. The maintained arithmetic table is regenerated from its declared shared authority; older tables remain historical rather than being edited to agree.

## 3. What the current comparison supports

Always Adapt, Always Freeze, simple internal threshold controls, point-benefit regression, and KGA are compared within the stated retrospective panel. Current numerical authority and per-cell output are:

`docs/research/kbound/paper/generated/current_cifar_baselines_20260912/`

In the current Tent comparison KGA has fewer observed false adaptations than the shared-prediction point gate, while the point gate has better point accuracy and regret. An observed zero error count is not a universal guarantee. Historical POEM/AETTA/ALine port labels do not establish faithful execution of those published algorithms.

The ImageNet local diagnostic remains a specified noise subset, not the entire ImageNet-C benchmark. POEM is an adapter; AETTA and ALine are accuracy estimators. Any estimator-to-routing controller must have its prediction timing and decision rule fixed explicitly before scoring. POEM's actions cannot be evaluated using another adapter's candidate outcomes.

## 4. Evidence still required for closure

1. A reviewed shared executor and fixed manifest for conditions, examples, preprocessing, checkpoint identities, seeds, resets, candidate settings, prediction timing, and metrics.
2. Authenticated paired native estimator outputs and a compatible real model bank, with an explicit estimator-to-controller contract.
3. Completion of official method identity requirements, including Kim code or an explicitly revised, clearly labeled reproduction requirement.
4. Full execution records and independent scoring on that panel, including whole-pipeline outcome-exclusion checks; environment-level tests before environment-held-out claims.
5. Regenerated manuscript tables and final release verification bound to that execution.

Clean validation label paths have been checked for 50,000 images/1,000 classes against the official development kit. This is not authentication of every image byte. CUDA absence is no longer a blanket explanation: scoped CPU paths execute. Remaining work is experimental integration, comparable inputs, source identity, computation, and verification.

## Supporting execution records

Paths below are relative to `experiments/kbound/results/task_closure_20260911/`:

- `POEM_REAL_IMAGE_REPRODUCTION_REVIEW_20260912.json`
- `AETTA_REAL_IMAGE_REPRODUCTION_REVIEW_20260912.json`
- `ALINE_RUNTIME_STAGE_INDEPENDENT_REREVIEW_20260912.json`
- `KIM_TTALINE_SOURCE_IDENTITY_20260913T032825Z.json`
- `NATIVE_EXECUTION_HANDOFF_20260912.md`

These receipts have different scopes. Their PASS labels do not combine into a completed official benchmark.
