# Retired workflow — current release route

This workflow is retired. It is not a current training instruction, completion
report, or evidence of publication readiness. No old experiment should be
restarted from this page.

The original pinned workflow remains in the historical archive at
`docs/research/kbound/archive/superseded_empirical_authorities_2026-09-02/retired_tree/docs/research/kbound/reports/READINESS_85PLUS.md`.
The pre-reconciliation local version is also preserved under the matching path in
`output/verification/archive-reconciliation.JkWiTI/before/`.

Current release verification, when resource and source gates permit:

```sh
bash docs/research/kbound/runbooks/release_candidate.sh all
```

This command does not authorize training, missing-data downloads, or publication.
Use `docs/research/kbound/ACTIVE_NATURAL_STUDY_PLAN.md` for the living execution
status and `docs/research/kbound/claim_ledger.json` for recorded claim boundaries.
These routes are not a declaration that all release gates have passed.
