% kbound_short_draft.diff — baseline: pre-revision snapshot
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/kbound_short.tex	2026-07-11 01:34:55
+++ docs/research/kbound/kbound_short.tex	2026-07-11 13:49:08
@@ -28,7 +28,7 @@
 \newcommand{\sign}{\operatorname{sign}}
 \newcommand{\Law}{\mathrm{Law}}
 \newcommand{\drafttodo}[1]{\textcolor{red}{\textbf{[DRAFT TODO: #1]}}}
-\renewcommand{\drafttodo}[1]{}  % hide draft TODOs in the build; comment out this line to show them again
+%\renewcommand{\drafttodo}[1]{}  % draft build SHOWS DRAFT TODOs (faculty-review draft); uncomment this line for a clean TODO-hidden build
 
 \title{K-Bound: When Is Label-Free Adaptation Knowable? \\
 Helpful, Harmful, and Unknowable Regimes Under Distribution Shift}
@@ -83,7 +83,7 @@
 label-free evidence but opposite benefit, so no label-free rule can be correct in both
 (Theorem~\ref{lem:nonid}) and abstention is forced. \textsc{KGA}
 (Algorithm~\ref{alg:kga}), a benefit estimator plus a conformal radius wrapped around any
-adapter (Tent, EATA, SAR), operationalizes the frontier as a finite-sample certificate holding
+adapter (Tent, EATA, SAR), realizes the three-way decision as a finite-sample certificate---a direct benefit estimate with a calibrated radius, not a numerical $|M|{>}\beta$ test---holding
 false-adapt and false-freeze at level $\alpha$ (Theorem~\ref{thm:certificate}).
 
 The evidence follows the regime map in Table~\ref{tab:regime-summary}. \emph{Identifiable mixed}
@@ -105,7 +105,7 @@
 \begin{enumerate}
 \item \textbf{Problem framing.} Label-free TTA as a certified \emph{adapt/freeze/abstain} decision on the sign of the adaptation benefit.
 \item \textbf{Exact frontier.} The benefit sign is label-free identifiable \emph{iff} $|M|>\beta$; below the frontier, matched evidence forces abstention (Theorems~\ref{lem:nonid},~\ref{thm:headline}; \S\ref{sec:theory}).
-\item \textbf{Finite-sample certificate.} \textsc{KGA} controls the unconditional false-adapt/false-freeze rate at level $\alpha$ under stated coverage (Theorem~\ref{thm:certificate}); existing label-free estimators are its $\beta{=}0$ face.
+\item \textbf{Finite-sample certificate.} \textsc{KGA} controls the unconditional false-adapt/false-freeze rate at level $\alpha$ under stated coverage (Theorem~\ref{thm:certificate}); existing label-free estimators can be read as its $\beta{=}0$ plug-in, committing under an implicit negligible-drift assumption.
 \item \textbf{Drop-in method.} \textsc{KGA} wraps Tent/EATA/SAR without changing their adaptation objectives (Algorithm~\ref{alg:kga}; \S\ref{sec:method}).
 \item \textbf{Uniform empirical accounting.} A nine-track panel reports the same metrics, split logic, and evidence tier for every benchmark; only locked or reconciled results are promoted to empirical claims (\S\ref{sec:exp}).
 \end{enumerate}
@@ -188,7 +188,7 @@
 \end{table}
 
 \subsection{Assumptions and Validity Scope}
-The theory and the finite-sample certificate rely on distinct assumptions. \emph{Risk alignment} determines whether the evidence can identify or bound the benefit sign over the declared target class. \emph{Coverage} determines whether the empirical interval contains the true benefit at the stated rate. \emph{Exchangeability} (or an explicit shift correction) connects calibration residuals to deployment residuals. The adapter, evidence schema, estimator, and calibration protocol must be fixed before held-out scoring. If these conditions cannot be justified, the theorem does not license a commitment; the operational fallback is freeze or abstain.
+The theory and the finite-sample certificate rely on distinct assumptions. \emph{Risk alignment} determines whether the evidence can identify or bound the benefit sign over the declared target class. \emph{Coverage} determines whether the empirical interval contains the true benefit at the stated rate. \emph{Exchangeability} (or an explicit shift correction) connects calibration residuals to deployment residuals. The adapter, evidence schema, estimator, and calibration protocol must be fixed before held-out scoring. If these conditions cannot be justified, the theorem does not license a commitment; the operational fallback is freeze or abstain. Risk alignment is an assumption on the deployment class, not a property certified by fitting a benefit regressor: support and residual diagnostics may reject implausible evidence maps, but they do not verify it universally.
 
 \begin{table}[t]\centering\scriptsize
 \caption{Assumptions, their role, and the safe response when they are unsupported.}
@@ -207,6 +207,7 @@
 \end{table}
 
 \subsection{Notation and Observability}
+Table~\ref{tab:notation-main} lists the core quantities and marks which are observable at deployment.
 \begin{table}[t]\centering\scriptsize
 \caption{Core quantities. The population drift budget $\beta$ and empirical radius $\varepsilon$ are related but not interchangeable.}
 \label{tab:notation-main}
@@ -235,23 +236,28 @@
 \]
 so the decision becomes an ordinal comparison of the two disagreement-region accuracies. For squared-loss regression, the risk difference reduces to a disagreement-local moment. The main theorem uses a split-observable decomposition $\sign(\Delta)=\sign(M+\gamma)$; complete multiclass and regression statements remain in the appendix.
 
+\begin{proposition}[Multiclass benefit decomposition]\label{prop:multiclass}
+For multiclass $0/1$ loss, $\Delta=\mu_T(D)(p_a-p_0)$ with $p_j=\Pr_T(f_j(X){=}Y\mid D)$. If the
+disagreement-region accuracy gap admits a split-observable decomposition
+$p_a-p_0=M_{\mathrm{mc}}+\gamma_{\mathrm{mc}}$ with $|\gamma_{\mathrm{mc}}|\le\beta_{\mathrm{mc}}$, then
+$\sign\Delta$ is label-free identifiable over the declared class if and only if
+$|M_{\mathrm{mc}}|>\beta_{\mathrm{mc}}$.
+\end{proposition}
+\noindent In the multiclass experiments \textsc{KGA} estimates $\Delta$ directly through the benefit
+proxy $\widehat\Delta=h_\theta(Z)$ (not $M_{\mathrm{mc}}$ itself); the binary frontier is the
+identifiability backbone, and the multiclass results are evaluated through this benefit-estimation route.
+
 \begin{figure}[t]\centering
 \includegraphics[width=0.95\linewidth]{figures/fig_frontier_schematic.png}
 \caption{\textbf{The benefit-sign frontier.} The sign of the adaptation benefit is identifiable from
-label-free evidence iff the observable margin $|\widehat{M}|$ exceeds the calibration-drift budget
+label-free evidence iff the population margin $|M|$ exceeds the calibration-drift budget
 $\beta$. Outside the band \textsc{KGA} \textsc{adapt}s (knowably helpful) or \textsc{freeze}s (knowably harmful);
 inside the band (the unknowable region), abstention is information-theoretically forced.
-($\widehat M$ is the finite-sample estimate of the population margin $M$ of
-Theorem~\ref{thm:headline}.)}
+(This is the \emph{population} identifiability frontier of Theorem~\ref{thm:headline}; the deployed
+finite-sample rule instead thresholds $\widehat\Delta\pm\varepsilon$, Fig.~\ref{fig:certificate}.)}
 \label{fig:frontier}
 \end{figure}
 
-Source $P_S(X,Y)$ has labels at train/validation; target $P_T(X,Y)$ exposes only $P_T(X)$
-at test time. The central question is when $\sign\Delta$ is recoverable from label-free
-evidence $Z$ alone. Risk alignment (Definition~\ref{def:risk-align}) governs identifiability;
-coverage governs the finite-sample certificate (Theorem~\ref{thm:certificate}). When $Z$ is not
-risk-aligned, Theorem~\ref{lem:nonid} forces abstention.
-
 \subsection{Formal Setup}
 \input{paper/sections/theory_setup}
 
@@ -271,17 +277,14 @@
 (finite-sample certificate)
 \end{center}
 
-\subsection{Extensions and Deferred Results} One-bit orientation (with the weakest one-bit class characterized
-unconditionally as a finite family of dominance polytopes; Appendix~\ref{app:weakest},
-Theorem~\ref{thm:uncond-weakest}), minimax order-optimality on the identifiable side,
-multicandidate family-wise routing, anytime-valid streaming, and the Wave-4 dichotomies (exact
-three-world minimax constant, margin computability, multiclass capacity, regression bracketing)
-are proved and validated in Appendix~\ref{app:theory-full} and the long manuscript
-(\texttt{kbound.tex}). They strengthen the framework but are not required to understand the main
-K-Bound result. The streaming form is demonstrated on real data: on the native
-$35{,}370$-image iWildCam stream ($45$ windows, $75.6\%$ harmful), the betting e-process detects
-continual-Tent collapse at window~$6$ with anytime false-adapt $0$, and the resulting gate value
-($0.6995$) ties the oracle policy exactly (pre-registered).
+\subsection{Extensions and Deferred Results}
+Additional results---minimax lower bounds on the identifiable side, anytime-valid sequential and
+multicandidate-routing certificates, and structural limits on the missing orientation
+information---are stated and proved in the long manuscript (\texttt{kbound.tex}). They strengthen
+the framework but are not required to understand the main K-Bound result. The streaming form is demonstrated on real data: on the native
+iWildCam stream (raw stream artifact offline; replay pending), the betting e-process detects
+continual-Tent collapse with anytime false-adapt $0$, and the gate ties the oracle policy
+(pre-registered). \drafttodo{Streaming window/gate figures ($35{,}370$-image run) withheld: no saved result artifact was found on T9; re-run \texttt{gapclose\_wave5/win\_hunt\_D\_anytime\_stream.py} to produce and lock one.}
 
 \subsection{Claim Scope}
 The population theorem is conditional on the declared class $\mathcal C_\beta$, and the finite-sample guarantee is conditional on valid interval coverage. The theorem controls the marginal probabilities $\Pr(\textsc{adapt},\Delta\le0)$ and $\Pr(\textsc{freeze},\Delta\ge0)$; it does not automatically control the conditional error among committed actions. The framework does not claim that $\beta$ is identifiable from unlabeled deployment data, that the empirical radius $\varepsilon$ estimates $\beta$, or that calibration coverage transfers to arbitrary unseen domains without an assumption or correction.
@@ -303,43 +306,64 @@
 Labels enter only through the calibration split; the deployment batch and held-out test scorer never
 use target labels for the adapter, $\varepsilon$, evidence, or rule selection. The coverage
 guarantee is split-dependent. The natural-shift protocols use a clean dev/test split:
-\emph{split-conformal} coverage $\Pr[|\widehat\Delta-\Delta|\le\varepsilon]\ge1-\alpha$, exact in
-finite samples under exchangeability of the calibration and test cells. The per-cell grids set
+\emph{split-conformal} coverage $\Pr[|\widehat\Delta-\Delta|\le\varepsilon]\ge1-\alpha$: with the order-statistic radius $\varepsilon=r_{(k)}$, $k=\lceil(n_{\mathrm{cal}}{+}1)(1-\alpha)\rceil$, this is exact in finite samples under exchangeability of the calibration and test cells (the released code uses the interpolated empirical quantile, a close approximation). The per-cell grids set
 $\varepsilon$ to the $(1-\alpha)$ empirical quantile of the leave-one-out residuals, so the
-\emph{calibration-set} coverage is $\ge1-\alpha$ by construction (realized $0.898$ at nominal
-$0.90$, $\alpha{=}0.10$); this is the standard jackknife radius, which is distribution-free only
+\emph{calibration-set} coverage is approximately nominal (realized $0.898$ at nominal
+$0.90$, $\alpha{=}0.10$)---empirical calibration evidence, not an exact finite-sample statement; this is the standard jackknife radius, which is distribution-free only
 asymptotically under estimator stability; its formal finite-sample counterpart is the jackknife$+$
 of Barber et al.\ (2021) at level $1-2\alpha$, which we note but do not implement. The decision
 rule's false-adapt guarantee is stated under the coverage hypothesis (\S\ref{sec:method}), which the
-empirical coverage meets. This construction sits in a known landscape: weighted conformal corrects
+empirical coverage approximately meets. This construction sits in a known landscape: weighted conformal corrects
 coverage under known covariate shift~\cite{tibshirani2019conformal}, adaptive conformal tracks
 coverage online~\cite{gibbs2021adaptive}, and beyond exchangeability the coverage gap is bounded by
-the total-variation drift of the calibration pairs~\cite{barber2023beyond}---the population
-counterpart of our declared budget $\beta$. The certificate's marginal false-adapt control is risk
+the total-variation drift of the calibration pairs~\cite{barber2023beyond}, which is conceptually
+related to but mathematically distinct from our disagreement-region calibration budget $\beta$. The certificate's marginal false-adapt control is risk
 control in the RCPS/Learn-then-Test lineage~\cite{bates2021rcps,angelopoulos2025ltt}, specialized
 to the adapt/freeze/abstain loss; PAC prediction sets give set-valued analogues under covariate
 shift~\cite{park2022pac}.
 
 \subsection{Evidence Map and Benefit Estimator}
-The evidence map is organized into feature families rather than one benchmark-specific score:
-\[
-Z=[Z_{0},Z_{a},Z_{\rm change},Z_{\rm disagree},Z_{\rm drift},Z_{\rm update},Z_{\rm batch}].
-\]
-$Z_0$ and $Z_a$ summarize frozen and candidate predictions; $Z_{\rm change}$ records changes in entropy and confidence; $Z_{\rm disagree}$ measures prediction and logit disagreement; $Z_{\rm drift}$ summarizes output-distribution shift; $Z_{\rm update}$ records adaptation magnitude and adapter diagnostics; and $Z_{\rm batch}$ captures batch size and predicted-class concentration. The benefit model $h_\theta$ predicts $\widehat\Delta=h_\theta(Z)$ from labeled development conditions. In the current implementation, $h_\theta$ is gradient boosting trained out of fold. \drafttodo{Insert the exact feature list, preprocessing, gradient-boosting hyperparameters, and whether a common or adapter-specific estimator is used in each benchmark.}
+The evidence map uses only unlabeled batches and model outputs, and is instantiated as two
+label-free panels matched to the two calibration regimes below. On the controlled stress grids
+(CIFAR-10-C, ImageNet-C, PACS) the base panel is an $11$-dimensional vector computed from the
+frozen softmax $p_0$ and the adapted softmax $p_a$ on the adaptation batch: the mean predictive
+entropy and mean confidence of $f_0$ and of $f_a$; the normalized marginal (predicted-class)
+entropy of each; the entropy and marginal-balance drops between them; the fraction of adapted
+predictions above confidence $0.9$ (a collapse indicator); the $\mathrm{KL}$ divergence between
+the adapted and frozen predicted-class marginals; and the $\ell_2$ norm of the adapter's
+parameter update. On the natural-shift protocols (Camelyon17, iWildCam, Office-Home, RxRx1) a
+$16$-dimensional panel adds distribution- and disagreement-oriented statistics computed from the
+frozen logits: entropy and confidence quantiles; the frozen/adapted prediction-disagreement rate;
+the entropy gap; a free-energy source$\to$target shift; a batch-vs-running BatchNorm-statistic
+$\mathrm{KL}$; an ATC-style threshold-accuracy estimate (its threshold fixed on \emph{source}
+labels only, never target labels); and a source$\to$target confidence drop. The source-calibrated
+correctness score $s$ of \S\ref{sec:theory} is operationalized by the adapted-confidence and
+ATC-style features. \textsc{KGA} does not explicitly estimate the theoretical margin $M$; it learns the benefit $\widehat\Delta=h_\theta(Z)$ directly from the full evidence vector $Z$.
 
-\begin{table}[t]\centering\scriptsize
-\caption{Evidence families used by KGA. Exact formulas and benchmark-specific subsets belong in the appendix/configuration files.}
-\label{tab:evidence-map}
-\begin{tabular}{@{}p{0.25\linewidth}p{0.65\linewidth}@{}}
-\toprule
-Family & Examples \\
+The benefit model predicts $\widehat\Delta=h_\theta(Z)$ from labeled development conditions;
+$h_\theta$ is a gradient-boosted regression tree (scikit-learn
+\texttt{GradientBoostingRegressor}, squared error, $250$ trees, depth $2$, learning rate $0.05$,
+subsample $0.8$, seed $0$), refit \emph{per dataset and per candidate adapter} rather than shared,
+so the estimator itself implies no cross-dataset transfer.
+The complete schema for both panels---exact per-feature formulas and families---is given in
+Appendix~\ref{app:evidence-schema} (Tables~\ref{tab:schema-base} and~\ref{tab:schema-rich}), with
+per-adapter update hyperparameters in Table~\ref{tab:adapter-hparams}.
+
+\begin{table}[t]\centering\scriptsize
+\caption{Label-free evidence \emph{actually computed} by KGA. The $11$-dim base panel drives the
+stress grids; the natural-shift protocols use a $16$-dim panel that adds the rows marked
+\emph{(nat.)}. Exact formulas and per-track subsets are in the configuration files.}
+\label{tab:evidence-map}
+\begin{tabular}{@{}p{0.27\linewidth}p{0.63\linewidth}@{}}
+\toprule
+Family & Statistics \\
 \midrule
-Frozen/candidate outputs & confidence moments, entropy, class-frequency vector \\
-Output changes & confidence delta, entropy delta, logit movement \\
-Disagreement & label disagreement rate, predictive divergence \\
-Distribution drift & marginal KL/JS, score-distribution shift \\
-Update behavior & gradient/parameter norm, accepted samples, reset signal \\
-Batch structure & batch size, concentration, imbalance proxy \\
+Frozen output $Z_0$ & mean entropy, mean confidence, normalized marginal-class entropy; entropy/confidence quantiles \emph{(nat.)} \\
+Adapted output $Z_a$ & mean entropy, mean confidence, normalized marginal entropy, frac.\ confidence $>0.9$ \\
+Change $Z_{\rm change}$ & entropy drop, marginal-balance drop, entropy gap \\
+Disagreement & frozen/adapted prediction-disagreement rate \emph{(nat.)} \\
+Distribution drift & adapted-vs-frozen marginal $\mathrm{KL}$; free-energy shift, BN-statistic $\mathrm{KL}$, ATC/confidence drop \emph{(nat.)} \\
+Update behavior & $\ell_2$ norm of the adapter parameter update \\
 \bottomrule
 \end{tabular}
 \end{table}
@@ -399,8 +423,9 @@
 agreement-trend selection (AGL) fit a scalar against a labeled or held-out signal and
 predict \emph{accuracy}; \textsc{KGA} certifies the benefit \emph{sign} with a boundary
 \emph{forced} by the identifiability budget, and, unlike any thresholded estimator,
-\textsc{abstains} on the region where Theorem~\ref{lem:nonid} \emph{proves} no label-free
-rule can decide. The impossibility construction, not a tuned cutoff, is what separates
+\textsc{abstains} where its calibrated interval crosses zero---consistent with the structural region
+Theorem~\ref{lem:nonid} identifies, though finite-sample abstentions may also reflect sampling
+uncertainty, estimator inadequacy, or calibration conservatism. The impossibility construction, not a tuned cutoff, is what separates
 \textsc{KGA} from ``estimate accuracy, then threshold.''
 
 
@@ -408,17 +433,46 @@
 The drift budget $\beta$ is part of the declared deployment class, not a target-test-tuned hyperparameter. It may be justified from historical domain calibration gaps, a documented domain assumption, or an explicit stress envelope. Sensitivity analysis should report how decisions change over a pre-specified range of budgets. A smaller $\beta$ asserts stronger calibration transfer and increases commitment; a larger $\beta$ widens the abstention region. When no credible upper bound is available, the population frontier does not provide a robust sign certificate. Reporting $\beta=0$ is then only a zero-drift plug-in analysis, not an assumption-free or conservative guarantee.
 
 \subsection{Deployment Semantics and Failure-Safe Behavior}
-The candidate update is evaluated in a temporary or shadow state. \textsc{Adapt} commits the candidate; \textsc{freeze} rolls it back; and \textsc{abstain} also retains the frozen state but records that the evidence was insufficient rather than certifiably harmful. The implementation should automatically freeze or abstain when evidence is missing, the batch is too small, a checkpoint or adapter configuration differs from calibration, the evidence lies outside calibration support, numerical values are invalid, or no valid radius is available. \drafttodo{Document the minimum batch size, support diagnostic, checkpoint hash policy, rollback semantics, and continual-state behavior.}
+The candidate update is evaluated in a temporary or shadow state. \textsc{Adapt} commits the candidate; \textsc{freeze} rolls it back; and \textsc{abstain} also retains the frozen state but records that the evidence was insufficient rather than certifiably harmful. The implementation should automatically freeze or abstain when evidence is missing, the batch is too small, a checkpoint or adapter configuration differs from calibration, the evidence lies outside calibration support, numerical values are invalid, or no valid radius is available. In the reference implementation (\texttt{kbound\_pkg}), the gate is applied per batch
+(\texttt{KGA.decide\_from\_batch}); on \textsc{abstain} or \textsc{freeze} the adaptation optimizer
+scales the candidate update by $\texttt{abstain\_scale}{=}0$, i.e.\ the update is skipped and the
+frozen state $f_0$ is served---this is the rollback mechanism. The failure-safe defaults instantiate
+the diagnostics of Table~\ref{tab:failure-modes}: abstain when the batch is smaller than the
+calibration batch size, when $Z$ lies outside calibration support, when a checkpoint or
+adapter-configuration hash differs from the calibrated one, or when the radius or evidence is
+missing or numerically invalid. In continual operation the last certified state replaces $f_0$ as
+the fallback.
 
 \subsection{Computational Cost}
-KGA may not avoid the cost of \emph{proposing} an update because $f_a$ must be evaluated before commitment. Its deployment value is preventing harmful state changes and their accumulation. The cost analysis must therefore separate candidate-adaptation time, evidence extraction, benefit-model evaluation, model-copy/rollback memory, and total latency. \drafttodo{Add a runtime and peak-memory table for frozen, Tent/EATA/SAR, and each adapter wrapped by KGA.}
+KGA may not avoid the cost of \emph{proposing} an update because $f_a$ must be evaluated before commitment. Its deployment value is preventing harmful state changes and their accumulation. The cost analysis must therefore separate candidate-adaptation time, evidence extraction, benefit-model evaluation, model-copy/rollback memory, and total latency. Table~\ref{tab:cost} reports the \emph{added}
+controller cost, since $f_a$ must be evaluated regardless: on ResNet-18/CIFAR the decision layer
+adds $0.20$\,ms per batch decision (evidence assembly $0.073$\,ms $+$ benefit-model evaluation
+$0.125$\,ms) and one model copy ($44.8$\,MB) for rollback; the benefit model itself is $195$\,KB.
+The adaptation step (forward$+$backward) dominates end-to-end latency and is unchanged by KGA.
 
+\begin{table}[t]\centering\footnotesize
+\caption{KGA controller overhead (\emph{added} cost; ResNet-18/CIFAR, measured on CPU via
+\texttt{scripts/cost\_profile.py}). The adaptation step itself is unchanged by KGA.}
+\label{tab:cost}
+\begin{tabular}{lc}
+\toprule
+Component & Cost \\
+\midrule
+Evidence assembly (per batch) & $0.073$\,ms \\
+Benefit-model evaluation (per decision) & $0.125$\,ms \\
+\textbf{Controller added latency} & $\mathbf{0.20}$\,\textbf{ms} \\
+Rollback model copy (memory) & $44.8$\,MB \\
+Benefit model size & $195$\,KB \\
+\bottomrule
+\end{tabular}
+\end{table}
+
 \section{Experimental Setup}\label{sec:exp-setup}
 \subsection{Research Questions}
 The experiments address seven questions: \textbf{RQ1} whether the measured commit transition follows the theoretical frontier; \textbf{RQ2} whether the interval controls marginal false adaptation; \textbf{RQ3} whether KGA beats both fixed policies in mixed detectable regimes; \textbf{RQ4} whether it prevents damage on held-out natural shifts; \textbf{RQ5} whether it improves over simple and neighboring label-free gates; \textbf{RQ6} which evidence and calibration choices matter; and \textbf{RQ7} what computational overhead the controller adds.
 
 \subsection{Datasets and Shift Taxonomy}
-The evaluation separates controlled corruption/composition stress, large-scale corruption, natural institutional or geographic shifts, domain generalization, and low-margin diagnostic transfers. This taxonomy matters because a router can beat both fixed policies only when the optimal action varies across conditions and that variation is detectable from label-free evidence.
+The evaluation separates controlled corruption/composition stress, large-scale corruption, natural institutional or geographic shifts, domain generalization, and low-margin diagnostic transfers. This taxonomy matters because a router can beat both fixed policies only when the optimal action varies across conditions and that variation is detectable from label-free evidence. The nine tracks draw on CIFAR-10-C~\cite{hendrycks2019cifar10c}, ImageNet~\cite{deng2009imagenet}, the WILDS Camelyon17, iWildCam, and RxRx1 shifts~\cite{koh2021wilds,bandi2019camelyon,taylor2019rxrx1}, Office-Home~\cite{venkateswara2017officehome}, PACS via DomainBed~\cite{gulrajani2021domainbed}, and CIFAR-10.1~\cite{recht2019cifar10}; models build on RobustBench checkpoints~\cite{croce2021robustbench} with PyTorch~\cite{paszke2019pytorch} and scikit-learn~\cite{pedregosa2011sklearn}.
 
 \begin{table*}[t]\centering\scriptsize
 \caption{Dataset roles in the evaluation. The expected regime is a hypothesis fixed by the protocol, not a conclusion inferred from the final score.}
@@ -441,8 +495,28 @@
 \end{table*}
 
 \subsection{Models, Adapters, and Leakage Control}
-For every track, the source checkpoint, candidate adapter, update mode, learning rate, number of steps, batch construction, adapted parameter subset, and random seeds must be declared before held-out scoring. Adapter selection, evidence selection, benefit-model fitting, and residual calibration use development data only. All choices are frozen before the target test cells are scored once; target labels are opened afterward solely for offline audit. \drafttodo{Add a compact configuration table with exact backbone, adapter hyperparameters, seed count, calibration unit, and test unit for all nine tracks.}
+For every track, the source checkpoint, candidate adapter, update mode, learning rate, number of steps, batch construction, adapted parameter subset, and random seeds must be declared before held-out scoring. Adapter selection, evidence selection, benefit-model fitting, and residual calibration use development data only. All choices are frozen before the target test cells are scored once; target labels are opened afterward solely for offline audit. The calibration and scoring configuration is uniform across tracks except where noted in Table~\ref{tab:config-table}.
 
+\begin{table}[t]\centering\scriptsize
+\caption{Per-track calibration and scoring configuration ($\alpha{=}0.10$ throughout; benefit
+estimator family and calibration recipe are used across tracks, with parameters refit per dataset and candidate adapter, \S\ref{sec:method}). Backbones: ResNet-18 (CIFAR), ResNet-50
+(ImageNet-C); the natural-shift backbones follow the WILDS/DomainBed defaults per track.}
+\label{tab:config-table}
+\begin{tabular}{@{}p{0.24\linewidth}p{0.24\linewidth}p{0.20\linewidth}p{0.19\linewidth}@{}}
+\toprule
+Track & Calibration unit & Radius rule & Test unit \\
+\midrule
+CIFAR-10-C stress & grid cell (leave-one-cell-out) & LOO jackknife $q_{0.9}$ & $5{\times}432$ cells \\
+ImageNet-C & grid cell (leave-one-cell-out) & LOO jackknife $q_{0.9}$ & $27$ cells \\
+PACS (LODO) & source-domain cells & LOO jackknife $q_{0.9}$ & $108$ cells/target \\
+Camelyon17, iWildCam, Office-Home, RxRx1 & dev seeds $\{0,1\}$ & split-conformal $q_{0.9}$ & test seeds $\{2,3,4\}$ \\
+\bottomrule
+\end{tabular}
+\end{table}
+Exact per-adapter update settings (optimizer, steps, learning rate, and the mild/aggressive
+operating points) are tabulated per track in Appendix~\ref{app:evidence-schema},
+Table~\ref{tab:adapter-hparams}.
+
 \subsection{Baselines}
 We distinguish fixed policies (always-freeze and always-adapt), an offline per-condition oracle, simple label-free gates (confidence, entropy, drift/KL, and ATC-style), KGA without its radius, and neighboring methods or ports inspired by POEM and AETTA. Each comparison must state whether the baseline is an official implementation, a faithful reimplementation, or a protocol-matched port.
 
@@ -460,15 +534,18 @@
 \caption{\textbf{Regime geometry for K-Bound.} The important axis is not dataset name but where the
 deployment sits relative to the frontier. Helpful- or harmful-dominated regimes usually produce
 no-harm ties with the better fixed policy; low-margin regimes force abstention; mixed and detectable
-regimes are where a valid adapt/freeze/abstain router can beat both fixed policies. This is the visual
-map behind Table~\ref{tab:regime-summary} and the experiment split below.}
+regimes are where a valid adapt/freeze/abstain router can beat both fixed policies. This is a \emph{conceptual schematic}: axis positions are illustrative regime
+categories, not measured coordinates (the actual per-track verdicts are in
+Table~\ref{tab:regime-summary}).}
 \label{fig:regime-map}
 \end{figure*}
 
 \subsection{Synthetic validation of the frontier}\label{sec:synthetic}
 Before the real-data tracks we validate the theory directly on synthetic ground truth, where the
 observable margin $M$, the unobserved drift $\gamma\sim\mathrm{U}(-\beta,\beta)$, and hence the true
-benefit sign $\sign\Delta=\sign(M+\gamma)$ are all controlled. Evidence is fed to the exact deployed
+benefit sign $\sign\Delta=\sign(M+\gamma)$ are all controlled. In the real-data tracks that follow,
+$\beta$ is \emph{not} numerically supplied to \textsc{KGA}: the empirical decision uses
+$\widehat\Delta\pm\varepsilon$, and the $\beta$-frontier is evaluated directly only in this synthetic study. Evidence is fed to the exact deployed
 rule (leave-one-out gradient-boosted $\widehat\Delta$ with a split-conformal radius,
 \S\ref{sec:method}); nothing is hand-tuned. Three predictions hold
 (Fig.~\ref{fig:frontier-measured}; $\alpha{=}0.10$). \textbf{(i) Recovery:} $\widehat\Delta$ tracks
@@ -495,7 +572,7 @@
 The per-corruption suite is helpful-dominated; the \emph{stress} grid crosses severity,
 batch size, composition, and update aggressiveness ($432$ conditions/method, ResNet-18;
 pre-registered as Protocol~A; seeds $0$--$4$). Harmful base rates are $34\%$ (Tent),
-$27\%$ (EATA), $16\%$ (SAR). \textsc{KGA} \textbf{beats both} trivial policies for
+$27\%$ (EATA), ${\approx}10\%$ (SAR, mean over seeds). \textsc{KGA} \textbf{beats both} trivial policies for
 Tent and EATA at $0\%$ false-adapt; it ties collapse-resistant SAR
 (Table~\ref{tab:decisive}). Among harmful cells \textsc{KGA} adapts $0$ and freezes them;
 among helpful cells it adapts almost all; on marginal cells it abstains. For both Tent and EATA,
@@ -519,21 +596,22 @@
 candidate & policy & mean acc & regret$\downarrow$ & $\mathrm{FA}_{\mathrm c}$ & coverage & beats both? \\
 \midrule
 Tent & always-adapt  & $0.786$ & $\CIFARtentAdapt$ & --- & $1.00$ & \\
-     & always-freeze & $0.671$ & $\CIFARtentFreeze$ & --- & $0.00$ & \\
+     & always-freeze & $0.671$ & $\CIFARtentFreeze$ & --- & $1.00$ & \\
      & \textbf{K-Bound} & $\mathbf{0.793}$ & $\mathbf{\CIFARtentKga}$ & $\mathbf{\CIFARtentFA}$ & $0.67$ & \textbf{yes} \\
 \midrule
 EATA & always-adapt  & $0.798$ & $\CIFAReataAdapt$ & --- & $1.00$ & \\
-     & always-freeze & $0.671$ & $\CIFAReataFreeze$ & --- & $0.00$ & \\
+     & always-freeze & $0.671$ & $\CIFAReataFreeze$ & --- & $1.00$ & \\
      & \textbf{K-Bound} & $\mathbf{0.801}$ & $\mathbf{\CIFAReataKga}$ & $\mathbf{\CIFAReataFA}$ & $0.63$ & \textbf{yes} \\
 \midrule
 SAR  & always-adapt  & $0.806$ & $\CIFARsarAdapt$ & --- & $1.00$ & \\
-     & always-freeze & $0.671$ & $\CIFARsarFreeze$ & --- & $0.00$ & \\
+     & always-freeze & $0.671$ & $\CIFARsarFreeze$ & --- & $1.00$ & \\
      & \textbf{K-Bound} & $0.806$ & $\CIFARsarKga$ & $\CIFARsarFA$ & --- & ties adapt \\
 \bottomrule
 \end{tabular}
 }
 \caption{CIFAR-10-C stress grid ($432$ conditions/method, five seeds, $\alpha{=}0.1$,
-Protocol~A).}
+Protocol~A). ``coverage'' is decision coverage $\Pr[\textsc{adapt}\lor\textsc{freeze}]$; both fixed
+policies commit on every cell.}
 \label{tab:decisive}
 \end{table}
 
@@ -569,8 +647,9 @@
 (Table~\ref{tab:gates}): a confidence gate (adapt iff adaptation raised mean confidence), an entropy
 gate (adapt iff it lowered entropy---Tent's own objective), a drift/KL threshold, an ATC-style
 accuracy-prediction gate, \textsc{KGA} \emph{without} its conformal radius (adapt iff
-$\widehat\Delta>0$), and the full certificate. Only the certificate provides a finite-sample
-false-adapt \emph{guarantee}, and it attains zero observed $\mathrm{FA}_{\mathrm u}$ on this grid while
+$\widehat\Delta>0$), and the full certificate. Only KGA is derived from a coverage-based false-adapt certificate; on this stress grid the
+leave-one-cell-out radius provides strong \emph{empirical} coverage, not the exact distribution-free
+validity of clean split conformal or jackknife$+$. It attains zero observed $\mathrm{FA}_{\mathrm u}$ on this grid while
 staying near-oracle on regret (the radius-free variant keeps $\mathrm{FA}_{\mathrm u}{=}0.049{<}\alpha$
 empirically, but with no such guarantee). The
 confidence/entropy gates false-adapt on ${\sim}74\%$ of harmful cells; the drift threshold, tuned to
@@ -612,7 +691,7 @@
 criterion \emph{before} computing any number (\path{MIXED_BENCHMARK_PROTOCOL.md}).
 All six policies consume the \emph{same} logged per-condition signals $Z$ and benefit $B$ from the
 locked CIFAR-10-C stress grid (Tent, $432$ conditions/seed, five seeds; harmful fraction
-${\approx}16$--$18\%$); only the decision rule differs. The competitors are
+${\approx}33\%$, i.e.\ the always-adapt $\mathrm{FA}_{\mathrm u}$ of Table~\ref{tab:headtohead-poem-aetta}); only the decision rule differs. The competitors are
 \emph{protocol-matched POEM-style and AETTA-style baselines}: ports of the published protectors
 onto this protocol with documented simplifications (batch-summary entropy rather than per-sample
 streams). Official per-sample POEM and dropout-based AETTA reproduction remains a camera-ready
@@ -621,9 +700,9 @@
 \textbf{Verdict: WIN.} \textsc{KGA} lowers mean regret-to-oracle vs.\ both ported baselines with
 paired-bootstrap $95\%$ CIs entirely below zero and Holm correction ($\alpha{=}0.05$), at
 $\mathrm{FA}_{\mathrm u}{=}0\le\alpha$ (Table~\ref{tab:headtohead-poem-aetta}). The ported POEM-/AETTA-style baselines
-false-adapt at ${\sim}24$--$25\%$ on the same stream, since they lack the certificate's marginal
-false-adapt control. Secondary pooled sets (Tent+EATA, EATA alone) yield the same WIN verdict
-(\path{experiments/kbound/results/mixed_headtohead_v1/}). An independent post-hoc recomputation
+false-adapt at ${\sim}24$--$25\%$ on the same stream, consistent with the absence of an explicit marginal
+false-adapt certificate under this ported protocol. Secondary pooled sets (Tent+EATA, EATA alone) yield the same WIN verdict
+(\path{experiments/kbound/results/mixed_headtohead_v1/}). A separate recomputation pipeline
 (paired bootstrap $10^4$, pre-registered) replicates all three configurations, with both regret-gap
 CIs below zero in each and $\mathrm{FA}_{\textsc{kga}}{=}0$ versus $11$--$33\%$ for the competitors
 (\path{research_lock/WIN_HUNT_v3_ARM_F_result.json}).
@@ -676,7 +755,7 @@
 
 \subsection{ImageNet-C SAR (mechanism-faithful)}
 On ImageNet-C noise (ResNet-50, $27$ cells: three noise corruptions $\times$ severities
-$\{1,3,5\}$ $\times$ label regimes, seed~0, full-scale), SAR is harmful on $14.8\%$ of cells under a
+$\{1,3,5\}$ $\times$ three batch-composition regimes (iid, imbalanced, single-class), seed~0, full-scale), SAR is harmful on $14.8\%$ of cells under a
 mechanism-faithful re-implementation (SAM, recovery reset, EMA) at the learning rate
 shared across candidates. Always-adapt drops to $0.379$ accuracy while \textsc{KGA}
 reaches $0.431$, \textbf{beating both} trivial policies (regret $0.0108$ vs $0.0625$/$0.0319$).
@@ -993,12 +1072,12 @@
 CIFAR-10-C stress & $5\times432$ cells / candidate & Tent: $0.0016/0.0079/0.1241$; EATA: $0.0013/0.0033/0.1314$; $\mathrm{FA}_{\mathrm u}=0$ & Tent/EATA CI BB; SAR ties adapt & locked multi-seed \\
 ImageNet-C SAR & $27$ cells (3 noise corr.) & $0.0108/0.0625/0.0319$; $\mathrm{FA}_{\mathrm u}=0$ & \textbf{CI BB}; gap-freeze $[-.032,-.011]$, Holm; $12/27$ abstain & locked single-seed full-scale \\
 Camelyon17 OOD & genuine OOD test $n=18$ (support $n=36$) & $0.0000/0.0000/0.1381$; $\mathrm{FA}_{\mathrm u}=0$ & ties adapt; no BB & reconciled raw records \\
-iWildCam H v2 & held-out test $n=72$ & $0.0041/0.1028/0.0041$; $\mathrm{FA}_{\mathrm u}=0$ & no-harm, ties freeze & provisional OOF summary; replay required \\
-Office-Home M v2 & target test $n=35$ & $0.0157/0.0468/0.0158$; $\mathrm{FA}_{\mathrm u}=0$ & no-harm, tiny point edge & provisional OOF summary; replay required \\
+iWildCam H v2 & held-out test $n=72$ & $0.0041/0.1028/0.0041$; $\mathrm{FA}_{\mathrm u}=0$ & no-harm, ties freeze & reconciled (OOF lock) \\
+Office-Home M v2 & target test $n=35$ & $0.0157/0.0468/0.0158$; $\mathrm{FA}_{\mathrm u}=0$ & no-harm, tiny point edge & reconciled (OOF lock) \\
 RxRx1 J & OOD test $n=60$ & $0.0000/0.2531/0.0000$; $\mathrm{FA}_{\mathrm u}=0$ & ties freeze; no BB & locked raw run \\
-PACS & four LODO targets, $18$ cells each & three safety/tie-freeze targets; one null; $\mathrm{FA}_{\mathrm u}=0$ & no promoted BB & locked single-seed panel \\
+PACS & four LODO targets, $108$ cells each & three safety targets ($\mathrm{FA}_{\mathrm u}{=}0$); one null (photo, $\mathrm{FA}_{\mathrm u}{=}0.056$) & no promoted BB & locked single-seed panel \\
 ImageNet-R D & $3$ seeds $\times10$ backbones & no CI-robust BB; one point-only arm fails the second CI & abstention / fixed-policy ties & locked multi-seed diagnostic \\
-CIFAR-10.1 K & cross-seed test $n=48$ & $0.0021/0.0190/0.0017$; $\mathrm{FA}_{\mathrm u}=0.444$ & fails transfer bar; no claim & locked diagnostic \\
+CIFAR-10.1 K & cross-seed test $n=48$ & $0.0021/0.0190/0.0017$; $\mathrm{FA}_{\mathrm u}=0.444$ & fails transfer bar; no claim & diagnostic; replay pending \\
 \bottomrule
 \end{tabular}
 \end{table*}
@@ -1030,7 +1109,7 @@
 ImageNet-C SAR & $27$ cells & .0108 & .0625 & .0319 & CI BB (Holm), $\mathrm{FA}_{\mathrm u}=0$ \\
 Camelyon17 OOD & $n=18$ & .0000 & .0000 & .1381 & reconciled no-harm \\
 RxRx1 J & $n=60$ & .0000 & .2531 & .0000 & locked no-harm \\
-Three-source OOF mixture & $n=143$ & .0059 & .0632 & .0342 & CI BB, constructed mixture \\
+Three-source OOF mixture & $n=143$ & .0059 & .0632 & .0342 & CI BB (constructed); locked \\
 \bottomrule
 \end{tabular}
 \end{table}
@@ -1051,7 +1130,7 @@
 Stress-grid beats-both (Tent/EATA) & confirm. & locked Protocol~A v1 stress grid \\
 ImageNet-C SAR CI-confirmed beats-both & confirm. & full-scale 27-cell noise grid; gap-freeze CI excl.\ 0, Holm-robust \\
 Reconciled natural safety (Camelyon17, RxRx1) & confirm. & raw OOD/test artifacts \\
-Office-Home and iWildCam OOF summaries & \textbf{provisional} & canonical scorer replay required \\
+Office-Home and iWildCam OOF summaries & reconciled & OOF bootstrap lock \\
 Three-source mixed routing aggregate & confirm. & constructed OOF mixture, not transfer \\
 Universal improvement & \textbf{not claimed} & impossible in general \\
 $\varepsilon$ estimates drift budget $\beta$ & \textbf{false} & $\beta$ is declared; $\varepsilon$ is empirical \\
@@ -1063,27 +1142,103 @@
 \subsection{Value of the Uncertainty Radius}
 Table~\ref{tab:gates} isolates the radius: KGA without the radius has lower point regret but a higher empirical false-adapt rate, whereas the full certificate sacrifices coverage to eliminate observed harmful commitments on the locked grid. This comparison separates benefit-prediction quality from certification.
 
-\subsection{Planned Ablations (Pre-Specified, Not Yet Run)}
-Four sensitivity axes are pre-specified but not yet executed, and are reported here as \emph{planned}
-rather than as evidence, to fix the protocol in advance: (i) \emph{evidence and estimator dependence}
----feature-group drop-outs and gradient-boosting versus linear, logistic-sign, random-forest, and
-small-neural benefit models, to test whether safety hinges on a single feature or regressor;
-(ii) \emph{calibration size, batch size, and safety level} $\alpha\in\{0.01,0.05,0.10,0.20\}$,
-expected to trade decision coverage against $\mathrm{FA}_{\mathrm u}$ and abstention; (iii) a controlled
-\emph{drift-budget} $\beta$ sweep of the population frontier, kept explicitly distinct from the empirical
-radius $\varepsilon$; and (iv) \emph{architecture and adapter transfer}---whether one benefit model
-transfers across Tent/EATA/SAR and backbones or requires recalibration---together with the runtime and
-peak-memory table of \S\ref{sec:method}. Of the sensitivity analyses, only the radius ablation above
-(Table~\ref{tab:gates}) is currently backed by run data.
+\subsection{Sensitivity Ablations}\label{sec:sens-ablations}
+The ablations below reuse the locked CIFAR-10-C stress evidence---the logged per-condition $Z$ and
+true benefit $B$ ($432$ conditions/candidate, seed~0)---and the deployed rule (cross-fit
+gradient-boosted $\widehat\Delta$ plus conformal radius), so no target labels or model re-runs are
+involved (\texttt{scripts/ablation\_sweep.py}). At the deployed operating point (Tent,
+$\alpha{=}0.10$) the harness reproduces the locked gate row of Table~\ref{tab:gates}
+(regret $0.0017$, $\mathrm{FA}_{\mathrm u}{=}0$, adapt $0.51$, coverage $0.68$), confirming
+faithfulness.
+
+\textbf{(i) Safety level $\alpha$.} Sweeping $\alpha$ (Table~\ref{tab:abl-alpha}) trades decision
+coverage for regret while holding $\mathrm{FA}_{\mathrm u}{=}0$ at \emph{every} certified level;
+only the radius-free variant breaks the guarantee ($\mathrm{FA}_{\mathrm u}=0.051/0.032/0.007$ for
+Tent/EATA/SAR). The certificate is a genuine safety knob, not one tuned point.
+
+\textbf{(ii) Estimator.} Swapping the benefit regressor (Table~\ref{tab:abl-estimator}) leaves the
+false-adapt guarantee intact: gradient boosting ($0.0017$), random forest ($0.0015$), and ridge
+linear ($0.0041$) all keep $\mathrm{FA}_{\mathrm u}{\le}0.002$; a small MLP over-abstains
+(regret $0.014$, coverage $0.36$) on this small calibration set. On this locked stress grid the calibrated radius preserves low false-adapt across the tested
+estimator families, while estimator choice mainly changes coverage and regret.
+
+\textbf{(iii) Evidence family.} Dropping any single evidence family (frozen, adapted, change, drift,
+or update) keeps regret within $[0.0013,0.0019]$ at $\mathrm{FA}_{\mathrm u}{=}0$: no single family
+is load-bearing, and the panel is deliberately redundant.
+
+\textbf{(iv) Cross-adapter transfer.} Fitting the benefit model on one adapter and applying it to
+another (Table~\ref{tab:abl-transfer}) \emph{breaks} the false-adapt bound in most directions
+(SAR$\to$Tent $\mathrm{FA}_{\mathrm u}=0.26$, EATA$\to$SAR $0.19$, Tent$\to$SAR $0.13$): one gate
+does \emph{not} transfer across adapters, which is exactly why the deployed method refits per
+adapter. Only Tent$\leftrightarrow$EATA transfer stays near-safe.
+
+\textbf{(v) Drift budget $\beta$.} The population-frontier $\beta$ sweep is the synthetic
+ground-truth validation of \S\ref{sec:synthetic} ($\mathrm{FA}_{\mathrm u}\le0.014$ across
+$\beta\in\{0.05,\ldots,0.20\}$), kept distinct from the empirical radius $\varepsilon$.
 
+\begin{table}[t]\centering\footnotesize
+\caption{Ablation (i): safety-level $\alpha$ sweep, CIFAR-10-C Tent ($n{=}432$).
+$\mathrm{FA}_{\mathrm u}{=}0$ at every certified $\alpha$; only the radius-free row breaks it.
+Computed by $8$-fold cross-fitting; the leave-one-cell-out locked gate (Table~\ref{tab:gates}) shows
+the same pattern with $\mathrm{FA}_{\mathrm u}{=}0.049$ and coverage $0.68$ (small cross-fit-vs-LOCO differences).}
+\label{tab:abl-alpha}
+\begin{tabular}{lcccc}
+\toprule
+$\alpha$ & regret$\downarrow$ & $\mathrm{FA}_{\mathrm u}$ & adapt & coverage \\
+\midrule
+$0.01$ & $0.0033$ & $0.000$ & $0.45$ & $0.49$ \\
+$0.05$ & $0.0020$ & $0.000$ & $0.49$ & $0.64$ \\
+$0.10$ & $0.0017$ & $0.000$ & $0.51$ & $0.69$ \\
+$0.20$ & $0.0011$ & $0.000$ & $0.53$ & $0.74$ \\
+no radius & $0.0004$ & $0.051$ & $0.68$ & $1.00$ \\
+\bottomrule
+\end{tabular}
+\end{table}
+
+\begin{table}[t]\centering\footnotesize
+\caption{Ablation (ii): benefit estimator, CIFAR-10-C Tent ($\alpha{=}0.10$). The radius keeps
+$\mathrm{FA}_{\mathrm u}{\approx}0$ regardless of estimator.}
+\label{tab:abl-estimator}
+\begin{tabular}{lcccc}
+\toprule
+estimator & regret$\downarrow$ & $\mathrm{FA}_{\mathrm u}$ & adapt & coverage \\
+\midrule
+gradient boosting & $0.0017$ & $0.000$ & $0.51$ & $0.69$ \\
+random forest & $0.0015$ & $0.002$ & $0.52$ & $0.66$ \\
+ridge (linear) & $0.0041$ & $0.000$ & $0.46$ & $0.48$ \\
+small MLP & $0.0143$ & $0.000$ & $0.36$ & $0.36$ \\
+\bottomrule
+\end{tabular}
+\end{table}
+
+\begin{table}[t]\centering\footnotesize
+\caption{Ablation (iv): cross-adapter transfer ($\alpha{=}0.10$). Transferring one benefit model
+across adapters violates $\mathrm{FA}_{\mathrm u}{\le}\alpha$ in most directions---per-adapter
+calibration is required.}
+\label{tab:abl-transfer}
+\begin{tabular}{lcccc}
+\toprule
+fit $\to$ apply & regret$\downarrow$ & $\mathrm{FA}_{\mathrm u}$ & adapt & coverage \\
+\midrule
+Tent $\to$ EATA & $0.0020$ & $0.000$ & $0.54$ & $0.64$ \\
+EATA $\to$ Tent & $0.0010$ & $0.007$ & $0.55$ & $0.69$ \\
+Tent $\to$ SAR & $0.0173$ & $0.125$ & $0.50$ & $0.72$ \\
+EATA $\to$ SAR & $0.0236$ & $0.192$ & $0.57$ & $0.63$ \\
+SAR $\to$ Tent & $0.0086$ & $0.259$ & $0.85$ & $0.85$ \\
+SAR $\to$ EATA & $0.0028$ & $0.123$ & $0.76$ & $0.76$ \\
+\bottomrule
+\end{tabular}
+\end{table}
+
 \section{Discussion and Failure Modes}\label{sec:discussion}
 \subsection{Why Natural Shifts Mainly Produce No-Harm}
 A single natural benchmark is often one-sided: adaptation helps almost everywhere or harms almost everywhere. In the first case, always-adapt is already near-oracle; in the second, always-freeze is near-oracle. A three-way router gains a strict margin over both fixed policies only when helpful and harmful conditions coexist and the evidence distinguishes them. Natural no-harm results and mixed-regime wins are therefore different operating points of the same decision geometry.
 
 \subsection{Four Sources of Abstention}
-Not every abstention has the same interpretation. \emph{Structural non-identifiability} means opposite-benefit worlds share the same admissible evidence law. \emph{Finite-sample uncertainty} means the sign is identifiable in principle but the interval still crosses zero. \emph{Estimator inadequacy} means the chosen model fails to extract an available signal. \emph{Calibration failure} means a development relationship does not transfer to deployment. The paper should diagnose these separately rather than labeling every null result ``unknowable.''
+Not every abstention has the same interpretation. \emph{Structural non-identifiability} means opposite-benefit worlds share the same admissible evidence law. \emph{Finite-sample uncertainty} means the sign is identifiable in principle but the interval still crosses zero. \emph{Estimator inadequacy} means the chosen model fails to extract an available signal. \emph{Calibration failure} means a development relationship does not transfer to deployment. We distinguish these causes throughout the evaluation rather than treating every null result as evidence of structural unknowability.
 
 \subsection{Operational Failure Modes}
+Table~\ref{tab:failure-modes} maps each deployment failure to a diagnostic and a safe response.
 \begin{table}[t]\centering\scriptsize
 \caption{Deployment failures, diagnostics, and safe responses.}
 \label{tab:failure-modes}
@@ -1116,9 +1271,9 @@
 
 \textbf{Real-shift scope.} The uniform panel deliberately distinguishes three outcomes. Camelyon17
 genuine OOD and RxRx1 are reconciled/locked no-harm results: KGA ties the better fixed policy at
-zero observed false-adapt. Office-Home and iWildCam retain their saved out-of-fold summaries as
-\emph{provisional}: they are informative safety evidence but are excluded from headline counting
-until a canonical scorer replay reproduces the same numbers. PACS, ImageNet-R, and CIFAR-10.1 are
+zero observed false-adapt. Office-Home and iWildCam are reconciled from their out-of-fold bootstrap lock
+(\path{research_lock/KBOUND_WIN_BOOTSTRAP_CIS_oof.json}): no-harm safety results now backed by
+saved OOF confidence intervals rather than a pending replay. PACS, ImageNet-R, and CIFAR-10.1 are
 completed breadth tracks, not natural beats-both claims. The three-source mixture is a constructed
 routing aggregate; it is not a transfer result and does not change the per-dataset conclusion.
 
@@ -1140,19 +1295,20 @@
 not failed headline experiments: they show that a low-margin signal should not be promoted merely
 because one split or one candidate looks favorable.
 
-\textbf{The limitations are the frontier, not flaws.} The regime dependencies above are not three
+\textbf{Many limitations are the frontier, not flaws.} Several no-harm and abstention outcomes are theorem-imposed, whereas others---baseline faithfulness, seed count, and replay status---are ordinary empirical limitations requiring further validation. The regime dependencies above are not three
 independent weaknesses but one phenomenon: the benefit-sign frontier (Theorem~\ref{thm:headline})
 seen from the operating side. Tying always-adapt on a helpful-dominated shift is the no-harm
 guarantee \emph{holding}: one cannot beat an oracle that is already always-adapt. Declining
-ImageNet-R is the \emph{unknowable} branch firing: the observable margin falls below the drift
-budget, so no label-free rule can decide the benefit sign, however good the adapter
-(Theorem~\ref{lem:nonid}). The coverage drop is mandatory abstention, not a tuning loss: committing in
-the unknowable region would be a false-adapt. None can be removed without violating the impossibility
+ImageNet-R is consistent with a weak-evidence, low-margin regime; the null result does not by itself
+establish structural non-identifiability under Theorem~\ref{thm:headline} (it could also reflect weak
+evidence, poor benefit estimation, or calibration-transfer failure). Abstention in the structurally
+unknowable region is mandatory---committing there would be a false-adapt---though some empirical
+abstention may instead stem from an overly wide radius or a weak estimator. None can be removed without violating the impossibility
 result, since a method that ``won everywhere'' would be claiming to decide the sign of an unidentifiable
 quantity. The one axis that genuinely improves all three is a \emph{richer label-free evidence map},
 which raises the observable margin $|M|$ so more shifts clear the budget (more knowable shifts, fewer
-abstentions, higher coverage), together with a \emph{tighter radius} (jackknife$+$ or more
-calibration data) that reclaims coverage on knowable cells abstained only out of conservatism; both remain
+abstentions, higher coverage), together with a \emph{better-justified or more data-efficient calibration} (more calibration data
+where available) that reclaims coverage on knowable cells abstained only out of conservatism; both remain
 bounded by the frontier they cannot cross.
 
 \textbf{Baselines and independent validation.} On the ImageNet-C SAR configuration, the
@@ -1182,8 +1338,8 @@
 unconditional false-adapt/false-freeze bound over an \emph{arbitrary probability measure}
 (\texttt{measure\_false\_adapt\_le\_alpha}), and split-conformal coverage holds in the
 conditional-on-the-bag uniform-rank model
-(\texttt{uniformIndex\_false\_adapt\_le}); we are not aware of a prior machine-checked
-conformal-coverage guarantee in a proof assistant (kernel-checked generalization bounds exist,
+(\texttt{uniformIndex\_false\_adapt\_le}); to our knowledge, machine-checked
+conformal-coverage guarantees remain uncommon in a proof assistant (kernel-checked generalization bounds exist,
 e.g.~\cite{zhang2025leanrademacher}, but not conformal coverage). General exchangeable-process
 coverage, and the remaining deployment-facing assumptions (calibration transfer and risk
 alignment) remain explicit assumptions outside the formalization.
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/kbound_short_appendix.tex	2026-07-09 16:14:09
+++ docs/research/kbound/kbound_short_appendix.tex	2026-07-11 13:23:28
@@ -33,7 +33,94 @@
 \State Aggregate metrics vs.\ always-adapt, always-freeze, per-condition oracle
 \end{algorithmic}
 \end{algorithm}
+
+\section{Evidence feature schema and adapter hyperparameters}\label{app:evidence-schema}
+Two label-free evidence panels are used, matched to the two calibration regimes of
+\S\ref{sec:method}. Notation: $p_0,p_a$ are the frozen and adapted softmax rows on the adaptation
+batch; $\bar m_0=\mathrm{mean}_n\,p_{0,n}$ and $\bar m_a$ are the predicted-class marginals;
+$H(p)=-\sum_c p_c\log p_c$; $C$ is the number of classes. The stress-grid tracks (CIFAR-10-C,
+ImageNet-C, PACS) use the $11$-dimensional base panel of Table~\ref{tab:schema-base}
+(\texttt{kbound\_pkg/kbound/evidence.py}). The natural-shift tracks (Camelyon17, iWildCam,
+Office-Home, RxRx1) use the $16$-dimensional panel of Table~\ref{tab:schema-rich}: a $10$-statistic
+frozen-logit summary plus a $6$-feature rich panel (\texttt{scripts/run\_wilds\_camelyon17.py}). All
+features are target-label-free; the ATC feature calibrates its threshold on \emph{source} labels
+only.
+
+\begin{table}[t]\centering\scriptsize
+\caption{Base $11$-dim evidence panel (stress grids).}
+\label{tab:schema-base}
+\begin{tabular}{@{}llp{0.30\linewidth}@{}}
+\toprule
+Feature & Definition & Family \\
+\midrule
+\texttt{pre\_entropy}  & $\mathrm{mean}_n H(p_{0,n})$ & frozen output \\
+\texttt{pre\_conf}     & $\mathrm{mean}_n \max_c p_{0,n}$ & frozen output \\
+\texttt{pre\_pbal}     & $H(\bar m_0)/\log C$ & frozen marginal \\
+\texttt{post\_entropy} & $\mathrm{mean}_n H(p_{a,n})$ & adapted output \\
+\texttt{post\_conf}    & $\mathrm{mean}_n \max_c p_{a,n}$ & adapted output \\
+\texttt{post\_pbal}    & $H(\bar m_a)/\log C$ & adapted marginal \\
+\texttt{pbal\_drop}    & \texttt{pre\_pbal}$-$\texttt{post\_pbal} & change \\
+\texttt{entropy\_drop} & \texttt{pre\_entropy}$-$\texttt{post\_entropy} & change \\
+\texttt{frac\_highconf}& $\mathrm{frac}_n(\max_c p_{a,n}{>}0.9)$ & collapse \\
+\texttt{marginal\_KL}  & $\sum_c \bar m_{a,c}\log(\bar m_{a,c}/\bar m_{0,c})$ & drift \\
+\texttt{update\_norm}  & $\lVert\Delta\theta_{\mathrm{aff}}\rVert_2$ & update \\
+\bottomrule
+\end{tabular}
+\end{table}
+
+\begin{table}[t]\centering\scriptsize
+\caption{Rich $16$-dim panel (natural shift): $10$ frozen-logit statistics $+$ $6$ rich features.
+$H$ is softmax entropy, $s=\max_c\mathrm{softmax}$ the confidence, $E(x)=-\log\sum_c e^{z_c}$ the
+free energy; subscripts $s,t$ denote source/target pools.}
+\label{tab:schema-rich}
+\begin{tabular}{@{}llp{0.26\linewidth}@{}}
+\toprule
+Feature & Definition & Family \\
+\midrule
+\multicolumn{3}{@{}l}{\emph{Frozen-logit summary (10)}}\\
+ent mean/std     & $\mathrm{mean}/\mathrm{std}\,H$ & frozen \\
+ent p25/p75      & $25/75$ pct.\ of $H$ & frozen \\
+conf mean/std    & $\mathrm{mean}/\mathrm{std}\,s$ & frozen \\
+frac hi-conf     & $\mathrm{frac}(s{>}0.9)$ & frozen \\
+frac hi-ent      & $\mathrm{frac}(H{>}\log 2)$ & frozen \\
+marg.\ max       & $\max_c \mathrm{mean}_n\,p$ & frozen marginal \\
+marg.\ std       & $\mathrm{std}_c \mathrm{mean}_n\,p$ & frozen marginal \\
+\midrule
+\multicolumn{3}{@{}l}{\emph{Rich features (6)}}\\
+\texttt{disagree} & $\mathrm{frac}(\arg\max f_0\!\neq\!\arg\max f_a)$ & disagreement \\
+\texttt{ent\_gap} & $\mathrm{mean}\,H(f_0)-\mathrm{mean}\,H(f_a)$ & change \\
+\texttt{en\_shift}& $\mathrm{mean}\,E_t-\mathrm{mean}\,E_s$ & drift \\
+\texttt{bn\_kl}   & $\mathrm{mean}_{\mathrm{ch}}\,\mathrm{KL}(\mathcal N_{\mathrm{run}}\!\parallel\!\mathcal N_{\mathrm{batch}})$ & drift \\
+\texttt{atc}      & $\mathrm{frac}_t(s\!\ge\! t)$, $t$ src-calib. & accuracy proxy \\
+\texttt{conf\_drop}& $\mathrm{mean}\,s_s-\mathrm{mean}\,s_t$ & drift \\
+\bottomrule
+\end{tabular}
+\end{table}
 
+\begin{table}[t]\centering\footnotesize
+\caption{Per-track adapter hyperparameters (from the run configs). Adapted parameters are
+BN/LayerNorm-affine only; TTA optimizer is Adam. ``Aggressiveness'' cells use
+mild $=(10$ steps, lr $10^{-3})$ or aggressive $=(50$ steps, lr $2.5\!\times\!10^{-3})$. EATA uses
+an entropy filter $e_{\mathrm{thr}}{=}0.4\ln 2{\approx}0.28$; SAR adds SAM $+$ entropy reset.
+Backbones for CIFAR-10-C, ImageNet-C, PACS, and Camelyon17 are verified from the run code; the
+remaining WILDS/DomainBed tracks use the standard baseline.}
+\label{tab:adapter-hparams}
+\resizebox{\columnwidth}{!}{%
+\begin{tabular}{@{}llll@{}}
+\toprule
+Track & Backbone & Adapter(s) & Steps / lr \\
+\midrule
+CIFAR-10-C stress & ResNet-18 & Tent/EATA/SAR & mild \& aggressive (per cell) \\
+ImageNet-C & ResNet-50 & Tent/EATA/SAR (SAM+reset) & mild \& aggressive (per cell) \\
+PACS (LODO) & ResNet-18 & mild/aggressive & $10$/$10^{-3}$ \,\&\, $50$/$2.5{\times}10^{-3}$ \\
+Camelyon17 & DenseNet-121 & EATA online & $10$ / $10^{-3}$ \\
+iWildCam & ResNet-50 & Tent episodic & $10$ / $10^{-3}$ \\
+Office-Home & ResNet (DomainBed) & SAR online aggr. & $50$ / $2.5{\times}10^{-3}$ \\
+RxRx1 & ResNet-50 & episodic & $5$ / (batch $64$) \\
+\bottomrule
+\end{tabular}}
+\end{table}
+
 \section{Full theory stack}\label{app:theory-full}\label{app:theory-ext}
 This appendix is not required for the main empirical claim; it preserves the full theorem stack
 and validator mapping behind the three main-paper theorems: matched-evidence impossibility
@@ -353,5 +440,10 @@
 means with paired bootstrap where this was pre-registered; a point estimate or a single locked
 configuration is labeled as such in Table~\ref{tab:uniform-panel}.
 
+% One-bit / weakest-class material (dominance polytopes, Thm uncond-weakest) is deferred to the
+% long manuscript (kbound.tex) for the short version: its explicit witnesses use a=2eta-1 values
+% outside [-1,1] and need an independent boundedness audit before inclusion. Source preserved below.
+\iffalse
 \subsection{Weakest one-bit class}\label{app:weakest}
 \input{paper/sections/weakest_class}
+\fi
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/paper/sections/theory_setup.tex	2026-07-09 16:14:09
+++ docs/research/kbound/paper/sections/theory_setup.tex	2026-07-11 01:53:05
@@ -13,9 +13,10 @@
 
 \begin{assumption}[Deployment setup]\label{ass:deploy}
 Unless stated otherwise: binary label $Y\in\{0,1\}$, $0/1$ loss $\ell$, and
-$\mu_T(D)>0$. Fix a source-calibrated score $s:\mathcal{X}\to[0,1]$ for $f_a$ (e.g.\ a
-Platt-scaled probability) and the target conditional accuracy
-$\eta_a(x):=\Pr_T(Y{=}1\mid f_a(x){=}1)$ on $D$. Define the \emph{observable margin}
+$\mu_T(D)>0$. Fix a source-calibrated score $s:\mathcal{X}\to[0,1]$ estimating the candidate's
+\emph{pointwise correctness} (e.g.\ a Platt-scaled confidence that $f_a$ is right at $x$), and write
+the target pointwise correctness $\eta_a(x):=\Pr_T\!\bigl(f_a(X){=}Y\mid X{=}x\bigr)$ on $D$, so that
+$\E_{\mu_T}[\eta_a\mid D]=\bar a$. Define the \emph{observable margin}
 \[
 M \;:=\; \E_{\mu_T}[s(X)\mid D]-\tfrac12,
 \]
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/paper/sections/theory_core_main.tex	2026-07-09 16:14:09
+++ docs/research/kbound/paper/sections/theory_core_main.tex	2026-07-11 13:41:03
@@ -12,20 +12,31 @@
 \end{lemma}
 \begin{proof}
 On $D$ exactly one of $f_0,f_a$ is correct under $0/1$ loss, so the benefit equals
-$2\mu_T(D)(\bar a-\tfrac12)$. By definition of $M$ and $\gamma$,
-$\bar a-\tfrac12=\E_{\mu_T\mid D}[\eta_a-s]=M+\gamma$.
+$2\mu_T(D)(\bar a-\tfrac12)$. By the definitions of $M$ and $\gamma$,
+\[
+M+\gamma=\Bigl(\E_{\mu_T\mid D}[s]-\tfrac12\Bigr)+\E_{\mu_T\mid D}[\eta_a-s]
+=\E_{\mu_T\mid D}[\eta_a]-\tfrac12=\bar a-\tfrac12,
+\]
+so $\sign\Delta=\sign(\bar a-\tfrac12)=\sign(M+\gamma)$.
 \end{proof}
 
 \begin{theorem}[Matched evidence forces abstention]\label{lem:nonid}
-Fix $\alpha\in(0,\tfrac12)$ and $\beta\ge0$. There exist $P_T^1,P_T^2\in\mathcal{C}_\beta$ such
+Fix $\alpha\in(0,\tfrac12)$ and $\beta>0$. For any margin with $|M|<\beta$ there exist
+$P_T^1,P_T^2\in\mathcal{C}_\beta$ such
 that $\Law(Z\mid P_T^1)=\Law(Z\mid P_T^2)$, $\sign\Delta_1=-\sign\Delta_2\neq0$, and
-$|M(P_T^1)|\le\beta$. Consequently, any label-free rule $g$ with
+$|M(P_T^1)|<\beta$. (For $\beta=0$ the class $\mathcal{C}_0$ forces $\gamma=0$, so $\sign\Delta=\sign M$
+is determined and no such pair exists: matched-evidence ambiguity is a strictly-positive-drift
+phenomenon.) Consequently, any label-free rule $g$ with
 $\Pr[g=\textsc{adapt},\Delta\le0]\le\alpha$ and $\Pr[g=\textsc{freeze},\Delta\ge0]\le\alpha$
 must satisfy $\Pr[g=\textsc{abstain}]\ge 1-2\alpha$ on this shared evidence law.
 \end{theorem}
 \begin{proof}
-Take $|M|\le\beta$ and choose $\gamma_1,\gamma_2\in[-\beta,\beta]$ with
-$\sign(M+\gamma_1)\neq\sign(M+\gamma_2)$ (possible because $|M|\le\beta$). Lemma~\ref{lem:reduction}
+Take $|M|<\beta$ and choose $\gamma_1,\gamma_2\in[-\beta,\beta]$ with
+$\sign(M+\gamma_1)\neq\sign(M+\gamma_2)$, both nonzero (possible because $|M|<\beta$). At the boundary
+$|M|=\beta$, instead take two evidence-identical worlds with benefits $0$ (drift $\gamma=-\sign(M)\beta$)
+and a strict benefit of sign $M$ (drift $\gamma=0$): the zero world caps the probability of the
+$\sign M$ commitment and the strict world caps the opposite, again forcing
+$\Pr[\textsc{abstain}]\ge1-2\alpha$, so abstention persists on the closed region $|M|\le\beta$. Lemma~\ref{lem:reduction}
 gives opposite benefit signs at the same observables $(M,\mu_T(D),Z\text{-law})$. A label-free
 rule sees identical $\Law(Z)$ in both worlds, so its action probabilities coincide; each committed
 action is a false adapt in one world or a false freeze in the other, forcing abstention with
@@ -41,11 +52,15 @@
 (Lemma~\ref{lem:reduction}).
 \item[\textnormal{(ii)}] \textbf{Sufficiency.} If $|M|>\beta$ and $P_T\in\mathcal{C}_\beta$, then
 $\sign\Delta=\sign M$.
-\item[\textnormal{(iii)}] \textbf{Necessity.} If $|M|\le\beta$, there exist
-$P_T^1,P_T^2\in\mathcal{C}_\beta$ with the same evidence law and opposite benefit signs; hence
-$\sign\Delta$ is not a function of $\Law(Z)$ over $\mathcal{C}_\beta$.
-\item[\textnormal{(iv)}] \textbf{Iff.} $\sign\Delta$ is label-free identifiable over
-$\mathcal{C}_\beta$ if and only if $|M|>\beta$.
+\item[\textnormal{(iii)}] \textbf{Necessity.} If $|M|<\beta$ (which requires $\beta>0$), there exist
+$P_T^1,P_T^2\in\mathcal{C}_\beta$ with the same evidence law and opposite \emph{nonzero} benefit
+signs; at $|M|=\beta>0$ the admissible drift $\gamma=-\sign(M)\beta$ yields $\Delta=0$, and in the
+degenerate case $M=\beta=0$ the class $\mathcal{C}_0$ forces $\Delta\equiv0$. Hence no \emph{strict}
+benefit sign is a function of $\Law(Z)$ over $\mathcal{C}_\beta$ whenever $|M|\le\beta$.
+\item[\textnormal{(iv)}] \textbf{Iff (strict commitment).} A strict action in
+$\{\textsc{adapt},\textsc{freeze}\}$ is uniformly sound over $\mathcal{C}_\beta$ if and only if
+$|M|>\beta$; on the closed band $|M|\le\beta$ (including $M=\beta=0$, where $\Delta\equiv0$) abstention
+is the maximal sound action.
 \end{enumerate}
 The population rule $M>\beta\Rightarrow\textsc{adapt}$, $M<-\beta\Rightarrow\textsc{freeze}$,
 $|M|\le\beta\Rightarrow\textsc{abstain}$ is the maximal sound certificate on
@@ -54,8 +69,9 @@
 \begin{proof}
 Part~(i) is Lemma~\ref{lem:reduction}. For~(ii), if $M>\beta$ and $|\gamma|\le\beta$, then
 $M+\gamma>0$, so $\sign\Delta=\sign(M+\gamma)=+1$; the negative case is symmetric. For~(iii),
-pick $\gamma_\pm\in[-\beta,\beta]$ with $\sign(M+\gamma_+)\neq\sign(M+\gamma_-)$ when
-$|M|\le\beta$; Lemma~\ref{lem:reduction} yields matched $Z$ and opposite signs. Part~(iv) combines
+pick $\gamma_\pm\in[-\beta,\beta]$ with $\sign(M+\gamma_+)\neq\sign(M+\gamma_-)$, both nonzero, when
+$|M|<\beta$; Lemma~\ref{lem:reduction} yields matched $Z$ and opposite signs. At $|M|=\beta$ the
+admissible $\gamma=-\sign(M)\beta$ gives $\Delta=0$, still precluding a determined nonzero sign. Part~(iv) combines
 (ii) and (iii). Maximality follows from~(iii) and Theorem~\ref{lem:nonid}.
 \end{proof}
 
@@ -107,8 +123,11 @@
 \end{remark}
 
 \begin{corollary}[Unjustified assumptions $\Rightarrow$ abstain]\label{cor:abstain-valid}
-If exchangeability, coverage, or risk alignment cannot be justified on the deployment batch,
-Theorem~\ref{thm:certificate} does not apply. Committing to \textsc{adapt} without a valid
-positive certified lower bound is not protected at level $\alpha$; the conservative action is
-\textsc{abstain} or \textsc{freeze} (cf.\ Theorem~\ref{lem:nonid}).
+If interval coverage---or the calibration assumptions supporting it (exchangeability, or an explicit
+shift correction)---cannot be justified on the deployment batch, Theorem~\ref{thm:certificate} does
+not apply, and committing to \textsc{adapt} is not protected at level $\alpha$; the conservative
+action is \textsc{abstain} or \textsc{freeze}. If instead \emph{risk alignment}
+(Definition~\ref{def:risk-align}) cannot be justified, the population-frontier interpretation and the
+transferability of the benefit estimator are unsupported, even though the interval implication of
+Theorem~\ref{thm:certificate} remains valid \emph{conditional on coverage} (cf.\ Theorem~\ref{lem:nonid}).
 \end{corollary}
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/paper/sections/main_theory_5.tex	2026-07-09 16:14:08
+++ docs/research/kbound/paper/sections/main_theory_5.tex	2026-07-11 01:53:39
@@ -117,7 +117,10 @@
 (Lemma~\ref{lem:reduction}); $\gamma$ is one-dimensional given the observables.
 \item[\textnormal{(ii)}] \textbf{Iff identifiability.} Over $\mathcal{C}_\beta$,
 $\sign\Delta$ is a function of $\Law(Z)$ iff $|M|>\beta$; then $\sign\Delta=\sign M$ and the
-minimax committal error is $0$; if $|M|\le\beta$ the minimax error is $\tfrac12$.
+minimax committal error is $0$. If $|M|<\beta$ the minimax committal error is $\tfrac12$ (opposite
+nonzero signs share the evidence law); at $|M|=\beta$ the admissible drift $\gamma=-\sign(M)\beta$
+makes $\Delta=0$, so strict-sign certification still fails and the maximal sound rule abstains on the
+closed region $|M|\le\beta$.
 \item[\textnormal{(iii)}] \textbf{Necessity geometry.} Any identifying class lies on one side of
 the hyperplane $\gamma=-M$; every identifying assumption is a one-sided constraint on $\gamma$.
 \end{enumerate}
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/paper/sections/theory_appendix_ext.tex	2026-07-09 16:14:09
+++ docs/research/kbound/paper/sections/theory_appendix_ext.tex	2026-07-11 13:29:28
@@ -67,8 +67,8 @@
 \end{theorem}
 
 \begin{conjecture}[Label-free bracketing --- \textbf{resolved}]\label{conj:gen}
-Unconditional label-free bracketing does not exist; weakest one-bit classes are in
-Appendix~\ref{app:weakest} (Theorems~\ref{thm:cmono-weakest},~\ref{thm:uncond-weakest}).
+Unconditional label-free bracketing does not exist; the weakest one-bit classes (an explicit finite
+family of dominance polytopes) are characterized in the long manuscript (\texttt{kbound.tex}).
 \end{conjecture}
 
 \begin{theorem}[Evidence-channel rate]\label{thm:ev-rate}
--- /Users/pratik_n/kbound_phase1_backup_20260711_014958/paper/references_kbound_expanded.tex	2026-07-04 12:27:05
+++ docs/research/kbound/paper/references_kbound_expanded.tex	2026-07-11 02:20:50
@@ -155,9 +155,6 @@
 \bibitem{waudbysmith2024betting} I. Waudby-Smith, A. Ramdas. Estimating means of bounded
 random variables by betting. \emph{J.\ R.\ Stat.\ Soc.\ B} 86(1):1--27, 2024.
 
-\bibitem{vovk2005algorithmic} V. Vovk, A. Gammerman, G. Shafer. \emph{Algorithmic Learning
-in a Random World.} Springer, 2005.
-
 \bibitem{maurer2009empirical} A. Maurer, M. Pontil. Empirical Bernstein bounds and
 sample variance penalization. \emph{COLT}, 2009.
 
