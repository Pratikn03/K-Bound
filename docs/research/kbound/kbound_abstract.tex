% =====================================================================
% kbound_abstract.tex -- SHORT/TMLR abstract = shared core + short closing.
%
% The shared text lives in kbound_abstract_core.tex and the weak-zeros caveat
% in kbound_abstract_disclosures.tex; kbound.tex \input s both and supplies its
% own (longer) closing paragraph. Do not paste either file's text here -- that
% duplication is what caused defect D2. Contains the abstract TEXT only; each
% driver supplies its own abstract environment.
% =====================================================================
\input{kbound_abstract_core}
On the one-sided natural tracks the supported conclusion is safety rather than dominance:
Camelyon17 OOD reproduces always-adapt, the primary iWildCam and Office-Home panels reproduce
always-freeze, and RxRx1 freezes throughout; \input{kbound_abstract_disclosures}. A separate
Office-Home replication has a small point edge over both fixed policies, but its seed interval
includes zero. PACS, ImageNet-R, and CIFAR-10.1 are negative or transfer-failure results and remain in
the panel. K-Bound is therefore a safety, validity, and abstention layer for test-time adaptation,
not a universal accuracy booster; the population drift budget is an external specification, while
the deployable KGA guarantee is conditional on coverage transfer at the declared inference unit.
