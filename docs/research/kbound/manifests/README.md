# Historical manifest directory

Status: **superseded for current paper claims** (reconciled 2026-08-29).

These CSV files describe the early synthetic/diagnostic manuscript layout. They are retained to
preserve result lineage, but they are not the publication claim authority and are not inputs to the
maintained compact or long paper. In particular, the eight-seed paired-t-test row is a historical
simulation diagnostic and has no current significance or confirmatory status.

Use these current authorities instead:

1. `../claim_ledger.json` for allowed and forbidden wording.
2. `../RESULT_MANIFEST.json` and
   `../../../../experiments/kbound/results/reconciled_panels_v1/canonical_panel_results.json` for
   the reconciled panel.
3. `../paper/generated/cct20_release_manifest.json` plus its receipt for CCT-20.
4. `../../../../experiments/kbound/results/so2sat_lcz42_prospective_v1/development_mps_bn_fix_v1/`
   for the stopped So2Sat development study.

Do not promote any row marked `superseded_historical_simulation`, `verify_before_claim`, or
`corroborating` without a new, versioned authority and claim-ledger entry.
