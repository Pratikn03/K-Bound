# Recovered historical mixed-sign receipts

These three JSON files are byte-preserved copies from the Gemini task identified in RECOVERY_AUDIT.json. Their four input hashes match the resident saved result files. The original producer and its completed execution log were located in the task artifact folder; local audit copies preserve those originals. No model was trained, refit, tuned or rescored during recovery. All reported arithmetic checks use already-saved arrays and actions.

This recovery supersedes blanket statements that mixed-sign per-cell KGA predictions, radii, actions and B6 split receipts are missing. It does not authenticate the separate helpful-only panel, establish prospective replication, or certify the B6 interval at alpha=0.10. The recovered producer clips a required rank of9 to8; that calibration limitation must accompany the historical B6 point estimates.

The original receipt's broad certification wording is preserved as historical text, not adopted as a new guarantee. See RECOVERY_AUDIT.json for the independent checks and limits.
