# Complete K-Bound research/collection input profile (Torch + WILDS).
#
# This portable, human-maintained input extends the paper/core profile with the
# exact direct dependencies used for repository-wide pytest collection.  On
# macOS, install this file directly to obtain the native CPU/MPS Torch wheel.
# Linux x86_64 CI must instead install requirements-research-ci.lock.txt with
# --require-hashes; that lock binds the CPU-only Torch wheels and every
# transitive dependency.  Edit this file only when intentionally refreshing
# that lock.
-r requirements-paper.txt

# API/security tests
fastapi==0.136.3
httpx==0.28.1
python-jose[cryptography]==3.5.0
passlib==1.7.4
psutil==7.2.2

# Headless edge-dashboard tests
opencv-python-headless==4.13.0.92

# Manuscript Word-export tests
python-docx==1.2.0

# Natural-shift runners and the NLP smoke-test import surface
torch==2.5.1
torchvision==0.20.1
wilds==2.0.0
transformers==4.46.3
h5py==3.16.0
