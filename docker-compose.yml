services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./models:/app/models:ro
      - ./experiments:/app/experiments:ro
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      UAIS_API_KEYS: ${UAIS_API_KEYS:?Set UAIS_API_KEYS before starting the API}
      UAIS_CORS_ORIGINS: ${UAIS_CORS_ORIGINS:?Set UAIS_CORS_ORIGINS before starting the API}
      UAIS_PRODUCTION_MODE: ${UAIS_PRODUCTION_MODE:-true}
      UAIS_REQUIRED_MODELS: ${UAIS_REQUIRED_MODELS:-fraud,cyber,fusion}
      UAIS_TRUSTED_MODEL_ARTIFACTS: ${UAIS_TRUSTED_MODEL_ARTIFACTS:-false}
      UAIS_REQUIRE_MODEL_CHECKSUMS: ${UAIS_REQUIRE_MODEL_CHECKSUMS:-true}
      # Set for multi-replica rate limiting (Gate P P5): UAIS_REDIS_URL=redis://redis:6379/0
      UAIS_REDIS_URL: ${UAIS_REDIS_URL:-}
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    profiles:
      - distributed
    ports:
      - "127.0.0.1:6379:6379"
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /data
    command: ["redis-server", "--save", "", "--appendonly", "no"]

  streamlit:
    profiles:
      - research
    image: python:3.11-slim-bookworm@sha256:8dca233de9f3d9bb410665f00a4da6dd06f331083137e0e98ccf227236fcc438
    working_dir: /app
    volumes:
      - ./:/app:ro
    command: bash -c "pip install --no-cache-dir -r requirements.txt && streamlit run dashboard/app_streamlit.py --server.port 8501 --server.address 0.0.0.0"
    ports:
      - "127.0.0.1:8501:8501"
    environment:
      - PYTHONPATH=/app/src
    depends_on:
      - api

  mlflow:
    profiles:
      - research
    image: ghcr.io/mlflow/mlflow:v2.14.0@sha256:7a1deb607de52710ea860911998ce2693ec50eb1a31dfc6754f5f58a17044e44
    command: mlflow server --host 0.0.0.0 --port 5000 --backend-store-uri sqlite:///mlflow.db --default-artifact-root /mlruns
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - ./mlruns:/mlruns
      - ./mlflow.db:/mlflow.db
    security_opt:
      - no-new-privileges:true

networks:
  default:
    driver: bridge
