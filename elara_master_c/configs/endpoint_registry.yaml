# Primary endpoints — authoritative copy in research_lock

version: 1
authoritative: research_lock/primary_endpoints_v1.yaml

families:
  M0_mechanism:
    primary: delta_roc_auc_under_coherent_collapse
    clean_constraint: clean_false_fire_rate <= 0.010

  M1_natural_paired_industrial:
    primary: delta_roc_auc_vs_strongest_frozen_comparator_degraded
    clean_constraint: false_fire <= 0.010

  M2_heldout_transfer:
    primary: delta_roc_auc_on_untouched_dataset
    pass: bootstrap_ci_excludes_zero

  M3_non_vision:
    primary: delta_roc_auc_degraded_same_fusion_interface

  M4_temporal:
    primary: monitored_switching_outcome

statistics: research_lock/statistical_policy_v1.md
