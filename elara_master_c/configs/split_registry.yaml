# Split definitions — pointers to authoritative split columns and policies

version: 1

datasets:
  elara_bench_la:
    split_column: fusion_split
    values: [train, validation, test]
    disjoint_key: domain/source_row/label
    metadata: experiments/fusion/real_domain_fusion_metadata.json

  mvtec_3d_ad:
    split_column: split
    values: [train, validation, test]
    normal_reference: train + defect_type good
    metadata: experiments/fusion/mvtec3d_patchcore_metadata.json

  eyecandies:
    role: development
    note: reclassified D1; not final audit

final_audit_splits:
  m2_new_untouched_transfer:
    status: NOT_SEALED
  m3_non_vision_paired:
    status: NOT_SEALED

hash_manifest_dir: elara_master_c/data/splits/split_hashes
