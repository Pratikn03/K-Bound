# ELARA-CHF v1 (theorem T8) — validation-locked heterogeneous fusion

version: 1
policy_id: ELARA_CHF_v1
status: LOCKED_DEV
ratified: 2026-05-30

theorem: T8
core_module: src/uais/fusion/attention/certified_heterogeneous_fusion.py

selection:
  validation_only: true
  route_priority_when_tied: [stack5, convex_sar_rga, sar_only, coherence_gated, rga_plus_only]
  shift_aware_test: true  # held-out categories -> SAR (matches frozen comparator on M2 test)

empirical_m2_seed42:
  validation_delta_vs_sar: 0.0033
  test_delta_vs_sar: 0.0
  test_route_locked: convex_sar_rga  # with shift_aware -> SAR on 100% unknown test cats

gate_note: >
  CHF does not beat frozen SAR on M2 test (ties at 0.543). Gate E requires strict superiority, not tie.
  Next empirical lever: upstream expert v2 or full TTA parity on multimodal head.
