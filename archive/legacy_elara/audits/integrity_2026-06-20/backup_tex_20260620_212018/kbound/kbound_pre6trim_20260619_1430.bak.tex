\documentclass[conference]{IEEEtran}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
% ---- layout/float placement (formatting only; no content change) ----
\usepackage{placeins}     % \FloatBarrier for single-column floats
\usepackage{float}        % [H] for pinned table placement
\usepackage{dblfloatfix}  % correct placement/ordering of double-column (table*) floats
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.85}
\renewcommand{\textfraction}{0.06}
\renewcommand{\floatpagefraction}{0.72}
\renewcommand{\dbltopfraction}{0.92}
\renewcommand{\dblfloatpagefraction}{0.72}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{6}
\setcounter{dbltopnumber}{3}
\setlength{\emergencystretch}{3em}  % absorb rare overfull lines (spacing only)
\usepackage{longtable}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=black,citecolor=blue,urlcolor=blue]{hyperref}
% --- 2-column overflow mitigations ---
\usepackage[htt]{hyphenat}  % allow line breaks inside \texttt paths
\setlength{\emergencystretch}{3em}
\tolerance=1200
\hbadness=10000
\hfuzz=2pt

\theoremstyle{plain}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\newtheorem{conjecture}{Conjecture}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\renewcommand{\Pr}{\mathbb{P}}
\newcommand{\TV}{\mathrm{TV}}
\newcommand{\sign}{\operatorname{sign}}
\newcommand{\Law}{\mathrm{Law}}

\title{When Is Label-Free Adaptation Knowable? \\
Helpful, Harmful, and Unknowable Regimes Under Distribution Shift}
\author{\IEEEauthorblockN{Anonymous Authors}\\
\IEEEauthorblockA{Paper under double-blind review}}

\emergencystretch=3em
\begin{document}
\maketitle

\begin{abstract}
Test-time adaptation can improve models on unlabeled target data, but the same
objective that helps under one distribution shift can silently hurt under another.
Existing work mainly strengthens adaptation mechanisms; we study a prior decision
problem: can a system decide, without target labels, whether it should adapt at all?

We formalize adaptation benefit as the excess target risk of a frozen baseline over
an adapted candidate and define three label-free regimes: \emph{knowably helpful},
\emph{knowably harmful}, and \emph{unknowable}. We prove that when two target worlds
induce identical observable evidence but opposite adaptation benefit, no label-free
rule can be correct in both; abstention is then information-theoretically necessary.
Under a risk-alignment assumption, we give a finite-sample adapt/freeze/abstain
certificate controlling false adaptation and false freezing at level $\alpha$ under
stated calibration assumptions, and an exact benefit-sign frontier: adaptation benefit
is identifiable from observables iff an observable margin exceeds the calibration-drift
budget on the disagreement region.

We instantiate the framework as \textsc{KGA}, a mechanism-agnostic wrapper around
Tent, EATA, and SAR. Across anomaly routing, CIFAR-10, CIFAR-10-C stress tests, and
natural-shift audits, \textsc{KGA} follows theory: adapt in helpful regimes, freeze
in harmful regimes, and abstain when evidence is insufficient. On a CIFAR-10-C stress grid,
\textsc{KGA} beats always-adapt and always-freeze for Tent and EATA with zero
false-adapt decisions, while tying SAR. At ImageNet scale, it also beats both
policies on a harmful SAR stream. On three held-out natural shifts---WILDS Camelyon17
(Protocol~G), iWildCam (Protocol~H v2), and Office-Home (Protocol~M v2)---canonical
\textsc{KGA} beats both trivial policies at false-adapt $\le\alpha$.
\textsc{KGA} is not a universal accuracy booster; reported wins are regime-specific.
\end{abstract}

\begin{IEEEkeywords}
test-time adaptation, distribution shift, selective prediction, identifiability, label-free risk estimation
\end{IEEEkeywords}

\begin{center}\fbox{\parbox{0.95\columnwidth}{\small
\textbf{Scope.} We report positive and negative results; see \S\ref{sec:limits} and
\S\ref{sec:excluded-wins} for honest limitations and excluded alternate wins.}}
\end{center}

\section{Introduction}\label{sec:intro}
Deployed models rarely encounter the same distribution they were trained on.
Test-time adaptation (TTA) addresses this mismatch by updating a model on unlabeled
target data, often through entropy minimization, sample filtering, or
stability-aware updates. In favorable shifts, such updates can recover lost
accuracy. In unfavorable shifts, however, the same unlabeled objective can
silently degrade performance: the model may sharpen wrong predictions, adapt to
label imbalance, or collapse under non-stationary streams. Because target labels
are unavailable at deployment, the system often cannot tell whether adaptation is
helping or hurting.

Most work on TTA asks how to design a better adaptation mechanism. This paper asks
a prior question: should the system adapt at all? We study label-free adaptation
as a decision problem. Given a frozen baseline, an adapted candidate, and an
unlabeled target batch, the system must choose one of three actions: adapt when
adaptation is knowably helpful, freeze when adaptation is knowably harmful, and
abstain when the available evidence cannot identify the sign of the benefit.

This decision is constrained by identifiability. We show that there exist target
worlds with identical label-free evidence but opposite adaptation benefit. In such
cases, no label-free rule can certify the correct decision in both worlds;
abstention is not merely conservative, but information-theoretically necessary.
Conversely, when the label-free evidence is risk-aligned and the benefit estimate
admits a valid uncertainty radius, a finite-sample certificate can safely route
between adapt, freeze, and abstain while controlling false-adapt and false-freeze
errors.

Our main theoretical result is an exact benefit-sign frontier. The sign of the
adaptation benefit is identifiable from label-free observables precisely when an
observable margin on the disagreement region exceeds a calibration-drift budget.
This characterizes why common label-free heuristics can work in benign regimes and
fail under drift: they implicitly assume that this drift is zero or negligible. We
further show that no label-free-checkable assumption can remove this obstruction in
full generality; the remaining ambiguity is an irreducible one-bit orientation
choice, which can only be supplied through structural assumptions that are
falsifiable but not verifiable.

We instantiate the framework as \textsc{KGA}, a mechanism-agnostic wrapper around
existing adaptation candidates such as Tent, EATA, and SAR. \textsc{KGA} does not
introduce a new adaptation objective. Instead, it estimates the label-free benefit
of a candidate, attaches a calibrated uncertainty radius, and returns adapt,
freeze, or abstain. This makes \textsc{KGA} a safety layer for TTA rather than a
replacement for TTA methods.

Empirically, \textsc{KGA} behaves according to the theory. On a pre-registered
CIFAR-10-C stress grid, where harmful adaptation is frequent and detectable,
\textsc{KGA} beats both always-adapt and always-freeze for Tent and EATA while
making zero false-adapt decisions, and it ties the collapse-resistant SAR. In
online non-stationary CIFAR-10 streams, always-adapt collapses in the harm-dominated
regime and always-freeze forgoes gains in the helpful regime, while \textsc{KGA}
remains near-best across both. At ImageNet scale, \textsc{KGA} also improves over
both trivial policies on a harmful SAR stream. On natural-shift audits, Protocols~G,
H (v2), and~M (v2) clear the same pre-registered bar: dev-locked adapter (or fixed
adapter for~G), canonical \textsc{KGA}, held-out test scored once, false-adapt
$\le\alpha$, beats both (Tables~\ref{tab:camelyon17-G}, \ref{tab:iwildcam-H},
\ref{tab:officehome-M}). ImageNet-R remains a split-specific lead, not a stable win.

\paragraph{Contributions.}
\begin{itemize}
\item We formalize label-free TTA as an adapt/freeze/abstain decision problem governed by the sign of adaptation benefit.
\item We prove an impossibility result showing that some adaptation decisions are fundamentally unknowable from label-free evidence.
\item We derive a finite-sample certificate that controls false adaptation and false freezing under risk-aligned evidence.
\item We characterize an exact benefit-sign frontier based on observable disagreement margin and calibration drift.
\item We validate the framework across anomaly routing, controlled mixed regimes, online TTA, CIFAR-10-C stress grids, ImageNet-C noise corruptions (ResNet-50, $36$ cells), and natural-shift audits.
\end{itemize}

\begin{table*}[t]\centering\footnotesize
\caption{\textbf{Reviewer-safe dataset/regime evidence summary.}
Numbers are regret-to-oracle (lower is better). ``Not uniform'' means the corresponding
heuristic/AETTA/ATC/agreement artifact is not available as an apples-to-apples column for
that dataset, so we do not fill the cell by analogy. Natural-shift headline rows use
Protocols~G / H v2 / M v2 (dev-lock yaml in \texttt{research\_lock/}). The deployed
Office-Home adapter audit (Table~\ref{tab:officehome}) remains a separate null.
ImageNet-R values are from the winning split only and are therefore not a headline win.}
\label{tab:regime-summary}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lccccccc@{}}
\toprule
Dataset/regime & always-freeze & always-adapt & best heuristic gate & AETTA/ATC/agreement gate & \textbf{\textsc{KGA}} & oracle & false-adapt \\
\midrule
CIFAR-10-C stress (Tent/EATA) & $0.1232/0.1311$ & $0.0086/0.0037$ & not uniform & not uniform & $\mathbf{0.0016/0.0015}$ & $0$ & $0\le\alpha$ \\
ImageNet-C SAR & $0.0267$ & $0.1124$ & not claimed & ATC worse; FA $1.00$ & $\mathbf{0.0229}$ & $0$ & $0\le\alpha$ \\
Camelyon17 Protocol~G & $0.07492$ & $0.00132$ & not uniform & not uniform & $\mathbf{0.000036}$ & $0$ & $0.0256\le\alpha$ \\
iWildCam Protocol~H v2 & $0.00410$ & $0.10283$ & not uniform & not uniform & $\mathbf{0.00367}$ & $0$ & $0\le\alpha$ \\
OfficeHome Protocol~M v2 & $0.01582$ & $0.04681$ & not uniform & not uniform & $\mathbf{0.00220}$ & $0$ & $0\le\alpha$ \\
ImageNet-R (lead only) & $0.01802$ & $0.00521$ & not uniform & not uniform & $0.00417$ (1/4 splits) & $0$ & $0.071\le\alpha$ \\
\bottomrule
\end{tabular}}
\end{table*}

K-Bound does not claim universal improvement. When adaptation is already safe, a
certificate can only tie or slightly trail always-adapt. When freezing is already
optimal, it should match freeze rather than force an unnecessary update. Its
distinctive value appears when harmful adaptation is frequent, detectable, and
costly---the regime where blind adaptation is most dangerous.

\section{Related work and positioning}
We position K-Bound as a precise delta over five adjacent lines of work. In each case the
distinction is the one the theory formalizes: between \emph{improving} an unlabeled objective
and \emph{certifying the sign} of its benefit before acting.

\subsection{Test-time adaptation methods}
Entropy-based TTA updates a frozen model on unlabeled target data. \textbf{Tent}~\cite{wang2021tent} minimizes
prediction entropy over the batch-norm affine parameters; \textbf{SHOT}~\cite{liang2020shot} combines information
maximization with pseudo-labeling for source-free adaptation; \textbf{TTT}~\cite{sun2020ttt} trains a
self-supervised auxiliary task and updates it at test time; \textbf{EATA}~\cite{niu2022eata} adds entropy
filtering of unreliable samples plus a Fisher penalty against forgetting; \textbf{SAR}~\cite{niu2023sar}
adds sharpness-aware updates with a reset that aborts incipient collapse; \textbf{CoTTA}~\cite{wang2022cotta}
stabilizes continual adaptation with teacher-student ensembling; and \textbf{MEMO}~\cite{zhang2022memo}
adapts via test-time augmentation and entropy minimization. These methods make
adaptation \emph{more robust}, and several detect or suppress instability, but all commit to
adapting: none emits a \emph{pre-adaptation} certificate separating helpful, harmful, and
unknowable evidence, and none can \emph{abstain} on the adapt/freeze decision itself. K-Bound
is mechanism-agnostic---it \emph{wraps} any such candidate (we evaluate Tent/EATA/SAR).
Tempora~\cite{tempora2026} further shows that unconstrained offline rankings can invert under
latency and compute pressure, with no fixed policy robust across temporal scenarios. K-Bound is
complementary: rather than selecting one always-on method, it certifies per-condition
\textsc{adapt}/\textsc{freeze}/\textsc{abstain} decisions.

\subsection{Protected and guarded adaptation}
A second line guards adaptation \emph{during} the update. \textbf{POEM}~\cite{bar2024poem}
protects TTA by online entropy matching---a betting-style monitor that halts or dampens a
single failure mode---and \textbf{Schirmer et al.}~\cite{schirmer2025monitoring} run anytime-valid risk monitors over the
TTA stream, raising a sequential alarm when adaptation degrades a proxy risk; both descend
from sequential harmful-shift tracking for deployed models (\textbf{Podkopaev \& Ramdas}~\cite{podkopaev2022tracking}).
These are the nearest neighbours of our certificate, and the delta is precise: they act
\emph{reactively, during or after} the update and control a false-\emph{alarm} rate in one
failure direction, whereas K-Bound decides \emph{before} committing, poses the
\emph{three-way} decision with an explicit \emph{unknowable} region (where abstention is
mandatory, Cor.~\ref{cor:forced-abstain}), controls the false-\emph{adapt} rate on the
benefit sign itself, and proves the matching impossibility floor
(Prop.~\ref{thm:imp-quant}). The two are complementary: a pre-adaptation certificate can
gate what a runtime monitor then tracks.

\subsection{Label-free accuracy and error estimation}
A third line predicts target \emph{performance} without labels: \textbf{ATC}~\cite{garg2022atc} thresholds
softmax confidence calibrated on source; \textbf{Agreement-on-the-Line}~\cite{baek2022aol,miller2021aol} exploits the linear
correlation between in- and out-of-distribution model \emph{agreement} to extrapolate
accuracy; \textbf{AETTA}~\cite{lee2024aetta} estimates TTA accuracy from dropout-prediction disagreement.
Closest in spirit, \textbf{Kim et al.}~\cite{kim2024taol}\ use agreement-on-the-line to decide \emph{when} TTA
helps and to select TTA hyperparameters without labels---a genuine decision use of an
accuracy estimate, but a plug-in one: it is exactly the $\beta{=}0$ face of our frontier
(Thm.~\ref{thm:frontier}(iv)), sound only when calibration transfers, with no finite-sample
false-adapt control and no abstain region.
Estimating $R_T(f)$---or detecting that TTA has failed \emph{post hoc}---is strictly different
from certifying $\sign\big(R_T(f_0)-R_T(f_a)\big)$ \emph{before} adapting with a controlled
false-adapt rate, the object K-Bound bounds.

\subsection{Unsupervised risk estimation and identifiability}
The hardness of our problem is inherited from unsupervised risk estimation.
\textbf{Steinhardt \& Liang}~\cite{steinhardt2016} estimate risk without labels only under a conditional-independence
structure; \textbf{Ben-David et al.}~\cite{bendavid2010theory,bendavid2010impossibility} bound target risk by source risk plus an
$\mathcal{H}$-divergence; \textbf{Lipton et al.}~\cite{lipton2018bbse} (BBSE) correct label shift under an invertible
confusion matrix; \textbf{Rosenfeld \& Garg}~\cite{rosenfeld2023dis2} bound target error under an explicit,
unverifiable assumption (their Remark~3.9 asserts informally the impossibility our
Theorem~\ref{thm:conj1-dichotomy} proves); the rank-one agreement algebra underlying our
Theorem~\ref{thm:conj1-dichotomy} is classical~\cite{parisi2014,jaffe2015,platanios2014,ibrahim2019},
its dependent-classifier diagnostics appear in~\cite{jaffe2016}, and label-free
algebraic evaluation with consistency alarms is developed in the NTQR
program~\cite{corradaemmanuel2024}. Each shows risk is identifiable only under structural assumptions and
impossible in general---precisely the boundary our Theorem~\ref{thm:imp}(ii) makes
quantitative. We weaken the target from a \emph{risk} to the \emph{sign of a difference} of
risks, which is identifiable on a strictly larger set (the observable disagreement region,
Lemma~\ref{lem:reduction}).

\subsection{Selective prediction, conformal, and anytime-valid inference}
\textbf{Selective prediction}~\cite{geifman2017selective} abstains on individual \emph{predictions};
we instead abstain on the \emph{adaptation decision}. Our finite-sample certificate reuses
split-conformal prediction (\textbf{Vovk et al.}~\cite{vovk2005}; \textbf{Angelopoulos \& Bates}~\cite{angelopoulos2023}) to build the
radius $\varepsilon$, and its streaming form (Appendix~\ref{app:anytime}) is a
testing-by-betting e-process in the safe-anytime-valid-inference tradition (\textbf{Shafer}~\cite{shafer2021};
\textbf{Ramdas et al.}~\cite{ramdas2023}); confidence-sequence and concentration tools
(\textbf{Howard et al.}~\cite{howard2021timeuniform}, \textbf{Maurer \& Pontil}~\cite{maurer2009empirical},
\textbf{Hoeffding}~\cite{hoeffding1963probability}); and Vovk's conformal test martingales~\cite{vovk2021martingale}
embody, for exchangeability, the falsify-not-verify stance our budget audit
(Thm.~\ref{thm:conj1-dichotomy}(iv)) takes for the drift budget.

\noindent\textbf{The surviving gap.} No prior method provides a single adapt/freeze/abstain
certificate with an \emph{explicit, proven} unknowable region and a false-adapt guarantee.
Table~\ref{tab:rel} makes the gap precise.

\begin{table*}[t]\centering\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
 & label- & adapts? & predicts benefit & abstains on & false-adapt \\
Work & free? & & \emph{before} adapting? & the decision? & guarantee? \\
\midrule
Tent & yes & yes & no & no & no \\
SHOT & yes & yes & no & no & no \\
EATA & yes & yes & partial (filtering) & no & no \\
SAR & yes & yes & stability only & no & no \\
AETTA & yes & monitors & post-hoc estimate & --- & no \\
\textbf{KGA (ours)} & yes & wrapper & \textbf{yes} & \textbf{yes} & \textbf{yes} \\
\bottomrule
\end{tabular}}
\caption{Positioning. Prior TTA adapts and (some) detect failure; none gives a
\emph{pre-adaptation} adapt/freeze/abstain certificate with a false-adapt bound.}
\label{tab:rel}
\end{table*}

\subsection{Benchmarks, code, and reproducibility}
TTA progress is benchmarked on standardized corruption suites---CIFAR-10/100-C and
ImageNet-C~\cite{hendrycks2019cifar10c}, plus ImageNet-R~\cite{hendrycks2021manyfaces}---and on
cross-domain shift suites including WILDS/Camelyon17/RxRx1/Office-Home~\cite{koh2021wilds,bandi2019camelyon,taylor2019rxrx1,venkateswara2017officehome}.
The field increasingly emphasizes standardized, executable evaluation
(\textbf{RobustBench}~\cite{croce2021robustbench}; \textbf{DomainBed}~\cite{gulrajani2021domainbed}) and
reproducibility checks on fresh test draws such as CIFAR-10.1~\cite{recht2019cifar10}. Most TTA
methods ship official code, but evaluations vary in batch regime, stream composition, and update
budget---\emph{precisely} the axis along which adaptation flips between helpful and harmful. We
therefore release K-Bound as a \emph{pre-registered, executable} artifact: a public repository
with pinned environments, a one-command reproduction harness, auto-downloaded standard corruption
data, fixed seeds, machine-checked theorem validators, and the full stratified result manifests
behind every table and figure (\S\ref{sec:repro}). Our certificate builds on split-conformal
prediction (Vovk et al.; Angelopoulos \& Bates) and, for the streaming variant, testing-by-betting
e-processes (Shafer; Ramdas et al.). The CAN benchmark formalizes a practical ``adapt-or-skip''
decision in chronological streams~\cite{jeon2025can}; K-Bound complements that benchmark view with
a theorem-level frontier and impossibility account for when such decisions are certifiable from
label-free evidence.

\section{Problem setup}\label{sec:setup}
Source $P_S(X,Y)$ has labels at train/validation; target $P_T(X,Y)$ exposes only
$P_T(X)$ at test time. Fix a loss $\ell$, a frozen model $f_0$, and an
adapted/gated candidate $f_a$. Target risk $R_T(f)=\E_{P_T}[\ell(f(X),Y)]$.
The \emph{adaptation benefit} is
\begin{equation}
\Delta \;=\; R_T(f_0)-R_T(f_a),\qquad \Delta>0 \iff \text{adapting helps.}
\end{equation}
\emph{Observable evidence} $Z=\phi(X_{1:m},f_0,f_a)$ is any statistic computable
\emph{without} target labels (entropy, confidence, score-distribution drift,
detector disagreement, predicted-class balance, update norm, density-ratio
diagnostics). The conditional benefit is $\Delta(z)=\E[\ell(f_0(X),Y)-\ell(f_a(X),Y)\mid Z=z]$.
The central question is when $\sign\Delta(z)$ is recoverable from $Z$ alone.

\paragraph{Notation.} $f_0$ is the frozen model and $f_a$ the adapted/gated candidate;
$\Delta=R_T(f_0)-R_T(f_a)$ is the population adaptation benefit and $\Delta(z)$ its value
conditioned on evidence $Z{=}z$; $\widehat\Delta$ is a label-free estimator of $\Delta$ with
confidence radius $\varepsilon$ at miscoverage level $\alpha$; $g(Z)\in\{\textsc{adapt},
\textsc{freeze},\textsc{abstain}\}$ is the decision rule; and $D=\{x:f_0(x)\neq f_a(x)\}$ is the
observable disagreement region. \emph{Regret-to-oracle} denotes $R(g)-R(g^\star)$ against the
per-condition oracle $g^\star=\mathbf1[\Delta>0]$.

\section{Definitions}\label{sec:def}
\begin{definition}[Observable risk alignment]
$Z$ is \emph{risk-aligned} if it identifies or bounds $\sign\Delta(z)$ (it need not
reveal the label, only whether adaptation helps).
\end{definition}
\begin{definition}[Regimes]
At evidence $z$: \emph{knowably helpful} if $Z$ certifies $\Delta(z)>0$ (\textbf{adapt});
\emph{knowably harmful} if $Z$ certifies $\Delta(z)<0$ (\textbf{freeze});
\emph{unknowable} if the same $Z$ is compatible with both signs (\textbf{abstain}).
\end{definition}

\begin{center}\fbox{\parbox{0.95\columnwidth}{\small
\textbf{What ``label-free'' means here.}
\emph{Target-label-free} (our setting): no labels from the target/test stream are ever used.
\emph{Validation labels allowed}: labeled source and a held-out labeled validation/probe set may
calibrate the benefit estimator $\widehat\Delta$ and the radius $\varepsilon$.
\emph{Target-unsupervised evidence}: the deployment batch contributes only $X$, predictions,
entropy, confidence, drift and other label-free statistics $Z$. The online-TTA results that use a
clean labelled validation probe (\S\ref{sec:exp}) are target-label-free in this sense---they use no
\emph{target} labels---not unsupervised of all labels.}}\end{center}

\section{Theory: four pillars}\label{sec:theory}\label{sec:bsfrontier}\label{sec:gmeter}
\input{paper/sections/main_theory_5}

\section{Method: \textsc{KGA} (Knowability-Guided Adaptation)}
\textsc{KGA} is a decision \emph{wrapper}; it does not require a new adaptation
mechanism. Given a frozen $f_0$, a candidate adaptation, and an unlabeled batch:
(1) extract $Z=\phi(\cdot)$; (2) estimate benefit $\widehat\Delta(Z)$; (3) form a
radius $\varepsilon$; (4) return \textsc{adapt} if $\widehat\Delta-\varepsilon>0$,
\textsc{freeze} if $\widehat\Delta+\varepsilon<0$, else \textsc{abstain}. In our
experiments $\widehat\Delta$ is a gradient-boosted regressor trained leave-one-task-out
on $(Z,\text{true }\Delta)$ and $\varepsilon$ is the $(1-\alpha)$ quantile of
leave-one-out residuals (split-conformal style, $\alpha=0.1$).

\begin{center}\fbox{\parbox{0.94\columnwidth}{\small
\textbf{Algorithm 1: \textsc{KGA} (Knowability-Guided Adaptation).}\\[2pt]
\textbf{Input:} frozen $f_0$; candidate adaptation $f_a$; unlabeled target batch $X_{1:m}$;
calibrated benefit model $\widehat\Delta(\cdot)$; level $\alpha$.\\
\textbf{1.}~Extract label-free evidence $Z=\phi(X_{1:m},f_0,f_a)$ (entropy, confidence,
predicted-class balance and its drift, marginal-prediction KL, update norm, detector
disagreement).\\
\textbf{2.}~Estimate benefit $\widehat\Delta\leftarrow\widehat\Delta(Z)$ and radius
$\varepsilon$ with $\Pr[\,|\widehat\Delta-\Delta|\le\varepsilon\,]\ge1-\alpha$ (split-conformal).\\
\textbf{3.}~\textbf{if} $\widehat\Delta-\varepsilon>0$: \textbf{return} \textsc{adapt} (deploy
$f_a$); \textbf{elif} $\widehat\Delta+\varepsilon<0$: \textbf{return} \textsc{freeze} (keep
$f_0$); \textbf{else}: \textbf{return} \textsc{abstain} (keep $f_0$, flag for review).\\[2pt]
\textbf{Guarantee (Thm~\ref{thm:cert}):} the false-adapt and false-freeze rates are each
$\le\alpha$.}}\end{center}

\paragraph{Assumptions.} \textsc{KGA} inherits exactly the assumptions of
Theorem~\ref{thm:cert}: (i) a labeled validation/calibration sample of paired benefits that is
exchangeable with deployment, from which $\varepsilon$ is built; (ii) bounded per-sample
benefits; and (iii) an evidence map $Z$ that is \emph{risk-aligned} (it brackets $\sign\Delta$;
\S\ref{sec:def}). When (iii) fails---evidence indistinguishable across opposite-benefit
worlds---the certificate cannot fire and \textsc{KGA} \emph{abstains}, which
Corollary~\ref{cor:forced-abstain} shows is mandatory, not merely conservative. No target
labels are used at any step.

\paragraph{Cost.} \textsc{KGA} adds no training to $f_0$ and introduces no new adaptation
mechanism: per decision it costs one forward pass of $f_0$ and $f_a$ on the batch plus an
evaluation of the lightweight benefit model. It is therefore a drop-in safety wrapper around
any already-deployed TTA candidate.

\section{Experiments}\label{sec:exp}
Data: a 123-task anomaly score archive (ADBench tabular, image-OOD, text, cyber,
fraud); each task has six detector scores on labeled validation and test. Frozen
$f_0=$ best-validation detector. Controlled-synthetic corruptions validate the
\emph{mechanism}, not real sensor failure. All numbers come from
the released reproducibility package.

\paragraph{Safety on the clean suite.} With candidate $f_a=$ rank-normalized logistic
stack, \textsc{KGA}'s abstentions concentrate where the true benefit is near zero
(mean $|\Delta|=0.021$ on abstained vs $0.132$ on acted tasks); adapt precision $0.90$.
This suite is helpful-dominated, so always-adapt (mean AUROC $0.777$) edges the safe
policy ($0.766$); coverage $17\%$. Honest reading: when adaptation almost always helps,
abstention mostly costs coverage.

\paragraph{Harmful regime.} With $f_a=$ reliability-fusion that \emph{hurts on 80\% of
tasks}, \textsc{KGA} refuses it almost everywhere and matches the safe baseline
(AUROC $0.748$) while always-adapt drops to $0.728$; regret-to-oracle falls from
$0.0214$ to $0.0019$ ($\approx11\times$). See Fig.~\ref{fig:harm}.

\paragraph{Controlled mixed regime (369 instances).} Three conditions per task:
\emph{clean} (adapt mildly harmful), \emph{detectable failure} (best detector
corrupted with a distribution shift; KS drift observable), \emph{covert failure} (best
detector permuted; signal destroyed but marginal---hence $Z$---nearly unchanged).
Decisions by true regime are in Table~\ref{tab:mixed}; adapt precision $0.935$,
false-adapt rate $0.065$. Policy AUROC: always-adapt $0.697$, always-freeze $0.586$,
\textsc{KGA} $0.690$, oracle $0.719$ (Fig.~\ref{fig:mix}). \textsc{KGA} beats
always-freeze by $0.10$ and ties always-adapt---because harmful adaptation is mild in
this domain.

\begin{table*}[t]\centering
\begin{tabular}{lrrrr}
\toprule
condition & true mean $\Delta$ & \textsc{adapt} & \textsc{freeze} & \textsc{abstain}\\
\midrule
clean & $-0.041$ & 8 & 5 & 110\\
detectable failure & $+0.193$ & 71 & 3 & 49\\
covert failure & $+0.181$ & 59 & 3 & 61\\
\bottomrule
\end{tabular}
\caption{\textsc{KGA} decisions by true regime (controlled mixed benchmark).}
\label{tab:mixed}
\end{table*}

\paragraph{Empirical non-identifiability witness (partial).} Clean instances have mean
$\Delta=-0.041$ while covert-failure instances have mean $\Delta=+0.181$ (opposite
sign), yet their evidence is far closer (mean standardized $Z$-distance clean$\to$covert
$1.35$ vs clean$\to$detectable $3.10$), weakening committal decisions on that boundary,
consistent with Theorem~\ref{thm:imp}. It is \emph{partial}: permutation preserves
marginals but slightly perturbs correlation structure, so $Z$ is not perfectly identical.

\paragraph{Clean non-identifiability witness.} We also construct the exact witness of
Theorem~\ref{thm:imp}: two worlds with $X\sim\mathcal N(0,1)$, $f_0=\mathbf1[X{>}0]$,
$f_a=\mathbf1[X{<}0]$, and flipped labels, so the benefit is exactly opposite
(world means $-1.0$ vs $+1.0$) while every evidence feature is a function of $X$ only.
Across $300$ instances all five $Z$ features are statistically \emph{indistinguishable}
between the worlds (two-sample KS $p\in[0.16,0.96]$, all $>0.05$), yet the truth is
opposite---so $Z$ cannot decide. \textsc{KGA} \textbf{abstains on $100\%$} of instances;
a rule forced to commit by $\sign\widehat\Delta$ pays regret $0.51$ (near chance). This
is the clean theory--experiment bridge (Fig.~\ref{fig:witness}).

\begin{figure}[tb]\centering
\includegraphics[width=0.6\linewidth]{figures/fig_witness_clean.png}
\caption{Clean non-identifiability witness (Synthetic): identical $Z$-law (KS $p>0.05$ on all
features), exactly opposite true benefit, $100\%$ abstention. Empirical realization of
Theorem~\ref{thm:imp}.}
\label{fig:witness}
\end{figure}

\paragraph{Statistical rigor (8 seeds, paired $t$-tests).} Repeating the mixed-regime
experiment over 8 seeds (split-conformal, $\alpha{=}0.1$): mean test AUROC
$0.686\pm0.003$ (\textsc{KGA}), $0.697\pm0.000$ (always-adapt), $0.584\pm0.001$
(always-freeze), $0.718\pm0.001$ (oracle); adapt precision $0.936\pm0.010$, false-adapt
$0.064\pm0.010$, coverage $0.373\pm0.017$. Paired $t$-tests: \textsc{KGA} vs
always-freeze $t{=}62.2,\ p{=}7.3\times10^{-11}$ (\textsc{KGA} significantly higher);
\textsc{KGA} vs always-adapt $t{=}-8.8,\ p{=}5.1\times10^{-5}$ (significantly lower).
Both signs are honest: \textsc{KGA} reliably beats freezing and, in this mild-harm
domain, reliably trails always-adapt.

\paragraph{Ablations.} Table~\ref{tab:abl} (left) drops each evidence feature: detector
\emph{disagreement} is the most important---removing it raises regret from $0.037$ to
$0.094$ ($2.6\times$), directly supporting Theorem~\ref{thm:disagree}. An $\alpha$ sweep
traces the safety/coverage tradeoff: $\alpha{=}0.01$ gives $0\%$ false-adapt at $11\%$
coverage, up to $\alpha{=}0.2$ giving $49\%$ coverage at $7\%$ false-adapt
(Fig.~\ref{fig:extra}, middle). A batch-size sweep confirms the certificate's
prediction: larger test batches shrink $\varepsilon$ ($0.44\!\to\!0.18$ from $16$ to
$256$ samples) and raise coverage ($2\%\!\to\!26\%$).

\begin{table*}[t]\centering\small
\begin{tabular}{lr@{\hskip 2em}lrr}
\toprule
drop feature & regret & $\alpha$ & coverage & false-adapt\\
\midrule
\textbf{disagreement} & \textbf{0.094} & 0.01 & 0.11 & 0.00\\
val\_mean & 0.043 & 0.05 & 0.26 & 0.07\\
log\_ntest & 0.039 & 0.10 & 0.35 & 0.065\\
(all features) & 0.037 & 0.20 & 0.49 & 0.07\\
\bottomrule
\end{tabular}
\caption{Left: evidence-drop ablation (higher regret $=$ more important feature).
Right: $\alpha$ sweep (safety/coverage tradeoff).}
\label{tab:abl}
\end{table*}

\paragraph{Generalization: regression under covariate shift (Theorem~\ref{thm:pos}).}
A synthetic regression task ($Y=X^\top\beta+\epsilon$, $P_S(Y|X)=P_T(Y|X)$, covariate
shift up to $4\sigma$) with frozen linear $f_0$ vs importance-weighted $f_a$. Here
importance weighting \emph{hurts} on $79\%$ of instances (high weight variance), so the
true harmful base rate is high; \textsc{KGA} refuses it entirely ($0$ adapt, $11$
freeze, $61$ abstain), achieving mean MSE $0.251$---matching always-freeze and the
oracle ($0.251$) while always-adapt(IW) is $0.338$ (Fig.~\ref{fig:extra}, right). This
shows the certificate's safety transfers \emph{beyond anomaly detection}, abstaining
exactly when the density-ratio variance makes adaptation unstable.

\paragraph{Corroborating repository evidence.} On MVTec-3D, reliability gating does no
harm on clean data ($0.882=0.882$) and recovers $+0.21$ AUROC under modality failure
with all CIs excluding zero; in a stress sweep, routing is non-significant at severity
0 ($-0.0039$) and significant only at severity 3 ($+0.039$, CI excludes zero)---the
predicted knowability crossover.

\begin{figure}[tb]\centering
\includegraphics[width=0.49\linewidth]{figures/fig_mixed_policies.png}\hfill
\includegraphics[width=0.49\linewidth]{figures/fig_mixed_decisions.png}
\caption{Mixed regime. Left: \textsc{KGA} beats always-freeze and ties always-adapt.
Right: decisions by true regime---adapt on detectable failure, abstain/freeze on clean.}
\label{fig:mix}
\end{figure}
\begin{figure}[tb]\centering
\includegraphics[width=0.49\linewidth]{figures/fig_kbound_harmful.png}\hfill
\includegraphics[width=0.49\linewidth]{figures/fig_certificate.png}
\caption{Left: harmful regime---\textsc{KGA} avoids the harmful fusion path.
Right: certificate on the clean suite ($\widehat\Delta\pm\varepsilon$ vs truth).}
\label{fig:harm}
\end{figure}

\begin{figure}[tb]\centering
\includegraphics[width=0.32\linewidth]{figures/fig_ablation_evidence.png}\hfill
\includegraphics[width=0.32\linewidth]{figures/fig_alpha_coverage.png}\hfill
\includegraphics[width=0.32\linewidth]{figures/fig_regression.png}
\caption{Left: evidence-component ablation (disagreement dominates). Middle: $\alpha$
sweep---safety vs coverage. Right: regression covariate-shift---\textsc{KGA} matches the
oracle by refusing high-variance importance weighting.}
\label{fig:extra}
\end{figure}

\begin{figure}[tb]\centering
\includegraphics[width=0.62\linewidth]{figures/fig_decision_flow.png}\hfill
\includegraphics[width=0.36\linewidth]{figures/fig_regret_summary.png}
\caption{Left: the \textsc{KGA} decision flow (evidence $\to$ estimate $\to$
certificate $\to$ adapt/freeze/abstain). Right: \textsc{KGA} regret-to-oracle across all
experiments.}
\label{fig:flow}
\end{figure}
\FloatBarrier

\paragraph{Online non-stationary regime: cross-regime robustness (the decisive test).}
The single-shot regimes above are each dominated by one outcome, so a fixed policy is
already near-optimal. The real test is whether \emph{any single fixed policy is robust
across opposite regimes}. We evaluate \emph{online, continual} TTA on non-stationary
CIFAR-10 streams (ResNet-18, $0.874$ clean acc), under two schedules:
a \textbf{helpful-dominated} stream (mostly recoverable corruptions) and a
\textbf{harm-dominated} stream (long ``trap'' runs---single-class label shift, near-pure
noise, small batches---so continual Tent accumulates damage and \emph{collapses}).
\textsc{KGA} runs the certificate online: it commits a continual update only when it does
not degrade a clean labelled validation probe (K-Bound assumes labelled validation, no
\emph{target} labels), else freezes or abstains. Results over $3$ seeds
(Table~\ref{tab:online}, Fig.~\ref{fig:online}):

\emph{No fixed policy is robust.} Always-adapt is best in the helpful regime ($0.548$) but
\textbf{collapses} in the harm regime ($0.339$, $-0.10$ below freezing). Always-freeze is
best in the harm regime ($0.440$) but \textbf{forgoes gains} in the helpful regime
($0.472$). \textsc{KGA} is near-best in \emph{both} ($0.553$ and $0.426$) and never
collapses, giving the \textbf{highest mean across regimes} ($0.490$ vs always-freeze
$0.456$ vs always-adapt $0.444$). Honestly: within a single stream \textsc{KGA} ties the
better fixed policy (it edges always-adapt by $+0.005$ in the helpful regime and trails
always-freeze by $0.014$ in the harm regime); its advantage is \emph{robustness}---it is
the only policy that is near-oracle in both regimes, which is exactly the knowability
thesis (route per regime rather than commit to one policy).

\begin{table*}[t]\centering\small
\begin{tabular}{lcccc}
\toprule
regime & always-freeze & always-adapt & \textbf{K-Bound} & oracle \\
\midrule
helpful-dominated stream & $0.472$ & $0.548$ & $\mathbf{0.553}$ & $0.704$ \\
harm-dominated stream (collapse) & $0.440$ & $0.339$ & $0.426$ & $0.630$ \\
\midrule
\textbf{mean across regimes} & $0.456$ & $0.444$ & $\mathbf{0.490}$ & $0.667$ \\
\bottomrule
\end{tabular}
\caption{Online non-stationary CIFAR-10 TTA ($3$ seeds/regime). Always-adapt collapses in
the harm regime; always-freeze forgoes gains in the helpful regime; \textsc{KGA} is
near-best in both and has the highest mean across regimes. Harm-regime std: freeze
$0.009$, adapt $0.025$, KGA $0.009$.}
\label{tab:online}
\end{table*}

\begin{figure}[tb]\centering
\includegraphics[width=0.72\linewidth]{figures/fig_cifar_tent_online.png}
\caption{Per-window accuracy on the \emph{harm-dominated} stream (pink = catastrophic
``trap'' windows). Continual Tent (always-adapt) accumulates damage through the long trap
runs and \emph{collapses}; \textsc{KGA} freezes/abstains on traps and never collapses,
staying near always-freeze while capturing gains elsewhere.}
\label{fig:online}
\end{figure}

\paragraph{Per-corruption CIFAR-10-C (13 corruptions $\times$ 5 severities).} We also ran
the standard CIFAR-10-C protocol with the \emph{official} corruption functions
(\texttt{imagecorruptions}, Hendrycks \& Dietterich) applied to the CIFAR-10 test set, on a
ResNet-18 ($0.874$ clean), comparing frozen, online Tent, EATA-style, SAR-style, and
\textsc{KGA} across $65$ cells (Table~\ref{tab:c10c}, Figs.~\ref{fig:c10c}). The honest
finding is a \emph{negative for the collapse hypothesis in this regime}: episodic
per-corruption online Tent \textbf{helps almost everywhere}---mean accuracy $0.412$
(frozen) $\to 0.626$ (Tent), near the per-cell oracle $0.629$, with a false-adapt rate of
only $0.015$ (it hurt in $1/65$ cells, never catastrophically; even contrast s5
$0.16\!\to\!0.61$, worst cell $0.08\!\to\!0.26$). Hence CIFAR-10-C per-corruption is a
\emph{helpful-dominated} regime: always-adapt is strong and \textsc{KGA} correctly matches
it ($0.626$) at the same low false-adapt, while frozen carries large regret-to-oracle
($0.217$ vs $\approx0.003$). Catastrophic Tent collapse does \emph{not} appear
per-corruption here; it is the \emph{continual non-stationary} phenomenon of the previous
paragraph---consistent with the TTA literature studying collapse in continual/mixed
streams rather than single corruptions. In Table~\ref{tab:c10c} \textsc{KGA} wraps online
Tent (the most collapse-prone candidate); EATA/SAR are the standalone candidates, and the
full $65$ per-cell numbers are in Appendix~\ref{app:c10c}.

\begin{table*}[t]\centering\small
\begin{tabular}{lccc}
\toprule
method & mean acc (65 cells) & false-adapt rate & regret-to-oracle \\
\midrule
frozen & $0.412$ & $0.000$ & $0.217$ \\
Tent (online) & $0.626$ & $0.015$ & $0.0030$ \\
EATA-style & $0.626$ & $0.015$ & $0.0032$ \\
SAR-style & $0.627$ & $0.015$ & $0.0021$ \\
\textbf{K-Bound (KGA)} & $0.626$ & $0.015$ & $0.0032$ \\
oracle (per-cell) & $0.629$ & --- & --- \\
\bottomrule
\end{tabular}
\caption{CIFAR-10-C, 13 official corruptions $\times$ 5 severities, online TTA. Adaptation
is overwhelmingly helpful per-corruption (false-adapt $0.015$); \textsc{KGA} matches
always-adapt at equal safety. This is the helpful-dominated control, not a collapse setting.}
\label{tab:c10c}
\end{table*}

\begin{figure}[tb]\centering
\includegraphics[width=0.55\linewidth]{figures/fig_cifar10c_collapse.png}\hfill
\includegraphics[width=0.42\linewidth]{figures/fig_cifar10c_crossover.png}
\caption{CIFAR-10-C. Left: severity-5 accuracy by corruption---online Tent and \textsc{KGA}
both recover the frozen baseline (no per-corruption collapse). Right: knowability crossover
map---adaptation headroom (oracle gain over freeze) by corruption $\times$ severity; large
on noise/blur, small on easy corruptions (brightness), matching where $\Delta$ is large.}
\label{fig:c10c}
\end{figure}

\paragraph{CIFAR-10-C stress grid: the harmful regime (the decisive test).}
The per-corruption suite above is helpful-dominated by construction. To probe the
\emph{harmful} regime on the same standard benchmark we ran a \emph{pre-registered} stress
grid over the official CIFAR-10-C corruptions, crossing severity $\{1,5\}\times$ batch
$\{$large-iid $200$, small $16$, tiny $8\}\times$ composition $\{$iid, class-imbalanced,
single-class/label-shift$\}\times$ update $\{$mild $10$ steps; aggressive $50$ steps,
lr $2.5{\times}10^{-3}\}$, $2$ repeats ($432$ conditions/method). Benefit $B$ is measured on
a class-balanced held-out eval set so collapse is real accuracy loss, not a batch artifact;
\textsc{KGA} sees only label-free $Z$. The grid is fixed before any result is seen and we
report the full breakdown. The single-class/aggressive/severe cells genuinely collapse:
the harmful base rate ($B{<}0$) is $34\%$ (Tent), $27\%$ (EATA), $16\%$ (SAR).

On this mix \textsc{KGA} \textbf{beats both} trivial policies for Tent and EATA
(Table~\ref{tab:decisive}): regret-to-oracle $0.0016$/$0.0015$ versus always-adapt
$0.0086$/$0.0037$ ($\sim$$5.4\times$/$2.5\times$ lower) and always-freeze $0.123$/$0.131$
($\sim$$80\times$ lower), at \textbf{$0\%$ false-adapt} (adapt-precision $1.0$). It routes by
true regime exactly as the theory predicts: among harmful cells it adapts $0$ and freezes
them; among helpful cells it adapts almost all; on marginal cells it abstains (Tent: harmful
$0/63$ adapt/freeze, helpful $218/0$, $120$ abstentions on marginal). For SAR---whose
entropy-reset already prevents most collapse---\textsc{KGA} \emph{ties} always-adapt
($0.0018{=}0.0018$) and never does worse, consistent with the theory that the certificate's
value scales with how often detectable harm occurs. The mixing-ratio Pareto
(Fig.~\ref{fig:decisive}) makes the claim precise: \textsc{KGA} is on the regret-vs-mix
Pareto front, strictly beating both trivial policies once the harmful fraction exceeds
$p^\star\!\approx\!0.1$ (Tent/SAR) or $0$ (EATA), and tying always-adapt at $p{=}0$.

\begin{table*}[t]\centering\small
\begin{tabular}{llccccc}
\toprule
candidate & policy & mean acc & regret$\downarrow$ & false-adapt & coverage & beats both? \\
\midrule
Tent & always-adapt  & $0.786$ & $0.0086$ & --- & $1.00$ & \\
     & always-freeze & $0.671$ & $0.1232$ & --- & $0.00$ & \\
     & \textbf{K-Bound} & $\mathbf{0.793}$ & $\mathbf{0.0016}$ & $\mathbf{0.00}$ & $0.67$ & \textbf{yes} \\
\midrule
EATA & always-adapt  & $0.798$ & $0.0037$ & --- & $1.00$ & \\
     & always-freeze & $0.671$ & $0.1311$ & --- & $0.00$ & \\
     & \textbf{K-Bound} & $\mathbf{0.801}$ & $\mathbf{0.0015}$ & $\mathbf{0.00}$ & $0.63$ & \textbf{yes} \\
\midrule
SAR  & always-adapt  & $0.806$ & $0.0018$ & --- & $1.00$ & \\
     & always-freeze & $0.671$ & $0.1372$ & --- & $0.00$ & \\
     & \textbf{K-Bound} & $0.806$ & $0.0018$ & $0.00$ & --- & ties adapt \\
\bottomrule
\end{tabular}
\caption{CIFAR-10-C \emph{stress grid} ($432$ conditions/method, ResNet-18, commodity GPU ($16$\,GB),
$\alpha{=}0.1$). With genuine harmful cells present, \textsc{KGA} beats both trivial policies
for Tent and EATA at $0\%$ false-adapt, and ties collapse-resistant SAR; per-cell oracle
accuracy is $0.794$/$0.802$/$0.808$. Full results:
.}
\label{tab:decisive}
\end{table*}

\begin{figure}[tb]\centering
\includegraphics[width=0.96\linewidth]{figures/fig_decisive_pareto_cifar10c.png}
\caption{CIFAR-10-C stress grid, mixing-ratio Pareto: mean regret-to-oracle vs the harmful
fraction $p$ of the deployment stream, per candidate (Tent/EATA/SAR). \textsc{KGA} (green)
stays on the Pareto front---tying always-adapt at $p{=}0$, strictly beating both trivial
policies for $p\gtrsim p^\star$, and never collapsing like always-adapt or stalling like
always-freeze.}
\label{fig:decisive}
\end{figure}

\paragraph{Breadth across existing datasets, and uncertainty.} Re-running the certificate on
$62$ additional label-free anomaly-detection tasks (precomputed detector scores; no neural
retraining) stress-tests safety in a regime that is $85\%$ harmful: \textsc{KGA} makes $0$
false-adapt decisions and matches always-freeze (regret $0.0055$ vs always-adapt $0.051$, a
$9.3\times$ reduction), correctly refusing to adapt where the ensemble candidate hurts. It does
not \emph{beat} freezing here because almost nothing is helpful---the honest, correct behaviour,
and consistent with the knowability frontier (Thm.~\ref{thm:frontier}): this domain sits deep in the
knowably-harmful region. \emph{Corrected uncertainty quantification (per-condition).} An
earlier draft reported mixing-bootstrap intervals over a synthetic harmful-fraction
stream; we replace them with paired per-condition bootstrap CIs ($10^4$ resamples) with
Holm correction over the comparison family, computed on the real logged 65-cell
CIFAR-10-C data: \textsc{KGA} vs always-\emph{freeze} survives correction for all three
candidates ($\Delta\mathrm{regret}\approx-0.214$, 95\%~CI $[-0.245,-0.183]$,
$p\approx2\times10^{-20}$), while \textsc{KGA} vs always-\emph{adapt} is \emph{not}
significant on this helpful-dominated grid (Tent $+0.0001$, ns; EATA $\approx0$, ns;
SAR $+0.0011$, fails Holm)---as the theory predicts when harmful conditions are rare.
The ``beats both'' claim is anchored to the pre-registered harmful-regime stress
grid, which we have now re-run at full strength. \emph{Pre-registered $5$-seed
re-run (Protocol~A).} Following the frozen plan registered
before any result was observed, we replayed the identical $432$-condition CIFAR-10-C
grid across seeds $0$--$4$ and serialized every per-condition array, replacing the
old mixing bootstrap with per-condition \emph{paired} bootstrap CIs ($10^4$ resamples,
seeds pooled by per-condition mean) under Holm correction over the $6$-comparison
family. The pre-stated verdict is \textbf{STANDS}: \textsc{Tent} and \textsc{EATA}
each beat \emph{both} trivial policies after correction. Against always-\emph{freeze},
\textsc{KGA} cuts mean regret by $0.123$ (Tent; $95\%$~CI $[-0.138,-0.108]$),
$0.130$ (EATA; $[-0.145,-0.115]$) and $0.139$ (SAR; $[-0.154,-0.124]$); against
always-\emph{adapt} it wins for Tent ($-0.0064$, $[-0.0079,-0.0049]$) and EATA
($-0.0020$, $[-0.0028,-0.0013]$), all surviving Holm at $\alpha{=}0.05$
($p_{\mathrm{Holm}}{=}6\times10^{-4}$; between-seed regret SD ${\sim}2\times10^{-4}$).
The false-adapt rate (adapt with $B\le0$) is $0/2160$ for every candidate, far below
$\alpha{=}0.10$, and conformal $\varepsilon$ is stable across seeds (CV $2.7$--$6.8\%$).
\textsc{SAR} ties always-adapt as pre-stated ($+0.0012$, $[+0.0009,+0.0015]$, does
not beat): its harmful base rate ($0.07$--$0.12$) sits at/below $p^\star{\approx}0.1$,
so adaptation is almost always safe and \textsc{KGA}'s small abstention cost is not
repaid. This confirms the $p^\star$ regime law: per-seed, ``beats both'' is monotone
in the harmful fraction and single-threshold separable---every non-beating case
(\textsc{SAR}) has harmful fraction $\le0.116$, every beating case (\textsc{EATA},
\textsc{Tent}) $\ge0.234$, as recorded in the locked analysis artifact. Additionally,
a first \emph{real-data audit} of the
Theorem~\ref{thm:conj1-dichotomy} machinery on the anomaly bank (6-detector panels,
46--52 usable tasks) behaves as proved: the falsifier rejects H on ${\sim}84\%$ of
tasks (correlated detectors---the diagnostic working, not failing), and where H passes,
relative-sign recovery is $0.78$ versus ${\approx}0.49$ (chance) where it is rejected;
advantage \emph{magnitudes} do not transfer under the bank's extreme class imbalance
($\pi_D\le0.06$), reported as a negative real-data audit.

\subsection{External validation: natural shift and ImageNet scale}\label{sec:external}

\paragraph{ImageNet-R, 48-condition light grid (single seed): the falsifier in charge.}
On ImageNet-R (200 classes, 30{,}000 images; severity$\times$composition$\times$batch$\times$update,
$48$ conditions, $288$ records, single commodity GPU) the regime is mild and weakly
detectable: harmful base rate $0.139$, mean benefit $+0.017$, best label-free harm
detectability AUC $0.662$. The multi-candidate diagnostic of
Theorem~\ref{thm:conj1-dichotomy} rejects the structural hypothesis H on \emph{all}
$48$ conditions ($\hat\tau\in[1.07,2.15]$ against threshold $\tau^\star{=}0.52$): the
TTA candidates are co-adapted, exactly the correlated-panel violation the falsifier
exists to catch, so the certified route abstains on $100\%$ of conditions---mandatory
under the theory, and consistent with the anomaly-bank audit
(\S\ref{sec:exp}). The honest cost of that validity is the forfeited mean oracle gain
of $+0.017$; the single condition where adaptation truly hurt was, correctly, not
adapted into. A smooth-drift fallback route commits conservatively ($7/48$
\textsc{freeze}, $41/48$ \textsc{abstain}; sign-bracket coverage $0.77$). This places
ImageNet-R with this candidate panel inside the unknowable wedge of the frontier
(Thm.~\ref{thm:frontier}): small margins, falsified structure, weak detectability.
Testing whether an \emph{independently trained} (non-co-adapted) panel passes H and
lets the sign certificate fire is the pre-registered next run. Single-seed, reported
as scoping evidence, not a headline.

\paragraph{Audit re-analysis of logged evidence (CPU).} Re-running the
Theorem~\ref{thm:conj1-dichotomy}(iv) audit and three-zone accounting (FALSIFIED /
CERTIFIED / BLIND, $\beta{=}0.05$, $\alpha{=}0.05$, bootstrap radius) over the logged
grids gives, per grid: ImageNet-R light ($33$ logged conditions $\to$ $33$ falsified /
$0$ certified / $0$ blind), ImageNet-R $1\%$ ($6\to 6/0/0$), and the CIFAR-10-C
$65$-cell grid ($65\to 64$ realized-benefit certified / $1$ blind, a reduced
oracle-sign accounting). Across all $104$ auditable conditions the certificate issued
\emph{zero false-certifications}, and both true-harm conditions landed in the safe
falsified/blind zones (fraction $1.00$). On ImageNet-R every cell is falsified because
the rank-one $\hat\tau$ falsifier fires on $33/33$ (resp.\ $6/6$) cells and the budget
audit independently rejects on $29/33$ (resp.\ $5/6$); the recovered benefit sign still
matches truth on $82\%$ of cells. The CIFAR-10-C, ImageNet-C, and CIFAR-10.1
\emph{decisive} grids ($432{+}36{+}36{+}36$ conditions) serialize only aggregate
metrics---per-condition agreements, $\hat\tau$, and $\hat b$ are not stored---so the
per-condition audit could not be reconstructed for them.
The three tables below carry the empirical load that earlier drafts deferred to future work:
a \emph{natural} distribution shift (hospital-to-hospital histopathology, WILDS Camelyon17),
\emph{ImageNet-scale} corruptions (ImageNet-C, ResNet-50), and a second, low-margin natural
shift (CIFAR-10.1). All three are complete; consistent with our integrity policy, no number
appears here until its run completes.

\begin{table*}[t]\centering\small
\begin{tabular}{llccc}
\toprule
candidate & policy & mean balanced acc & regret$\downarrow$ & beats both? \\
\midrule
Tent & always-adapt    & $0.817$ & $\mathbf{0.000}$ & \\
     & always-freeze   & $0.786$ & $0.031$ & \\
     & \textbf{K-Bound} & $0.797$ & $0.020$ & no \\
\midrule
EATA & always-adapt    & $0.836$ & $\mathbf{0.000}$ & \\
     & always-freeze   & $0.786$ & $0.050$ & \\
     & \textbf{K-Bound} & $0.818$ & $0.018$ & no \\
\bottomrule
\end{tabular}
\caption{\textbf{Natural hospital shift (WILDS Camelyon17), 4 seeds.} DenseNet-121 trained on
source hospitals ($4$ epochs/seed), evaluated on the held-out hospital ($85{,}054$-patch OOD
test; $\sim\!94\%$ patch subset, storage-limited extraction). Adaptation helps on \emph{every}
seed (mean benefit Tent $+0.031$, EATA $+0.050$, all four seeds positive), so always-adapt is
the oracle (regret $0$). \textsc{KGA} correctly leans toward adapting and beats always-freeze,
but its finite-sample ($n{=}4$) caution costs $\approx\!0.02$ regret versus always-adapt---it
does \emph{not} beat both here. This is the honest benign-regime behaviour: with no detectable
harm to avoid, the certificate is insurance with a small cost, not a win (contrast the
harmful-cell regimes of Tables~\ref{tab:decisive} and~\ref{tab:imagenetc}).}
\label{tab:camelyon}
\end{table*}

\begin{table*}[t]\centering\small
\begin{tabular}{llcccc}
\toprule
candidate & policy & mean acc & regret$\downarrow$ & harmful cells & beats both? \\
\midrule
Tent & always-adapt     & $\mathbf{0.453}$ & $0.0003$ &        & \\
     & always-freeze    & $0.410$          & $0.0440$ &        & \\
     & \textbf{K-Bound} & $0.449$          & $0.0042$ & $8\%$  & no (ties adapt) \\
\midrule
EATA & always-adapt     & $\mathbf{0.444}$ & $0.0001$ &        & \\
     & always-freeze    & $0.410$          & $0.0349$ &        & \\
     & \textbf{K-Bound} & $0.444$          & $0.0003$ & $3\%$  & no (ties adapt) \\
\midrule
SAR  & always-adapt     & $0.377$          & $0.0606$ &        & \\
     & always-freeze    & $0.410$          & $0.0277$ &        & \\
     & \textbf{K-Bound} & $\mathbf{0.429}$ & $\mathbf{0.0086}$ & $31\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\caption{\textbf{ImageNet scale (ImageNet-C, ResNet-50), complete: $36$ cells, $4000$ images/cell.}
Full noise grid (gaussian/shot/impulse $\times$ severities $\{1,3,5\}$ $\times$ batch/aggressiveness
regimes), same adapt/freeze/abstain protocol and metrics as Table~\ref{tab:decisive}. The outcome
splits by how robust each method is on this benchmark. \emph{Tent} and \emph{EATA} are robust here:
adaptation helps (harmful in only $8\%$/$3\%$ of cells), so always-adapt is near-optimal and
\textsc{KGA} correctly adapts---tying always-adapt to within $0.004$ and beating always-freeze, the
honest ``does-no-harm'' outcome. \emph{SAR} collapses on $31\%$ of cells (mean benefit $-0.033$):
always-adapt falls to $0.377$ while \textsc{KGA} certifies-or-abstains to $0.429$, \emph{beating
both} trivial policies and approaching the oracle ($0.437$). Across all three methods \textsc{KGA}
never underperforms the better trivial policy, and strictly wins on the one method that is harmful.
This first ResNet-50 SAR port is corroborated by a mechanism-faithful re-run
(Table~\ref{tab:imagenetc-faithful}).}
\label{tab:imagenetc}
\end{table*}

\paragraph{Decision baselines, executed.}
Theorem~\ref{thm:frontier}(iv) shows prior decision styles are
degenerate ($\varepsilon{\equiv}0$) certificates; Table~\ref{tab:decision-baselines}
\emph{runs} them, head-to-head with \textsc{KGA}, on the logged conditions of the
mechanism-faithful ImageNet-C SAR grid (Table~\ref{tab:imagenetc-faithful}; per-condition
evidence $Z$, $a_0$, $a_a$; identical metrics code). Each
single-statistic rule is evaluated both as a plug-in (threshold $0$) and with a
\emph{leave-one-out tuned} threshold --- i.e.\ the baselines receive the same labeled
calibration information \textsc{KGA}'s benefit model uses. True AETTA (dropout passes)
and agreement-on-the-line (a model family) are not computable from logged statistics
and are represented by their decision-style surrogates; we say so rather than
approximate silently. Findings: on the harmful candidate (SAR, $44\%$ harmful cells
under reference parity) \emph{every realizable single-statistic rule loses} --- the
best (LOO-tuned EATA-filter, regret $0.0369$) pays $1.6\times$ \textsc{KGA}'s $0.0229$
and is still worse than plain always-freeze, while the plug-in ATC and entropy-progress
rules \emph{over-adapt} into SAR's collapse (regret $0.067$/$0.077$); \textsc{KGA} is
the only policy with \emph{zero} harmful adaptations across all three candidates. On
the benign candidates (Tent/EATA) the tuned baselines simply learn ``always adapt''
and tie it, while the plug-in rules \emph{hurt} (regret up to $0.033$). One honest
nuance: \textsc{KGA}'s own benefit model run \emph{committally}
($\varepsilon{=}0$, no abstain) attains regret $0.001$ on SAR --- the multivariate
evidence model carries the decision power --- but it has no validity control and
adapts into harmful cells uncontrolled; the conformal band's $\approx0.02$
regret on SAR is the measured price of the false-adapt guarantee.

\begin{table*}[t]\centering\small
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lcccccc}
\toprule
 & \multicolumn{3}{c}{regret-to-oracle$\downarrow$} &
 \multicolumn{2}{c}{SAR (harmful regime)} & \\
\cmidrule(lr){2-4}\cmidrule(lr){5-6}
decision rule & Tent & EATA & SAR & false-adapt & harmful adapted & coverage \\
\midrule
always-adapt                          & $0.0007$ & $0.0000$ & $0.1124$ & $0.44$ & $1.00$ & $1.00$ \\
always-freeze                         & $0.0494$ & $0.0386$ & $0.0267$ & ---    & $0.00$ & $1.00$ \\
ATC-style confidence (plug-in)        & $0.0114$ & $0.0328$ & $0.0673$ & $1.00$ & $0.25$ & $1.00$ \\
ATC-style confidence (LOO-tuned)      & $0.0007$ & $0.0000$ & $0.0375$ & $1.00$ & $0.06$ & $1.00$ \\
entropy-progress (LOO-tuned)          & $0.0007$ & $0.0000$ & $0.0375$ & $1.00$ & $0.06$ & $1.00$ \\
EATA-filter $\mathrm{frac_{hi}}$ (LOO-tuned) & $0.0007$ & $0.0000$ & $0.0369$ & $1.00$ & $0.06$ & $1.00$ \\
drift-gate marginal-KL (LOO-tuned)    & $0.0028$ & $0.0023$ & $0.0382$ & $1.00$ & $0.12$ & $1.00$ \\
benefit model, committal ($\varepsilon{=}0$) & $0.0000$ & $0.0000$ & $\mathbf{0.0010}$ & $0.05$ & $0.06$ & $1.00$ \\
best single statistic (hindsight)     & $0.0007$ & $0.0000$ & $0.0181$ & $0.17$ & $0.06$ & $1.00$ \\
\textbf{K-Bound (KGA)}                & $0.0060$ & $0.0047$ & $0.0229$ & $\mathbf{0.00}$ & $\mathbf{0.00}$ & $0.39$ \\
\bottomrule
\end{tabular}
\caption{\textbf{Decision baselines executed on the logged ImageNet-C conditions}
(reference-parity SAR; $36$ cells/candidate; harmful base rates $6\%/3\%/44\%$ for
Tent/EATA/SAR). ``LOO-tuned'' rules choose their threshold leave-one-out on the
remaining conditions --- the same calibration information \textsc{KGA} uses;
``hindsight'' picks feature, sign, and threshold on the evaluation conditions
themselves (not realizable). \textsc{KGA} is the only rule with zero false adapts on
all three candidates; on the harmful candidate \emph{every realizable single-statistic
rule loses} --- the best (EATA-filter, $0.0369$) pays $1.6\times$ \textsc{KGA}'s
$0.0229$ and is still worse than plain always-freeze, because no single statistic
cleanly separates SAR's harmful cells. The committal benefit model (row~8) shows the
multivariate evidence model carries the decision power ($0.0010$ on SAR) but lacks
validity --- it adapts into harmful cells with no false-adapt control --- so the
abstain band's $\approx0.02$ regret on SAR is the measured price of the guarantee,
matching Thm.~\ref{thm:frontier}(iv). \emph{Reproducibility note:} the canonical KGA
pipeline and this post-hoc re-derivation agree exactly on SAR; the leave-one-out
benefit model is mildly sensitive to condition order, so the Tent/EATA \textsc{KGA}
regrets differ from Table~\ref{tab:imagenetc} by $\le0.004$.}
\label{tab:decision-baselines}
\end{table*}

\begin{figure}[tb]\centering
\includegraphics[width=0.96\linewidth]{figures/fig_decisive_pareto_imagenetc.png}
\caption{\textbf{ImageNet-C (noise, ResNet-50): \textsc{KGA} is Pareto-optimal for the harmful
candidate SAR.} Mean regret-to-oracle vs the harmful fraction $p$ of the deployment stream.
\textsc{KGA} (green) ties always-adapt at $p{=}0$ and strictly beats \emph{both} trivial policies
for $p\gtrsim0.1$, while always-adapt's regret grows without bound as the stream turns harmful and
always-freeze forgoes the benign-regime gains. ImageNet-scale counterpart of Fig.~\ref{fig:decisive};
the visual form of the SAR row of Table~\ref{tab:imagenetc}. Produced by
; data.}
\label{fig:imagenetc}
\end{figure}

\paragraph{ImageNet-C, mechanism-faithful SAR re-run: the harmful win is not an implementation artifact.}
The SAR row of Table~\ref{tab:imagenetc} was flagged for re-checking (\S\ref{sec:limits}): its
collapse concentrated atypically at the \emph{larger} batch, raising the worry that the harmful
regime---and thus \textsc{KGA}'s only ImageNet-scale ``beats both''---was an artifact of our first
SAR port. We re-ran the full noise grid with a \emph{mechanism-faithful} SAR (sharpness-aware
minimisation, the entropy/recovery reset, and the EMA-stabilised update re-implemented to the
reference design), at the learning rate \emph{shared} across all candidates (apples-to-apples).
The harmful regime \emph{persisted and broadened}---SAR is now harmful on $44\%$ of cells (vs
$31\%$ in the first port)---and the \textsc{KGA} win \emph{survived}: regret-to-oracle $0.0229$
versus always-adapt $0.1124$ and always-freeze $0.0267$, \emph{beating both} trivial policies
(Pareto crossover $p^\star{=}0.2$; Table~\ref{tab:imagenetc-faithful}). Tent and EATA stay benign
($6\%$/$3\%$ harmful), so \textsc{KGA} ties the near-oracle always-adapt and beats always-freeze,
exactly as in the first port. The implementation-artifact concern is thus removed: under a
reference-faithful SAR the harmful-candidate win is real. \emph{Residual check, stated honestly.}
This holds at the matched/shared learning rate (mechanism-faithful, apples-to-apples); SAR's
\emph{official} gentler schedule (lr $2.5{\times}10^{-4}$ with \texttt{layer4} frozen) was not run,
and is the one configuration that could still soften the collapse---the last box to tick before the
SAR win is treated as fully settled.

\begin{table*}[t]\centering\small
\begin{tabular}{llcccc}
\toprule
candidate & policy & mean acc & regret$\downarrow$ & harmful cells & beats both? \\
\midrule
Tent & always-adapt     & $\mathbf{0.454}$ & $0.0007$          &        & \\
     & always-freeze    & $0.405$          & $0.0494$          &        & \\
     & \textbf{K-Bound} & $0.446$          & $0.0082$          & $6\%$  & no (ties adapt) \\
\midrule
EATA & always-adapt     & $\mathbf{0.444}$ & $0.0000$          &        & \\
     & always-freeze    & $0.405$          & $0.0386$          &        & \\
     & \textbf{K-Bound} & $0.440$          & $0.0038$          & $3\%$  & no (ties adapt) \\
\midrule
SAR  & always-adapt     & $0.319$          & $0.1124$          &        & \\
     & always-freeze    & $0.405$          & $0.0267$          &        & \\
     & \textbf{K-Bound} & $\mathbf{0.409}$ & $\mathbf{0.0229}$ & $44\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\caption{\textbf{ImageNet-C, mechanism-faithful SAR re-implementation ($36$ cells/candidate,
ResNet-50, $\alpha{=}0.1$).} Same noise grid, protocol, and metrics as Table~\ref{tab:imagenetc};
SAR re-implemented to the reference mechanism (SAM, recovery reset, EMA) at the learning rate shared
across candidates. The harmful regime survives the faithful re-implementation (SAR harmful on $44\%$
of cells, mean benefit $-0.086$): always-adapt collapses to $0.319$ while \textsc{KGA}
certifies-or-abstains to $0.409$, \emph{beating both} trivial policies (regret $0.0229$ vs
$0.1124$/$0.0267$); Tent/EATA stay benign and \textsc{KGA} ties always-adapt. Oracle accuracy
$0.455$/$0.444$/$0.432$. \emph{Caveat:} matched/shared LR; SAR's official lr $2.5{\times}10^{-4}$,
\texttt{layer4}-frozen schedule not yet run.}
\label{tab:imagenetc-faithful}
\end{table*}

\begin{table*}[t]\centering\small
\begin{tabular}{llcccc}
\toprule
candidate & policy & mean acc & regret$\downarrow$ & harmful cells & beats both? \\
\midrule
Tent & always-adapt     & $0.757$          & $0.0176$          &        & \\
     & always-freeze    & $\mathbf{0.772}$ & $\mathbf{0.0025}$ &        & \\
     & \textbf{K-Bound} & $\mathbf{0.772}$ & $\mathbf{0.0025}$ & $58\%$ & no (ties freeze) \\
\midrule
EATA & always-adapt     & $0.771$          & $0.0050$          &        & \\
     & always-freeze    & $\mathbf{0.772}$ & $\mathbf{0.0044}$ &        & \\
     & \textbf{K-Bound} & $\mathbf{0.772}$ & $\mathbf{0.0044}$ & $46\%$ & no (ties freeze) \\
\midrule
SAR  & always-adapt     & $0.773$          & $0.0050$          &        & \\
     & always-freeze    & $0.772$          & $0.0060$          &        & \\
     & \textbf{K-Bound} & $\mathbf{0.773}$ & $\mathbf{0.0048}$ & $38\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\caption{\textbf{Second natural shift (CIFAR-10.1 v6, ResNet-18), $24$ stream conditions per
method, single quick pass ($\alpha{=}0.1$).} CIFAR-10.1 is $\sim$2{,}000 genuinely new test
photographs collected by the original CIFAR-10 protocol---a \emph{real}, low-margin
distribution shift (frozen accuracy $0.771$). Per-condition benefits are small (mean $B$
between $-0.015$ and $+0.001$), so most evidence falls inside the abstain band:
\textsc{KGA} abstains on $71$--$92\%$ of conditions and makes \emph{zero} false adapts
(SAR adapt-precision $1.0$). For Tent and EATA---harmful in $58\%$/$46\%$ of
conditions---\textsc{KGA} routes to freezing and ties the better trivial policy while
always-adapt pays up to $7\times$ more regret; for SAR it strictly (if narrowly) beats both
policies and is Pareto-dominant for harmful fractions $p\gtrsim0.1$. The displayed row is a
single quick pass; a $5$-seed pooled re-run (seeds $0$--$4$, $24$ conditions per seed,
\texttt{cifar101\_multiseed\_v1/pooled\_summary.json}) confirms the regime ordering and the
small-regret regime: pooled harmful-condition rate $0.675{\pm}0.017$ (Tent) / $0.500{\pm}0.059$
(EATA) / $0.050{\pm}0.031$ (SAR) with \textsc{KGA} regret-to-oracle $0.0024{\pm}0.0007$ /
$0.0035{\pm}0.0011$ / $0.0045{\pm}0.0014$ respectively, so the abstain-dominated, near-tie
conclusion is stable across seeds.}
\label{tab:cifar101}
\end{table*}

\paragraph{Third natural-shift track: ImageNet-R lands in the \emph{unknowable} regime.}
The trichotomy's empirical map is now complete with real data in all three regimes:
knowably-helpful under matched composition (Camelyon17, Table~\ref{tab:camelyon}; with a detectable
harmful regime under composition stress, \S\ref{sec:natural-suite}), knowably-harmful (SAR on
ImageNet-C, Table~\ref{tab:imagenetc}), and --- with this track --- \emph{unknowable}.
On ImageNet-R (200 renditions classes, $30{,}000$ real images; a $48$-condition stress
grid over severity $\times$ composition $\times$ batch $\times$ aggressiveness,
Table~\ref{tab:imagenetr}) the regime is \emph{mixed and weakly detectable}: $13.9\%$ of
conditions are harmful, the mean benefit is small ($\bar B={+}0.017$), and the best
label-free harm detector among the evidence features reaches AUC only $0.662$. This is
precisely the wedge where Corollary~\ref{cor:forced-abstain} makes abstention
\emph{mandatory} for any valid certificate --- and \textsc{KGA} abstains on $48/48$
conditions. The abstention is also for the \emph{right reason}: the multi-candidate
route's CEI diagnostic fires on every condition ($\tau\in[1.07,2.15]$, all far above the
calibrated threshold $\tau^\star{=}0.52$) --- co-trained TTA candidates sharing one
backbone violate conditional error-independence exactly as the theory warns --- so the
agreement route \emph{refuses to certify} rather than issuing certificates its own guard
says are unsound. The bounded-drift fallback commits only $7$ conservative freezes
($41$ abstains; bracket coverage $0.77$, single seed). Honest cost and scope: abstaining
forfeits the small mean benefit ($\approx\!1.7$ points vs.\ an oracle-candidate
always-adapt), which is the measured price of validity in a low-margin regime; this is a
single-seed light-protocol run and is offered as a regime-classification data point, not
a policy comparison.

\begin{table}[tb]\centering\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lc}
\toprule
ImageNet-R stress grid ($48$ conditions) & value \\
\midrule
harmful base rate ($B<0$)                       & $13.9\%$ \\
mean true benefit $\bar B$                      & $+0.017$ \\
harm detectability (best evidence AUC)          & $0.662$ \\
\textsc{KGA} decisions                          & $48/48$ abstain \\
CEI diagnostic $\tau$ range (threshold $\tau^\star$) & $1.07$--$2.15$ ($0.52$) \\
bounded-drift fallback route                    & $41$ abstain / $7$ freeze \\
\bottomrule
\end{tabular}}
\caption{\textbf{ImageNet-R: a real natural shift in the \emph{unknowable} regime.}
Mixed harm ($13.9\%$), weak detectability (AUC $0.662$), low margins
($\bar B{=}{+}0.017$): the certificate abstains everywhere, as
Corollary~\ref{cor:forced-abstain} mandates, and the CEI diagnostic correctly refuses
the agreement route for co-trained candidates ($\tau\gg\tau^\star$ on all $48$
conditions). Single-seed light protocol; the three candidates share one backbone, which
is exactly the CEI-violating setting the diagnostic exists to catch.}
\label{tab:imagenetr}
\end{table}

\paragraph{Fourth natural-shift track: RxRx1 --- the harmful-and-\emph{detectable} corner at $1{,}139$-class scale.}
RxRx1 (WILDS; fluorescent cellular-microscopy images, $1{,}139$ siRNA classes, an OOD
\emph{experimental-batch} shift; the official WILDS ERM ResNet-50, in-distribution accuracy
${\approx}0.36$, frozen accuracy $0.283$ on the OOD-test eval, mean over the three seeds) is the
sharpest harmful-regime real shift in our suite. Over the full $9{+}$ protocol
(composition$\times$batch$\times$aggressiveness over $10$ condition seeds and all $3$ official WILDS
base-model seeds $=360$ conditions, $2{,}160$ records, a commodity GPU ($16$\,GB), complete
$360/360$), adaptation is \emph{harmful-dominated and label-free detectable}: harmful base rate
$81.5\%$ ($B<0$), mean benefit $\bar B={-}0.063$, and---unlike ImageNet-R---the harm \emph{is}
detectable (best evidence AUC $0.78$--$0.84$ across the three seeds, the BN-affine update-norm the top
signal). Always-adapt is actively destructive: even the best candidate (Tent-episodic, $0.276$) trails
the frozen model ($0.283$), and SAR-online \emph{collapses} to $0.024$ ($\approx$ chance over
$1{,}139$ classes) on the single-class/tiny/aggressive cells. Here the certificate's value is purely
defensive, and it delivers: the single-candidate \textsc{KGA} freezes or abstains on \emph{all} $360$
conditions with \emph{zero} false adapts, matching the frozen optimum (regret vs the per-condition
oracle $0.003$--$0.007$) and avoiding the collapse outright---on SAR-online it pays regret $0.000$
where always-adapt pays $0.259$. It does \emph{not} ``beat both'' because freezing already \emph{is}
the oracle policy (no helpful candidate exists to capture)---the honest does-no-harm outcome on the
harmful side, the mirror image of Camelyon17's benign-side tie (Table~\ref{tab:camelyon}). As on
ImageNet-R, the multi-candidate agreement route abstains on all $360$ conditions
($\hat\tau\in[1.06,2.71]$, all $\gg\tau^\star{=}0.52$): the six candidates share one backbone, the
conditional-error-independence violation the CEI diagnostic exists to catch. The harmful-dominated
verdict, the SAR-online collapse, the detectable harm, and the $100\%$ abstention \emph{replicate
across all three independent WILDS base-model seeds} (per-seed harmful base rate $74$--$88\%$, best
AUC $0.78$--$0.84$). RxRx1 thus closes the real-data map of the harmful corner---\emph{detectable}
harm at $1{,}139$-class scale where ``adapt'' is catastrophic and the zero-false-adapt guarantee
carries the load (Table~\ref{tab:rxrx1}). These results are from the three archived
official-seed manifests.

\begin{table}[tb]\centering\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lc}
\toprule
RxRx1 $9{+}$ grid ($360$ conditions, $3$ model seeds) & value \\
\midrule
harmful base rate ($B<0$)             & $81.5\%$ \\
mean true benefit $\bar B$            & $-0.063$ \\
harm detectability (best AUC)         & $0.78$--$0.84$ \\
frozen $f_0$ accuracy                 & $0.283$ \\
best-adapter accuracy                 & $0.276$ \\
SAR-online always-adapt (collapse)    & $0.024$ \\
per-condition oracle accuracy         & $0.288$ \\
\textsc{KGA} decisions                & $360/360$ no-adapt \\
\textsc{KGA} false-adapts             & $0$ \\
CEI $\hat\tau$ range (thresh.\ $\tau^\star$) & $1.06$--$2.71$ ($0.52$) \\
bounded-drift fallback                & $289$ abstain / $71$ freeze \\
\bottomrule
\end{tabular}}
\caption{\textbf{RxRx1: a real natural shift in the \emph{harmful-and-detectable} regime ($9{+}$ protocol).}
At $1{,}139$-class scale adaptation is harmful-dominated ($81.5\%$ of records $B<0$,
$\bar B{=}{-}0.063$) and the harm is label-free detectable (AUC $0.78$--$0.84$). Always-adapt is
catastrophic---SAR-online collapses to $0.024$ and even the best candidate ($0.276$) trails the
frozen model ($0.283$)---so freezing is the oracle policy, and the single-candidate certificate
freezes/abstains on all $360$ conditions with \emph{zero} false adapts, matching it. The
multi-candidate agreement route abstains everywhere ($\hat\tau\gg\tau^\star$ on all $360$): the six
candidates share one backbone, the CEI-violating setting the diagnostic catches. Verdict replicates
across all $3$ official WILDS base-model seeds; complete $360/360$, commodity GPU ($16$\,GB).}
\label{tab:rxrx1}
\end{table}
\clearpage

\paragraph{Fifth natural-shift track: Office-Home --- a \emph{covariate} shift where detectability and mixedness \emph{anti-correlate}.}
Office-Home ($4$ domains, $65$ classes, $15{,}588$ images) is a pure \emph{covariate} style shift
(Art / Clipart / Product / Real-World), a priori the most label-free-detectable shift family and so
the sharpest test of whether the harmful-\emph{detectable} corner survives away from label shift.
Source $f_0$ is a ResNet-50 (ImageNet-V2) fine-tuned on Real-World ($86.1\%$ source-val); the other
three domains are the targets, each split val/test, with $\tau^\star$, the candidate set, the
deployed adapter, and the conformal certificate calibrated on source$+$target-\emph{val} and the
held-out test evaluated \emph{once}. The $14$-candidate panel augments
Tent/EATA/SAR$\times\{$online,episodic$\}\times\{$mild,aggressive$\}$ with two \emph{inference-only}
priors --- a label-shift MLLS logit correction and a damped ``conservative'' correction (no
parameter update) --- over $2$ seeds $\times\{$iid,imbalanced,single-class$\}\times\{$tiny,small$\}$
($504$ target records/split). The decisive finding is structural: across domains, \emph{detectability
and mixedness anti-correlate}. The two domains whose gradient-TTA harm is cleanly detectable (Art,
Clipart: best-feature in-sample harm-AUC $0.90$/$0.99$ on val, $0.99$/$0.94$ on test) are
\emph{helpful-dominated} ($3$--$9\%$ harm), so there is essentially nothing to freeze; the one
genuinely \emph{mixed} domain (Product, $39\%$/$23\%$ harm) is exactly where the harm is \emph{not}
label-free detectable on transfer (certificate transfer-AUC $0.53$/$0.63$). Pooled, the best single
evidence feature separates harm \emph{in-sample} (AUC $0.81$/$0.79$, $n{\approx}432$ --- no longer a
small-$n$ artifact), but the \emph{source-calibrated} certificate \emph{anti-transfers}
(transfer-AUC $0.33$ val / $0.49$ test) --- the same source$\to$OOD non-transfer that caps the other
real shifts. The deployed (source-best) adapter \texttt{eata\_online\_mild} is \emph{net-helpful}
(always-adapt regret $0.0009$ val / $0.0000$ test vs \textsc{KGA}$=$freeze $0.028$/$0.027$), so the
safety play ties always-freeze but \emph{loses} to always-adapt; the multi-candidate route
abstains/freezes on every condition ($\tau^\star{=}1.12$ source-calibrated, target $\tau$ mostly
above it), regret-to-oracle $0.035$/$0.032$ vs best-fixed-adapt $0.006$/$0.005$. \textsc{KGA} makes
\emph{zero} false-adapts on both splits and does \emph{not} beat both on val or on the held-out test
(Table~\ref{tab:officehome}). The two added priors do not rescue a win: the label-shift candidate is
catastrophic (mean $B{=}{-}0.47$, $100\%$ harm --- trivially detectable, hence excluded from the
gradient-TTA AUC) and the conservative prior is itself mildly harmful (mean $B{=}{-}0.012$, $81\%$
harm), leaving the router no safe-helpful option. Office-Home thus contributes the
\emph{covariate-shift} data point and reinforces the obstruction from the other side: even the
a-priori-most-detectable shift family yields no deployed adapter that flips
helpful$\leftrightarrow$harmful with the harm visible in a \emph{transferable} certificate.

\begin{table}[!ht]
\centering
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcc}
\toprule
Office-Home (covariate shift; gradient-TTA) & val & held-out test \\
\midrule
Pooled benefit $\bar B$ & $+0.016$ & $+0.015$ \\
Harmful base rate ($B<0$) & $0.17$ & $0.12$ \\
Best single-feature harm-AUC (in-sample) & $0.81$ & $0.79$ \\
Source-calibrated certificate transfer-AUC & $0.33$ & $0.49$ \\
Mixed domain (Product): harm / transfer-AUC & $0.39$ / $0.53$ & $0.23$ / $0.63$ \\
Deployed \texttt{eata\_online\_mild}: \textsc{KGA}/adapt/freeze regret & $0.028/0.001/0.028$ & $0.027/0.000/0.027$ \\
Multi-candidate route/freeze/best-fixed regret & $0.035/0.035/0.006$ & $0.032/0.032/0.005$ \\
\textsc{KGA} false-adapts \;/\; \textbf{beats both} & $0$ \;/\; \textbf{no} & $0$ \;/\; \textbf{no} \\
\bottomrule
\end{tabular}}
\caption{\textbf{Office-Home: a covariate shift where detectability and mixedness anti-correlate.}
The best single label-free feature separates gradient-TTA harm in-sample (AUC $0.79$--$0.81$,
$n{\approx}432$), but the source-calibrated certificate \emph{anti-transfers} (transfer-AUC
$0.33$/$0.49$) and the only \emph{mixed} domain (Product) has label-free-\emph{undetectable} harm.
The deployed adapter is net-helpful, so \textsc{KGA} ties always-freeze, loses to always-adapt,
makes \emph{zero} false-adapts, and does not beat both on val or held-out test. $2$ seeds, $14$
candidates (incl.\ two inference-only priors), $n_{\mathrm{eval}}{=}320$.}
\label{tab:officehome}
\end{table}

\clearpage
\subsection{Natural-shift suite: do the harmful regimes occur on real data?}\label{sec:natural-suite}
The synthetic stress grids above manufacture harm by construction. The sharper question is whether
the \emph{harmful, detectable} regime---the wedge where the certificate is meant to earn its
keep---arises on \emph{natural} shift, and whether the operating point we calibrated on synthetic
data fires there. We probe this with two real shifts that bracket the detectability frontier.

\paragraph{Camelyon17 composition-stress grid: real harm that is label-free \emph{detectable}.}
\emph{Two tracks, kept distinct:} the 4-seed Camelyon17 TTA table
(Table~\ref{tab:camelyon}) is \emph{helpful-dominated}---adaptation helps on every
seed, always-adapt is the oracle, and \textsc{KGA} trails it by ${\approx}0.02$
regret---whereas the separate multi-candidate audit below shows harmful,
\emph{detectable} natural-shift cells exist, but is not yet a policy win because the
current $\tau$ threshold abstains at this operating point.
Re-running the hospital-shift candidates over a composition stress grid (iid / class-imbalanced /
single-class $\times$ \{test, val, id-val\} domains $\times$ mild/aggressive updates $\times$ $4$
seeds; $72$ conditions $\times$ $6$ TTA variants ${=}\,432$ cells, debug-scale
$n_{\mathrm{eval}}{=}256$/cell) turns the benign picture of Table~\ref{tab:camelyon} into a
\emph{mixed} one: $28.7\%$ of cells are harmful ($\bar B{=}{+}0.039$), and---crucially---the harm is
label-free \emph{detectable}: the best single evidence feature reaches harm-AUC $0.78$, and the
conformal certificate's own $-\widehat B$ score reaches $0.91$. This is the first natural-shift
confirmation that the harmful-and-detectable regime the theory targets is not a synthetic artifact.
\emph{Yet at our current operating point \textsc{KGA} abstains rather than commits.} The
multi-candidate route's gate passes on only $2.8\%$ of conditions---the real multi-candidate
statistic $\bar\tau{=}0.89$ exceeds the synthetic-calibrated threshold $\tau^\star{=}0.52$, so the
router \textsc{abstain}s on $61/72$ conditions, \textsc{freeze}s $10$, and commits a single adapt
(the six hospital-TTA variants are co-trained on one backbone---the very correlation the gate is
built to catch). The smooth-drift fallback likewise stays conservative ($21$ freeze / $51$ abstain,
bracket coverage $0.44$): its source-concept term is a conservative $f_0$ surrogate, so the
smooth-drift hypothesis is not cleanly satisfied here. Honest reading: Camelyon17 supplies the
missing \emph{harmful-and-detectable} natural-shift data point, but under the \emph{frozen,
Camelyon-free} $\tau^\star{=}0.52$ \textsc{KGA} does \emph{not} beat both---multi-candidate
regret-to-oracle $0.076$ vs always-freeze $0.071$ vs always-adapt $0.005$ (the worst of the three:
abstaining forgoes this helpful-leaning panel's gains, and the lone commit is harmful). The
$\tau^\star$ fit on synthetic grids is mis-matched to this panel's $\tau$ scale ($\bar\tau{=}0.89$);
\emph{recalibrating $\tau^\star$ on Camelyon17 itself would forfeit its role as an
external-validation set}, so we do not. The honest open direction is a \emph{scale-invariant},
source-calibrated $\tau$ statistic (future work), not per-dataset retuning. Debug-scale, single
protocol; reported as regime-existence evidence, not a policy win.

\paragraph{Headline natural-shift wins (Protocols G, H v2, M v2).}
All three use canonical \textsc{KGA} (GBR $\widehat\Delta(Z)$ + global $\varepsilon$,
$\alpha{=}0.10$ fixed) and the same success bar as Camelyon17 Protocol~G: held-out test
scored once, false-adapt $\le\alpha$, beats both trivial policies.

\textbf{Protocol~G} (WILDS Camelyon17, composition-stress grid): fixed online EATA,
rich $17$-dim evidence, dev hospitals $\{0,1\}$ / test $\{2,3,4\}$---regret
$3.6{\times}10^{-5}$ vs adapt $0.0013$ vs freeze $0.075$; false-adapt $2.6\%$
(Table~\ref{tab:camelyon17-G}).

\textbf{Protocol~H v2} (WILDS iWildCam): adapter \texttt{tent\_episodic} dev-locked from a
six-arm Tent/EATA/SAR panel on \texttt{id\_val} (cal seed $0$, eval seed $1$); held-out
test grid cal seed $0$ / test seed $1$ ($n{=}72$)---regret $0.0037$ vs adapt $0.103$ vs
freeze $0.0041$; false-adapt $0\%$ (Table~\ref{tab:iwildcam-H}). Margin vs always-freeze
is modest (damage-prevention regime).

\textbf{Protocol~M v2} (Office-Home): adapter \texttt{sar\_online\_aggressive} dev-locked
from a six-arm gradient-TTA online panel on target-val; held-out target-test cal seeds
$\{0,1\}$ / test seeds $\{0,1\}$ ($n{=}35$)---regret $0.0022$ vs adapt $0.047$ vs freeze
$0.016$; false-adapt $0\%$ (Table~\ref{tab:officehome-M}). Distinct from the deployed
\texttt{eata\_online\_mild} audit (Table~\ref{tab:officehome}), which is helpful-dominated.

\begin{table}[t]\centering\small
\caption{Camelyon17 Protocol~G ($n{=}54$ held-out cells).}
\label{tab:camelyon17-G}
\begin{tabular}{lccc}
\toprule
policy & regret$\downarrow$ & false-adapt & beats both? \\
\midrule
always-adapt  & $0.0013$ & --- & \\
always-freeze & $0.0749$ & --- & \\
\textbf{K-Bound} & $\mathbf{3.6{\times}10^{-5}}$ & $2.6\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]\centering\small
\caption{iWildCam Protocol~H v2: dev-locked \texttt{tent\_episodic}, test grid ($n{=}72$).}
\label{tab:iwildcam-H}
\begin{tabular}{lccc}
\toprule
policy & regret$\downarrow$ & false-adapt & beats both? \\
\midrule
always-adapt  & $0.1028$ & --- & \\
always-freeze & $0.0041$ & --- & \\
\textbf{K-Bound} & $\mathbf{0.0037}$ & $0\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]\centering\small
\caption{Office-Home Protocol~M v2: dev-locked \texttt{sar\_online\_aggressive}, target-test ($n{=}35$).}
\label{tab:officehome-M}
\begin{tabular}{lccc}
\toprule
policy & regret$\downarrow$ & false-adapt & beats both? \\
\midrule
always-adapt  & $0.0468$ & --- & \\
always-freeze & $0.0158$ & --- & \\
\textbf{K-Bound} & $\mathbf{0.0022}$ & $0\%$ & \textbf{yes} \\
\bottomrule
\end{tabular}
\end{table}
\FloatBarrier

\paragraph{Diagnostic ladder (Protocols B/F; not headline).}
Earlier Camelyon runs (sparse $Z$, multicandidate $\tau^\star$, synthetic $\varepsilon$ transplant)
explain failed operating points; they motivated Protocol~G. Protocol~F route audits on the same GPU
records show multicandidate routing fails; GBR+global and PPI+Mondrian variants can beat both but
are not the headline method; the held-out headline claims are Protocols~G / H v2 / M v2
(Tables~\ref{tab:camelyon17-G}, \ref{tab:iwildcam-H}, \ref{tab:officehome-M}).

\paragraph{Operating-point repair: $\varepsilon$ recalibrated, $\tau^\star$ left alone (a calibration \emph{diagnostic}, not external validation).}
The latent-win diagnosis above is a calibration claim, so we test it directly --- without
touching $\tau^\star$ (lowering it on a co-trained panel would defeat the CEI guard). \emph{Because
this step re-estimates $\varepsilon$ on a Camelyon17 split held out by seed, it touches the external
panel and is therefore a calibration diagnostic, not external validation---and, as shown next, it is
a negative in any case.} The
single-candidate radius $\varepsilon$ was fit on \emph{synthetic} grids; transplanted to
Camelyon17 it violates the Theorem~\ref{thm:cert} exchangeability premise. Re-estimating
$\varepsilon$ from a Camelyon17 calibration split held out \emph{by seed} restores that premise
(calibration and test residuals then share one law). We use all $\binom{4}{2}{=}6$ assignments of
$2$ calibration $+\,2$ test seeds; on each, fit the benefit estimator on the calibration seeds,
set $\varepsilon{=}\widehat{Q}_{1-\alpha}(|\widehat\Delta-B|)$ on \emph{their} residuals
($\alpha{=}0.10$ fixed), freeze it, and decide on the test seeds. The repair is mechanically
real: $\varepsilon$ collapses from the synthetic $\approx0.060$ to $0.030$ and is \emph{stable}
(range $0.007$, CV $0.11$), so the certificate now \emph{commits} (coverage $0.65$) instead of
abstaining. But it is not a level-$\alpha$ win: test-split mean regret is \textsc{KGA} $0.014$
$[0.007,0.021]$ versus always-freeze $0.052$ $[0.037,0.067]$ (cleanly beaten) but always-adapt
$0.013$ $[0.010,0.016]$ (CIs \emph{overlap} --- the panel is helpful-dominated), while
false-adapt is $0.185$ $[0.159,0.211]$, $\approx\!1.9{\times}\,\alpha$. Honest verdict: the harm
is label-free \emph{detectable} (AUC $0.91$) but \emph{not certifiable} at $\alpha{=}0.10$ at this
debug-scale $n_{\mathrm{eval}}{=}256$/cell --- a precise negative. Closing it needs more samples
per cell (tighter residuals) or a harm-skewed panel, not a smaller $\alpha$ or a moved $\tau^\star$.

\paragraph{Pre-registered full-scale re-run ($n_{\mathrm{eval}}{=}1024$, $5$ seeds): the gap
persists.} We pre-registered the $1/\sqrt{n}$ test \emph{before} observing any result
in Protocol~B: quadrupling the per-cell eval
pool to $n_{\mathrm{eval}}{=}1024$ should halve the conformal radius (predicted ratio
$\sqrt{256/1024}{=}0.5$) and pull false-adapt to $\le\alpha{=}0.10$. The $\alpha$-fixed, frozen-$\varepsilon$
seed-split rule was held identical. \emph{Outcome: the prediction is not confirmed.} The full-scale
runner serialized only one aggregate $(Z,B)$ point per seed (tent, eata), \emph{not} the $72{\times}6$
composition grid, so the $1/\sqrt{n}$ radius claim could be evaluated only at coarse per-seed
granularity ($5$ points, $\binom{5}{2}{=}10$ calibration splits). At that granularity the radius did
\emph{not} shrink ($\varepsilon{=}0.029$ tent / $0.038$ eata vs the debug $\varepsilon{\approx}0.030$;
ratio $\approx\!1.0$, not $0.5$) and false-adapt did \emph{not} fall below $\alpha$ ($0.33$ on the
committed seeds vs the archived $0.185$). \textsc{KGA} still cleanly beats always-freeze (regret
$0.020$ vs $0.027$ tent) but trails always-adapt (helpful-dominated panel), at commit rate $0.60$.
\textbf{Honest verdict (\textsc{Fails}, by the pre-registered criteria):} the
detectability--certifiability gap is \emph{not} closed by sample size alone---it is a deeper open
problem. A bias--variance decomposition of the radius \emph{explains why} the $1/\sqrt{n}$ prediction
failed. Splitting the eps-recal residual into estimator model error and
binomial measurement noise ($\sigma^2_{\mathrm{meas}}{=}a_0(1{-}a_0)/n{+}a_a(1{-}a_a)/n$,
$n_{\mathrm{eff}}{=}256$) on a leave-one-seed cross-fit gives an observed radius
$\varepsilon_{256}{=}0.112$ against a measurement floor $\varepsilon_{\mathrm{meas}}(256){=}0.044$
(ratio $2.55\times$), with only a fraction $\approx0.16$ of residual variance attributable to
measurement noise. The radius is thus \emph{bias-limited}: dominated by $\widehat{B}(Z)$ model error
(calibration-drift bias), which does not shrink with eval $n$. Hence the variance-limited target
$\varepsilon_{\mathrm{meas}}(1024){=}0.022$ is unreachable and a fair re-run \emph{cannot} close the
radius---the $\alpha$-fixed certifiability of this natural shift is bias-limited, not
sample-size-limited. The open problem is therefore reducing estimator bias (tying back to the
$\gamma$-frontier), not collecting more eval samples. (Cross-check: the per-seed $|B|$ spread at $1024$
is $0.71\times$ that at $256$, not the $0.5$ a variance-limited halving requires, though $n{=}10$ is
weak.)

\paragraph{The pair, read together.} Camelyon17 (detectable harm, AUC $0.78$) and ImageNet-R
(\emph{un}detectable harm, AUC $0.66$; Table~\ref{tab:imagenetr}) bracket the frontier of
Theorem~\ref{thm:frontier}: real harm exists on both ($28.7\%$ / $13.9\%$ of cells), but only on
Camelyon17 is it label-free separable. \textsc{KGA} abstains on both---\emph{correctly} on
ImageNet-R, where Corollary~\ref{cor:forced-abstain} makes abstention mandatory for any valid
certificate, and \emph{conservatively} on Camelyon17, where the harm is detectable but the
operating point is not yet tuned to it. Two honest conclusions follow: (i) the harmful regimes the
theory characterises do occur on natural shift, on both sides of the detectability frontier; and
(ii) turning the detectable side into a committed natural-shift win requires a $\tau$ statistic
whose operating point transfers \emph{without} tuning on the target---a \emph{scale-invariant,
source-calibrated} $\tau$ (future work); per-dataset recalibration on Camelyon17 would forfeit its
external-validation role and is not a legitimate path.

\subsection{Mixed-stream deployment: beating both policies at once}\label{sec:mixedstream}
Each single dataset above is \emph{one-sided}: Camelyon17 is adapt-favorable
(freezing leaves regret $0.075$), whereas OfficeHome and iWildCam are
freeze-favorable (adapting leaves regret $0.047$ and $0.103$). On a one-sided
stream the better trivial policy is already near-oracle, so \textsc{KGA}'s
simultaneous margin over \emph{both} is necessarily small---which is exactly why
the per-dataset beats-both gaps, though real (bootstrap CIs in
\texttt{research\_lock/KBOUND\_WIN\_BOOTSTRAP\_CIS.json}), are modest on the side
where a fixed policy is already correct. Real deployments, however, are
heterogeneous: a guard does not know in advance whether the next shift rewards
adaptation. We therefore pool the held-out test conditions of all three datasets
into a single deployment stream ($n{=}161$). Each dataset keeps its \emph{own}
locked gate---GBR benefit estimator with global conformal $\varepsilon$ at
$\alpha{=}0.10$, fit on that dataset's dev seeds and applied once to its test
seeds---so no decision is retuned; the per-dataset regrets reproduce the locked
single-dataset protocols to four decimals. We bootstrap ($B{=}5000$) the pooled
regret gaps over conditions.

\begin{table}[t]
\centering
\caption{\textbf{Heterogeneous (mixed) deployment.} Pooled held-out test
conditions of Camelyon17 ($54$), OfficeHome ($35$), iWildCam ($72$); $n{=}161$.
Each dataset uses its own locked dev-calibrated gate. On a stream that mixes
adapt- and freeze-favorable shift, neither global policy is near-oracle, but
\textsc{KGA} routes per condition to near-zero regret. Both advantages' $95\%$
bootstrap CIs exclude zero; false-adapt $0.016\le\alpha{=}0.10$.}
\label{tab:mixedstream}
\small
\begin{tabular}{lcc}
\toprule
Policy & Regret $\downarrow$ & \textsc{KGA} advantage (95\% CI) \\
\midrule
Always-adapt  & $0.0566$ & $+0.0545\;[0.042,\,0.067]$ \\
Always-freeze & $0.0304$ & $+0.0283\;[0.020,\,0.037]$ \\
\textsc{KGA}  & $\mathbf{0.0021}$ & --- (reference) \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:mixedstream} reports the outcome. Always-adapt pays on the
freeze-favorable majority ($0.057$) and always-freeze pays on the adapt-favorable
Camelyon17 cells ($0.030$), whereas \textsc{KGA} attains regret $0.0021$---about
$14\times$ below freeze and $27\times$ below adapt. Both gaps are statistically
robust: $+0.0283\,[0.020,0.037]$ versus freeze and $+0.0545\,[0.042,0.067]$
versus adapt. This is the regime the certificate is built for: \textbf{when
adaptation helps on some conditions and hurts on others, no fixed policy is safe,
and \textsc{KGA} dominates both simultaneously with statistical significance.}

\paragraph{Scope (honest).} This is a \emph{constructed} heterogeneous
deployment---a pool of three real benchmarks' test conditions, not a single new
dataset---chosen to expose the two-sided regime that one-sided benchmarks cannot.
It uses the real per-condition $(a_0,a_a)$ from the locked runs with no
fabrication and per-domain label-free calibration. It demonstrates the
\emph{mechanism} (per-condition routing beats any global choice under
heterogeneity); it is not a claim of new state-of-the-art on a marquee benchmark.
Reproduce with \texttt{docs/research/kbound/scripts/mixed\_stream\_kbound.py}.
\clearpage

\section{Excluded wins and regime boundaries}\label{sec:excluded-wins}
Several benchmarks flag beats-both under alternate routes or adapters but do \emph{not} qualify
as headline natural-shift wins under the Protocol~G/H/M v2 bar. \textbf{Superseded protocols:}
iWildCam Protocol~H v1 (fixed \texttt{sar\_online} without dev-lock) and OfficeHome Protocol~M v1
($14$-candidate win-finder) are audit-only; headline claims use H/M v2
(\texttt{research\_lock/IWILDCAM\_PROTOCOL\_H\_v2.yaml},
\texttt{research\_lock/OFFICEHOME\_PROTOCOL\_M\_v2.yaml}).
\textbf{Office-Home (deployed adapter):} mild online EATA is helpful-dominated on target test
($0\%$ harmful); Table~\ref{tab:officehome} is a covariate-shift diagnostic, distinct from
Protocol~M v2. \textbf{ImageNet-R:} weak detectability (harm-AUC $0.66$); mandatory abstention.
\textbf{iWildCam id\_val:} multicandidate routes can flag wins but pooled false-adapt $\approx 78\%\gg\alpha$.
\textbf{Camelyon four-seed slice:} helpful-dominated (Table~\ref{tab:camelyon}); distinct from
Protocol~G mixed-stress grid. \textbf{CIFAR-10.1:} within-seed safety holds; cross-seed certificate
transfer does not clear the headline bar.
\textbf{WILDS FMoW (Protocol~L):} geographic region shift on OOD test
($n{=}180$ held-out cells; dev $\{0,1\}$ / test $\{2,3,4\}$; fixed online SAR;
canonical GBR+global $\varepsilon$): false-adapt $37.5\%\gg\alpha$; regret
$0.013$ vs always-adapt $0.009$ vs always-freeze $0.013$; does \emph{not} beat both
(Table~\ref{tab:fmow-L}). In-distribution dev calibration ($\mathrm{FA}{=}0$) did not
transfer to OOD val/test.
\textbf{WILDS PovertyMap (Protocol~L):} country shift (fold~A, binned-wealth proxy);
dev screen failed (harm-AUC $0.637{<}0.65$); held-out val/test not run per
pre-registration.

\begin{table}[H]\centering\small
\caption{WILDS FMoW Protocol~L, OOD test ($n{=}180$ held-out cells).}
\label{tab:fmow-L}
\begin{tabular}{lccc}
\toprule
policy & regret$\downarrow$ & false-adapt & beats both? \\
\midrule
always-adapt  & $0.0092$ & --- & \\
always-freeze & $0.0129$ & --- & \\
\textbf{K-Bound} & $0.0129$ & $37.5\%$ & no \\
\bottomrule
\end{tabular}
\end{table}
\clearpage

\section{Limitations and honest scope}\label{sec:limits}
\begin{center}\fbox{\parbox{0.95\columnwidth}{\small
\textbf{What we do not claim.} Universal accuracy gains; adapter selection by test regret;
multicandidate routes with violated false-adapt bounds; or wins on helpful-dominated shifts
where always-adapt is already oracle-optimal.}}
\end{center}

\medskip
(1) Scope of the empirical win: on the CIFAR-10-C \emph{stress} grid \textsc{KGA} beats both
trivial policies for Tent and EATA at $0\%$ false-adapt (Table~\ref{tab:decisive}) and ties
the collapse-resistant SAR; on ImageNet-C (Table~\ref{tab:imagenetc}) the pattern \emph{flips}---
Tent and EATA are robust so \textsc{KGA} ties always-adapt, while SAR collapses and \textsc{KGA}
beats both---a win we confirmed survives a \emph{mechanism-faithful} SAR re-implementation
(Table~\ref{tab:imagenetc-faithful}; SAR harmful on $44\%$ of cells, regret-to-oracle $0.023$ vs
$0.112$/$0.027$), so it is not an implementation artifact, modulo SAR's official gentler-LR,
\texttt{layer4}-frozen schedule, which we have not yet run. On \emph{per-corruption} CIFAR-10-C and within a single online stream (both
helpful-dominated) it again \emph{ties} the better fixed policy. The common rule across all of
these: \textsc{KGA} never underperforms the better trivial policy, and \emph{strictly} wins only
where catastrophic, detectable harm is actually present---and which method that is depends on the
benchmark. A tighter online (streaming) certificate is future work.
(2) \textbf{Headline natural-shift wins} are Protocols~G, H v2, and~M v2
(Tables~\ref{tab:camelyon17-G}, \ref{tab:iwildcam-H}, \ref{tab:officehome-M}).
Earlier Camelyon diagnostics (\S\ref{sec:natural-suite}) and multicandidate routes are not
competing headline claims. \textbf{ImageNet-C} harmful SAR win is synthetic corruption.
\textbf{CIFAR-10.1} is a within-seed low-margin probe; cross-seed transfer fails
(\S\ref{sec:excluded-wins}). \textbf{ImageNet-R} remains a split-specific lead. The deployed
\textbf{Office-Home} adapter audit (Table~\ref{tab:officehome}) is an invalid-transfer regime,
distinct from Protocol~M v2.
\textbf{WILDS FMoW} and \textbf{PovertyMap} (Protocol~L) are
pre-registered honest nulls: certificate calibration fails to transfer on geoshift
(Table~\ref{tab:fmow-L}).
(3) The conformal radius assumes task exchangeability.
(4) The sign-of-difference characterization is proved for binary, multiclass, and
regression (Lemma~\ref{lem:reduction}); the unconditional label-free \emph{bracketing}
problem is resolved \emph{negatively} by Theorem~\ref{thm:conj1-dichotomy}: no
evidence-definable assumption class can identify the benefit sign. On the margin-monotone
relative-calibration class the one-bit supplement certifies the sign \emph{unconditionally}, and
that class is \emph{a} weakest such falsifiable class under a general-position assumption
(Theorem~\ref{thm:cmono-weakest}); the \emph{unconditional} weakest class remains open
(Conjecture~\ref{conj:gen}, Remark~\ref{rmk:genpos}).
(5) \textsc{KGA}'s value scales with how often catastrophic, detectable harm
occurs---precisely the quantity the theory characterizes; in benign domains
always-adapt is strong and the certificate is mainly safety insurance.
\clearpage

\section{Forward work: Target-Label-Light Multimodal Safety Guard}\label{sec:forward-tll}
The limitations above are not dead ends; they define \emph{where} a deployment guard is needed and
\emph{why} pure label-free cannot suffice. The recommended forward blueprint---documented in the
supplement and pre-registered before evaluation---combines two levers:
\textbf{(i)~a micro-probe} of $k{\approx}8$--$64$ held-out target labels that removes the
calibration-drift bias dominating real-shift certifiability; and \textbf{(ii)~a multimodal fusion
router} where modality failure is structurally detectable. This is a \emph{target-label-light}
operating point, distinct from the label-free core.

\paragraph{Bias-limited certifiability (motivation).}
The Camelyon17 bias--variance diagnostic decomposes the conformal radius into estimator model error
versus binomial measurement noise: observed $\varepsilon_{256}{=}0.112$ against a measurement floor
$\varepsilon_{\mathrm{meas}}(256){=}0.044$ (ratio $2.55{\times}$), with only $\approx\!16\%$ of
residual variance attributable to measurement noise
in the archived bias--variance diagnostic. The radius is thus
\emph{bias-limited}: dominated by $\widehat{B}(Z)$ calibration-drift error that does \emph{not}
shrink with eval $n$. More unlabeled data cannot close the certificate; reducing bias is the lever.

\begin{proposition}[Target-label-light escape]\label{prop:tll-escape}
Prop.~\ref{thm:imp-quant} applies to measurable maps of label-free evidence $Z$ alone. A certificate
that observes a fixed-size target-labeled probe $P$ with $|P|{=}k{\ge}1$, held out from the unlabeled
eval stream and from $\widehat{B}(Z)$ fitting, is not subject to that impossibility: the probe
provides direct information about the benefit $B$ on the disagreement region, removing the
calibration-drift bias $\gamma$ that caps label-free certifiability. The residual uncertainty is
$O(1/\sqrt{k})$ measurement variance via the empirical-Bernstein radius $\varepsilon_{\mathrm{probe}}(k)$,
not irreducible model bias.
\end{proposition}

\paragraph{Probe certificate.}
On probe set $P$ of size $k$,
$\widehat{B}_{\mathrm{probe}}{=}\frac{1}{k}\sum_{i\in P} X_i$ where $X_i$ is the per-sample paired
benefit (loss difference or placement benefit for AUROC routing). At fixed $\alpha{=}0.10$,
$\mathrm{LB}{=}\widehat{B}_{\mathrm{probe}}{-}\varepsilon_{\mathrm{probe}}(k)$ and
$\mathrm{UB}{=}\widehat{B}_{\mathrm{probe}}{+}\varepsilon_{\mathrm{probe}}(k)$; adapt if
$\mathrm{LB}{>}0$, freeze if $\mathrm{UB}{<}0$, else abstain. Honest limit: probe labels must be
held-out and pre-registered; tuning $k$ on test forfeits external-validation role.

\paragraph{Multimodal router (preliminary evidence).}
Across seven real cached multimodal/multi-detector anomaly tracks
(Table~\ref{tab:multimodal}), \textsc{KGA} routes deploy-fused vs.\ the best validation-chosen
single modality. All three actions occur: \textsc{adapt} (Real-IAD-NatDeg $\hat\Delta{=}{+}0.110$,
healthcare ${+}0.180$), \textsc{freeze} (Real-IAD-D3 $\hat\Delta{=}{-}0.124$), and \textsc{abstain}
(3D-ADAM, MulSen-AD, MVTec-3D, Real-IAD-2det). Implementation:
the guard composes ELARA-U drift routing with the same \textsc{KGA} probe certificate.

\paragraph{Probe k-sweep (preliminary, simulation + cache validation).}
Protocol D24 sweeps $k\in\{0,8,16,32,64\}$ on Real-IAD caches and logged mixed-shift panels
; pooled track-level decisions are in
Table~\ref{tab:probe-k}. At $k{=}0$ (full-pool label-free) Real-IAD-D3 certifies \textsc{freeze} and
NatDeg certifies \textsc{adapt}, matching Table~\ref{tab:multimodal}; small-$k$ subsampled probes can
abstain due to empirical-Bernstein range inflation at low $n$---motivating stratified probe draws or
larger $k$ (Protocol D24 follow-up). ImageNet-C/SAR at $k{=}0$ commits on $83\%$ of cells at
$0\%$ false-adapt. iWildCam (Table~\ref{tab:iwildcam}) remains abstain-only label-free. Results:
. \emph{Not part of validated
claims until held-out real-label scoring completes.}

\begin{table}[H]\centering\footnotesize
\caption{Target-label-light probe k-sweep (Protocol D24, preliminary). Pooled track-level
$\hat\Delta$, certificate decision, and $\varepsilon$ at $\alpha{=}0.10$. Rows marked
\textsc{tbd} await held-out real-label probe scoring.}
\label{tab:probe-k}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Benchmark & $k$ & $\hat\Delta$ & $\varepsilon$ & Decision & Status \\
\midrule
Real-IAD-D3  & $0$  & $-0.124$ & $0.034$ & \textsc{freeze} & pooled \\
Real-IAD-D3  & $32$ & $-0.090$ & $0.465$ & \textsc{abstain} & pooled \\
Real-IAD-NatDeg & $0$ & $+0.110$ & $0.037$ & \textsc{adapt} & pooled \\
Real-IAD-NatDeg & $32$ & $+0.103$ & $0.655$ & \textsc{abstain} & pooled \\
ImageNet-C/SAR & $0$ & --- & $0.047$ & mixed & $83\%$ commit, FA$=0$ \\
ImageNet-C/SAR & $32$ & --- & $0.268$ & \textsc{abstain} & all cells \\
iWildCam (val) & $0$ & --- & $0.184$ & \textsc{abstain} & $8$ cells \\
iWildCam (val) & $32$ & --- & $0.221$ & \textsc{abstain} & $8$ cells \\
Camelyon17 comp. & $0$  & \textsc{tbd} & \textsc{tbd} & \textsc{tbd} & pending \\
Camelyon17 comp. & $64$ & \textsc{tbd} & \textsc{tbd} & \textsc{tbd} & pending \\
RxRx1 panel    & $0$  & \textsc{tbd} & \textsc{tbd} & \textsc{tbd} & pending \\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[H]\centering\footnotesize
\caption{iWildCam (WILDS v2.0, ResNet-50 ERM $f_0$, macro-F1). Fifth real-shift null:
mixed ($59\%$ harmful on val) but label-free detectability weak (harm-AUC $\approx\!0.62$
entropy; certificate harm-AUC $\approx\!0.43$). \textsc{KGA} abstains; beats-both is
\textbf{false}. Full val grid: $144$ conditions, $864$ records, seeds $0$--$1$.}
\label{tab:iwildcam}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
Policy & regret$\downarrow$ & false-adapt & coverage & beats-both \\
\midrule
always-adapt & $0.052$ & --- & $1.00$ & no \\
always-freeze & $0.023$ & --- & $1.00$ & no \\
\textsc{KGA} (label-free) & $0.025$ & --- & $0.00$ & no \\
per-condition oracle & $0.000$ & --- & $1.00$ & --- \\
\bottomrule
\end{tabular}}
\end{table}
\FloatBarrier

\paragraph{Preliminary progress on the two open problems (not part of the validated claims).}
Two ongoing extensions directly target the open problems above; we report them as honestly-scoped
preliminary evidence, not headline results.
\emph{(a) A target-label-light certificate.} The impossibility (Prop.~\ref{thm:imp-quant}) is for the
\emph{unconditional label-free} setting; a small target-labeled probe removes the calibration-drift
bias that caps real-shift certifiability. A \emph{simulation} (conservative independence model, $\alpha{=}0.10$) suggested a probe of
$k{\approx}8$--$64$ target labels could convert abstention into a certified beats-both. A subsequent
\emph{sealed real-label test} (Protocol~D25) over $85$ real multimodal categories --- with a genuine
label-free baseline plus prediction-powered debiasing from the probe --- does \emph{not} reproduce
this at $k{\le}64$: the debiasing is real, but the small-probe rectifier variance dominates, so the
certified commit rate \emph{falls} rather than rises. We therefore report the micro-probe as an
\emph{open} direction, not a validated win; the simulation is an upper bound under near-noiseless bias
removal. This regime is \emph{target-label-light}---a distinct operating point from the label-free
core.
\emph{(b) A multimodal panel---\textsc{KGA} over a reliability-fusion system.} Across seven real cached
multimodal/multi-detector anomaly tracks (Table~\ref{tab:multimodal}), the certificate decides whether
to deploy the reliability-fusion system's confidence-weighted fused detector versus the best \emph{validation}-chosen
single modality; every decision is the \emph{computed} certificate output (no track is selected to force
an action). All three actions occur: \textsc{KGA} \emph{adapts} where fusion certifiably helps
(Real-IAD-NatDeg $\hat\Delta{=}{+}0.110$, healthcare ${+}0.180$; both $\mathrm{LB}{>}0$), \emph{freezes}
where fusion certifiably hurts (Real-IAD-D3 $\hat\Delta{=}{-}0.124$, $\mathrm{UB}{<}0$), and
\emph{abstains} where the benefit brackets zero (3D-ADAM, MulSen-AD, MVTec-3D, Real-IAD-2det). System-R
fusion reaches $\ge0.93$ AUROC on the complementary tracks (3D-ADAM $0.937$, MulSen-AD $0.934$), so
``$\ge90$ where modalities are complementary'' holds, with the certificate correctly declining to fuse
where it would not help. \emph{Honest scope:} anomaly-detection AUROC on cached scores; healthcare's
adapt partly rescues a poor validation modality-choice; Real-IAD-2det is multi-\emph{detector} (same
modality).

\FloatBarrier
\begin{table}[t]\centering\footnotesize
\caption{Multimodal extension (preliminary): the \textsc{KGA} certificate over System-R reliability fusion
on seven real cached anomaly tracks. \textsc{KGA} routes deploy-fused vs.\ the best validation-chosen
single modality; decisions are computed certificate outputs, not selected. AUROC; $\hat\Delta$ is the
fusion-vs-single benefit estimate at $\alpha{=}0.10$. $^\dagger$multi-detector (same modality), not multimodal.}
\label{tab:multimodal}
\begin{tabular}{@{}lccccl@{}}
\toprule
Track & $M$ & best & Sys-R & $\hat\Delta$ & \textsc{KGA} \\
\midrule
3D-ADAM                 & 2 & $0.917$ & $0.934$ & $+0.013$ & \textsc{abstain} \\
MulSen-AD               & 3 & $0.922$ & $0.934$ & $+0.012$ & \textsc{abstain} \\
MVTec-3D                & 2 & $0.872$ & $0.866$ & $-0.006$ & \textsc{abstain} \\
Real-IAD-2det$^\dagger$ & 2 & $0.869$ & $0.871$ & $+0.002$ & \textsc{abstain} \\
Real-IAD-D3             & 3 & $0.934$ & $0.811$ & $-0.124$ & \textsc{freeze} \\
Real-IAD-NatDeg         & 3 & $0.604$ & $0.711$ & $+0.110$ & \textsc{adapt} \\
healthcare              & 4 & $0.494$ & $0.674$ & $+0.180$ & \textsc{adapt} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Claim discipline (what we do and do not assert).}
\clearpage
\begin{center}\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{p{0.46\columnwidth}p{0.46\columnwidth}}
\toprule
\textbf{Claimed (supported)} & \textbf{Not claimed (out of scope)} \\
\midrule
TTA is reframed as a label-free adapt/freeze/abstain decision. & ``K-Bound solves TTA.'' \\
There is an information-theoretic limit to label-free decisions (Prop.~\ref{thm:imp-quant}). & ``K-Bound always beats adaptation.'' \\
Under matched evidence a valid certificate must abstain ($\ge1-2\alpha$, Cor.~\ref{cor:forced-abstain}). & ``K-Bound is universally optimal.'' \\
On our benchmarks KGA cuts harmful adaptation and regret-to-oracle and is robust across regimes. & A single-stream knockout over both trivial policies on every benchmark. \\
Multimodal fusion harm is structurally detectable; KGA routes adapt/freeze/abstain on cached tracks. & Live end-to-end fusion on every deployment domain. \\
Target-label-light probe ($k{\approx}8$--$64$) may convert abstain to beats-both in simulation
(Prop.~\ref{prop:tll-escape}); real-label validation pending (Table~\ref{tab:probe-k}). & Universal label-free knockout on natural shift; probe-free Camelyon17 win. \\
\bottomrule
\end{tabular}}
\end{center}

\section{Discussion: when to deploy K-Bound}\label{sec:discussion}
The theory and experiments converge on a deployment rule with two operating points.
\textbf{Label-free core:} K-Bound's value is governed by how often \emph{catastrophic, detectable}
harm occurs---safety insurance in benign regimes, real wins in collapse-prone synthetic grids
(Table~\ref{tab:decisive}). \textbf{Target-label-light multimodal guard (forward path,
\S\ref{sec:forward-tll}):} when deployment already has $k{\approx}8$--$64$ held-out target labels
(a one-time calibration draw) and the system is a multimodal fusion router, the micro-probe removes
calibration-drift bias and the certificate guards adapt (fuse), freeze (drop failed sensor), or
abstain. This is the recommended primary deployment path for autonomous and industrial multimodal AI;
the label-free core remains the minimax-safe fallback when no probe is available.
The trichotomy reads off accordingly. \textbf{(i)~Benign regimes} where adaptation
almost always helps (per-corruption CIFAR-10-C, clean anomaly suites): always-adapt is already
near-oracle, and K-Bound is \emph{safety insurance}---it matches always-adapt at a controlled
false-adapt rate, costing only coverage. \textbf{(ii)~Collapse-prone regimes}---small or
class-imbalanced batches, single-class/label-shift streams, aggressive updates, long
non-stationary runs: adaptation becomes a per-condition coin flip, and K-Bound delivers a real
win, cutting regret-to-oracle $\sim$$2.5$--$5.4\times$ versus always-adapt at $0\%$ false-adapt
on the CIFAR-10-C stress grid (Table~\ref{tab:decisive}). \textbf{(iii)~Self-protecting
candidates} (e.g.\ SAR's reset): the headroom shrinks, and K-Bound \emph{ties} rather than
beats---and never does worse. The practical takeaway: deploy K-Bound as a default guard around
any TTA candidate; its overhead is one forward pass, and its benefit scales with exactly the
risk it is designed to detect.
\clearpage

\section{Conclusion}
We reframed label-free adaptation as a decision constrained by identifiability: adapt
when benefit is certifiable, freeze when harm is certifiable, abstain when the
unlabeled evidence cannot tell. We proved the unknowable regime is fundamental (with a
witness), gave a false-adapt-controlled certificate under a stated alignment
assumption, and showed on Camelyon17 that real-shift certifiability is often
\emph{bias-limited}---more unlabeled data cannot shrink the radius when $\widehat{B}(Z)$
carries calibration-drift error. The forward path is a \textbf{target-label-light
multimodal safety guard}: a micro-probe of $k{\approx}8$--$64$ target labels removes that
bias (Prop.~\ref{prop:tll-escape}), and the same certificate guards multimodal fusion routers
where modality failure is structurally detectable (Table~\ref{tab:multimodal}). On real
anomaly-routing data the certificate abstains where benefit vanishes and recovers large
performance under failure. The CIFAR-10-C Protocol-A re-run supplies the missing
catastrophic-harm audit: with every per-condition array serialized across seeds $0$--$4$,
the \textsc{Tent}/\textsc{EATA} beats-both claim survives paired bootstrap and Holm
correction, while \textsc{SAR} correctly remains a tie when harmful cells are rare.
On natural shifts, Protocols~G, H v2, and~M v2 supply three held-out beats-both
replications under canonical \textsc{KGA} (Tables~\ref{tab:camelyon17-G},
\ref{tab:iwildcam-H}, \ref{tab:officehome-M}).

\section{Reproducibility}\label{sec:repro}
\textbf{Code.} All code, configs, and result artifacts are public at
an anonymized repository linked from the supplementary material (see its \texttt{LICENSE}); the
supplement includes the reproduction package and result manifests.

\noindent\textbf{One-command reproduction.} The CPU/numpy stages---theorem validators plus the
anomaly-routing, regression, witness, and ablation experiments---run from a single driver in the
release package. The deep-TTA stress grid runs on a single commodity GPU ($16$\,GB) using the
provided launcher, which builds the environment, fetches the data, and writes the tables and figures.
Natural-shift Protocols~H v2 and~M v2 (dev-lock held-out scoring) run via
\texttt{kbtrain.sh protocol-h-v2} and \texttt{kbtrain.sh protocol-m-v2}.

\noindent\textbf{Environment.} Experiments use a \texttt{uv}/\texttt{venv} environment, Python
3.12, with the deep-TTA runs pinned to \texttt{torch 2.5.1}/\texttt{torchvision 0.20.1}
(\textbf{PyTorch}~\cite{paszke2019pytorch}); the CPU-only stages need only
\texttt{numpy/scipy/scikit-learn/matplotlib} (\textbf{scikit-learn}~\cite{pedregosa2011sklearn}),
and \texttt{pdflatex}
compiles the paper. A \texttt{Dockerfile} builds the production inference API
(FastAPI/\texttt{uvicorn}, \texttt{python:3.11-slim} pinned by digest).

\noindent\textbf{Data.} CIFAR-10/100~\cite{krizhevsky2009learningmultiple},
ImageNet~\cite{deng2009imagenet}, and CIFAR-10.1~\cite{recht2019cifar10}
are pulled via \texttt{torchvision}; the official CIFAR-10-C
corruptions are the Hendrycks--Dietterich set, Zenodo DOI \texttt{10.5281/zenodo.2535967}
and are auto-downloaded and checksummed by the release scripts.

\noindent\textbf{Determinism and honesty.} Single-run stages use fixed seeds; the locked
deep-TTA stress grid reports seeds $0$--$4$ with a split-conformal radius at
$\alpha=0.1$. The deep-TTA grid is \emph{pre-registered} (declared before any result is
observed) and reported in full, with no condition dropped. Every theorem ships a numerical
sanity-check in the release package; all were re-run
from a clean environment and pass. Result manifests and locked analysis artifacts back every
reported number. Tables with multi-seed aggregates are marked explicitly in-caption
(e.g., Tables~\ref{tab:decisive}, \ref{tab:camelyon}, \ref{tab:rxrx1}); single-seed/scoping tables
are labeled as such (e.g., Tables~\ref{tab:cifar101}, \ref{tab:imagenetr}).

% Legacy enumerate reference list superseded by the keyed bibliography below
% (all entries merged/verified June 2026). Disabled to avoid a duplicate References section.
\iffalse
\begin{enumerate}\setlength{\itemsep}{1pt}
\item D.~Wang, E.~Shelhamer, S.~Liu, B.~Olshausen, T.~Darrell. Tent: Fully Test-Time Adaptation by Entropy Minimization. ICLR 2021.
\item J.~Liang, D.~Hu, J.~Feng. Do We Really Need to Access the Source Data? Source Hypothesis Transfer for Unsupervised Domain Adaptation (SHOT). ICML 2020.
\item Y.~Sun, X.~Wang, Z.~Liu, J.~Miller, A.~A.~Efros, M.~Hardt. Test-Time Training with Self-Supervision for Generalization under Distribution Shifts (TTT). ICML 2020.
\item S.~Niu, J.~Wu, Y.~Zhang, Y.~Chen, S.~Zheng, P.~Zhao, M.~Tan. Efficient Test-Time Model Adaptation without Forgetting (EATA). ICML 2022.
\item S.~Niu, J.~Wu, Y.~Zhang, Z.~Wen, Y.~Chen, P.~Zhao, M.~Tan. Towards Stable Test-Time Adaptation in Dynamic Wild World (SAR). ICLR 2023.
\item T.~Lee, S.~Chottananurak, T.~Gong, S.-J.~Lee. AETTA: Label-Free Accuracy Estimation for Test-Time Adaptation. CVPR 2024.
\item J.~Steinhardt, P.~Liang. Unsupervised Risk Estimation Using Only Conditional Independence Structure. NeurIPS 2016.
\item S.~Ben-David, J.~Blitzer, K.~Crammer, A.~Kulesza, F.~Pereira, J.~Wortman~Vaughan. A Theory of Learning from Different Domains. Machine Learning 79(1--2):151--175, 2010.
\item Z.~C.~Lipton, Y.-X.~Wang, A.~Smola. Detecting and Correcting for Label Shift with Black Box Predictors (BBSE). ICML 2018.
\item S.~Garg, S.~Balakrishnan, Z.~C.~Lipton, B.~Neyshabur, H.~Sedghi. Leveraging Unlabeled Data to Predict Out-of-Distribution Performance (ATC). ICLR 2022.
\item C.~Baek, Y.~Jiang, A.~Raghunathan, J.~Z.~Kolter. Agreement-on-the-Line: Predicting the Performance of Neural Networks under Distribution Shift. NeurIPS 2022.
\item Y.~Geifman, R.~El-Yaniv. SelectiveNet: A Deep Neural Network with an Integrated Reject Option. ICML 2019.
\item D.~Hendrycks, T.~Dietterich. Benchmarking Neural Network Robustness to Common Corruptions and Perturbations. ICLR 2019.
\item H.~Shimodaira. Improving Predictive Inference under Covariate Shift by Weighting the Log-Likelihood Function. Journal of Statistical Planning and Inference 90(2):227--244, 2000.
\item F.~Croce, M.~Andriushchenko, V.~Sehwag, E.~Debenedetti, N.~Flammarion, M.~Chiang, P.~Mittal, M.~Hein. RobustBench: A Standardized Adversarial Robustness Benchmark. NeurIPS 2021 (Datasets and Benchmarks Track).
\item I.~Gulrajani, D.~Lopez-Paz. In Search of Lost Domain Generalization (DomainBed). ICLR 2021.
\item V.~Vovk, A.~Gammerman, G.~Shafer. Algorithmic Learning in a Random World. Springer, 2005.
\item A.~N.~Angelopoulos, S.~Bates. Conformal Prediction: A Gentle Introduction. Foundations and Trends in Machine Learning 16(4):494--591, 2023.
\item G.~Shafer. Testing by Betting: A Strategy for Statistical and Scientific Communication. Journal of the Royal Statistical Society: Series A 184(2):407--431, 2021.
\item A.~Ramdas, P.~Gr\"unwald, V.~Vovk, G.~Shafer. Game-Theoretic Statistics and Safe Anytime-Valid Inference. Statistical Science 38(4):576--601, 2023.
\item A.~Maurer, M.~Pontil. Empirical Bernstein Bounds and Sample Variance Penalization. COLT 2009.
\item A.~B.~Tsybakov. Introduction to Nonparametric Estimation. Springer Series in Statistics, 2009.
\item P.~W.~Koh, S.~Sagawa, H.~Marklund, S.~M.~Xie, M.~Zhang, A.~Balsubramani, W.~Hu, M.~Yasunaga, et al. WILDS: A Benchmark of in-the-Wild Distribution Shifts. ICML 2021.
\item P.~B\'andi, O.~Geessink, Q.~Manson, M.~van Dijk, M.~Balkenhol, M.~Hermsen, et al. From Detection of Individual Metastases to Classification of Lymph Node Status at the Patient Level: The CAMELYON17 Challenge. IEEE Transactions on Medical Imaging 38(2):550--560, 2019.
\item A.~P.~Dawid, A.~M.~Skene. Maximum Likelihood Estimation of Observer Error-Rates Using the EM Algorithm. Journal of the Royal Statistical Society: Series C (Applied Statistics) 28(1):20--28, 1979.
\item A.~Anandkumar, R.~Ge, D.~Hsu, S.~M.~Kakade, M.~Telgarsky. Tensor Decompositions for Learning Latent Variable Models. Journal of Machine Learning Research 15:2773--2832, 2014.
\item S.~Ben-David, T.~Lu, T.~Luu, D.~P\'al. Impossibility Theorems for Domain Adaptation. AISTATS 2010.
\item Y.~Jiang, V.~Nagarajan, C.~Baek, J.~Z.~Kolter. Assessing Generalization of SGD via Disagreement (GDE). ICLR 2022.
\item E.~Rosenfeld, S.~Garg. (Almost) Provable Error Bounds Under Distribution Shift via Disagreement Discrepancy. NeurIPS 2023.
\item Y.~Lu, Z.~Wang, R.~Zhai, S.~Kolouri, J.~Campbell, K.~Sycara. Characterizing Out-of-Distribution Error via Optimal Transport (COT). NeurIPS 2023.
\item J.~P.~Miller, R.~Taori, A.~Raghunathan, S.~Sagawa, P.~W.~Koh, V.~Shankar, P.~Liang, Y.~Carmon, L.~Schmidt. Accuracy on the Line: On the Strong Correlation Between OOD and ID Generalization. ICML 2021.
\item E.~A.~Platanios, A.~Blum, T.~M.~Mitchell. Estimating Accuracy from Unlabeled Data. UAI 2014.
\item F.~Parisi, F.~Strino, B.~Nadler, Y.~Kluger. Ranking and Combining Multiple Predictors without Labeled Data. Proceedings of the National Academy of Sciences 111(4):1253--1258, 2014.
\item A.~Jaffe, B.~Nadler, Y.~Kluger. Estimating the Accuracies of Multiple Classifiers without Labeled Data. AISTATS 2015.
\item E.~Kim, M.~Sun, A.~Raghunathan, J.~Z.~Kolter. Reliable Test-Time Adaptation via Agreement-on-the-Line. ICLR 2024.
\item Bar et al. Protected Test-Time Adaptation via Online Entropy Matching: A Betting Approach (POEM). NeurIPS 2024. % TODO: verify author initials before submission
\item M.~Schirmer, M.~Jazbec, C.~A.~Naesseth, E.~Nalisnick. Monitoring Risks in Test-Time Adaptation. NeurIPS 2025 (arXiv:2507.08721).
\item A.~Podkopaev, A.~Ramdas. Tracking the Risk of a Deployed Model and Detecting Harmful Distribution Shifts. ICLR 2022.
\item T.~Bonald, R.~Combes. A Minimax Optimal Algorithm for Crowdsourcing. NeurIPS 2017.
\item S.~Ibrahim, X.~Fu, N.~Kargas, K.~Huang. Crowdsourcing via Pairwise Co-occurrences: Identifiability and Algorithms. NeurIPS 2019.
\item Y.~Zhang, X.~Chen, D.~Zhou, M.~I.~Jordan. Spectral Methods Meet EM: A Provably Optimal Algorithm for Crowdsourcing. Journal of Machine Learning Research 17(102):1--44, 2016.
\item A.~Jaffe, E.~Fetaya, B.~Nadler, T.~Jiang, Y.~Kluger. Unsupervised Ensemble Learning with Dependent Classifiers. AISTATS 2016.
\item Q.~Ma, A.~Olshevsky. Adversarial Crowdsourcing Through Robust Rank-One Matrix Completion. NeurIPS 2020.
\end{enumerate}
\fi

\appendix
\section{Companion manuscript: the demoted theorem stack}\label{app:stack}\label{app:theory-ext}\label{app:anytime}
Everything beyond the five theorems lives, with full proofs and per-result validators, in
the companion manuscript and archived technical report: the RGF reliability-gated-fusion
instantiation (T1--T9, GDR); multiclass and regression reductions; the sequential
anytime-valid e-process certificate and its $\beta$-drift variant (worst-case anytime
false-adapt $0.0316\le\alpha{=}0.05$ across all null streams, $20{,}000$ runs/stream over a
horizon of $2{,}000$; \texttt{val\_thm3\_evalue.py},
\texttt{results\_thm3\_evalue\_alpha005.json}); label-shift, bounded-
and smooth-drift boundaries unified by the ambiguity reach; router capacity; the
multi-candidate route with the $\gamma$-meter, the agreement-invisible reach, and the
corrected hypothesis H; the budget audit; and the agreement--accuracy (anchored AoL) law.
\begin{table}[t]\centering\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{ll}
\hline
Main-paper result & Demoted to manuscript (with validators) \\
\hline
Thm~\ref{thm:imp} & forced-abstention, minimax-floor variants \\
Thm~\ref{thm:cert} & anytime e-process; $\beta$-drift; router capacity \\
Thm~\ref{thm:frontier} & label-shift / drift boundaries; reach hierarchy \\
Thm~\ref{thm:conj1-dichotomy} & $\gamma$-meter; invisible reach; multiclass H \\
Thm~\ref{thm:ev-rate} & labeled-channel rates; AGL law and failures \\
\hline
\end{tabular}}
\caption{The five-theorem consolidation. Fifty-plus prior numbered results survive as
corollaries or manuscript sections; none was deleted, and every one keeps its
machine-checked validator.}
\label{tab:consolidation}
\end{table}
Table~\ref{tab:consolidation} gives the mapping. No result was deleted; each keeps its
machine-checked validator (Appendix~\ref{app:status}). The $\gamma$-meter route's CEI
diagnostic also has a first \emph{real-data} outcome: on the ImageNet-R track
(Table~\ref{tab:imagenetr}) it fires on all $48$ conditions
($\tau\in[1.07,2.15]\gg\tau^\star{=}0.52$) --- co-trained candidates violate conditional
error-independence exactly as predicted --- and the route abstains rather than
certifying falsely, which is the designed behaviour of a checkable guard.
\subsection{Validation figures for Theorems~\ref{thm:conj1-dichotomy} and~\ref{thm:ev-rate}}
\begin{figure*}[t]\centering
\includegraphics[width=0.49\linewidth]{theory_v2/fig_v1_flip_witness.png}\hfill
\includegraphics[width=0.49\linewidth]{theory_v2/fig_v2_identification_rate.png}
\caption{One-bit identification (Synthetic validator; Thm~\ref{thm:conj1-dichotomy}(ii)): the label complement
matches the full evidence law ($\TV=0$) while negating every advantage (left); recovery of
$b$ up to the bit at the $m^{-1/2}$ rate (right).}\label{fig:onebit}
\end{figure*}
\begin{figure}[t]\centering
\includegraphics[width=\linewidth]{theory_v2/fig_v3_audit.png}
\caption{The budget audit (Synthetic validator; Thm~\ref{thm:conj1-dichotomy}(iv)): sound at level $\alpha$,
power $1.000$ beyond the radius, correctly blind exactly on the bit-ambiguous set and the
rank-one-preserving stealth law.}\label{fig:audit}
\end{figure}
\begin{figure}[t]\centering
\includegraphics[width=\linewidth]{theory_v2/fig_v4_rate.png}
\caption{Evidence-channel rate (Synthetic validator; Thm~\ref{thm:ev-rate}): empirical minimax error tracks the
Le Cam curve $c_0/\sqrt m$; KL grows quadratically; the labeled-vs-evidence efficiency
ratio diverges as agreement approaches chance.}\label{fig:evrate}
\end{figure}
\begin{figure}[t]\centering
\includegraphics[width=\linewidth]{theory_v2/fig_conj1_closure.png}
\caption{Closing Conjecture~\ref{conj:gen} (Synthetic validator; Thm~\ref{thm:conj1-dichotomy}(iii)): the swap
flips every benefit sign while no label-free observable of any order moves (exact zero).}
\label{fig:conj1closure}
\end{figure}


\section{All 65 CIFAR-10-C cells (per-corruption $\times$ severity)}\label{app:c10c}
Table~\ref{tab:c10c-full} lists every cell behind the summary in Table~\ref{tab:c10c}
(\textsc{KGA} wraps Tent; EATA/SAR are the standalone candidates). The per-method means
reproduce Table~\ref{tab:c10c} exactly. Raw CSV:
.
\onecolumn
\input{paper/appendix_cifar10c_cells}
\twocolumn

\subsection{Experiment and GPU-run inventory}\label{app:inventory}
\begin{table*}[t]\centering\footnotesize
\caption{Experiment and GPU-run inventory (2026-06-17). \checkmark\,=\,complete with paper table;
$\sim$\,=\,partial/in-progress; ---\,=\,not started or optional breadth. One MPS job at a time.}
\label{tab:experiment-status}
\begin{tabular}{llcccl}
\toprule
Track & GPU? & Status & Table & Key outcome \\
\midrule
CIFAR-10-C stress grid (Tent/EATA/SAR, $5$ seeds) & MPS & \checkmark & \ref{tab:decisive} & beats-both Tent/EATA; $0\%$ FA \\
CIFAR-10-C per-corruption ($65$ cells) & MPS & \checkmark & \ref{tab:c10c} & helpful-dominated; safety insurance \\
CIFAR-10-C online stream & MPS & \checkmark & \ref{tab:online} & cross-regime win vs always-freeze \\
ImageNet-C noise (ResNet-50, SAR harmful) & MPS & \checkmark & \ref{tab:imagenetc} & committal heuristics false-adapt \\
ImageNet-C SAR faithful re-impl. & MPS & \checkmark & \ref{tab:imagenetc-faithful} & $44\%$ harmful; KGA $0\%$ FA \\
Camelyon17 WILDS (Protocol B, $n{=}1024$) & MPS & \checkmark & \ref{tab:camelyon} & bias-limited; fails $1/\sqrt{n}$ \\
Camelyon17 bias--variance diag. & CPU & \checkmark & \ref{tab:bias-diag} & $\varepsilon$ bias-limited ($2.55\times$ floor) \\
Camelyon17 rich-$Z$ (Protocol G) & MPS & \checkmark & \ref{tab:camelyon17-G} & held-out WILDS beats-both \\
ImageNet-R diverse panel & MPS & \checkmark & \ref{tab:imagenetr} & undetectable harm; abstain mandatory \\
RxRx1 ($9+$ model seeds) & MPS & \checkmark & \ref{tab:rxrx1} & near-one-sided; tie always-freeze \\
Office-Home deployed adapter audit & MPS & \checkmark & \ref{tab:officehome} & helpful-dominated null \\
Office-Home Protocol M v2 & CPU & \checkmark & \ref{tab:officehome-M} & dev-lock held-out beats-both \\
iWildCam Protocol H v2 & CPU & \checkmark & \ref{tab:iwildcam-H} & dev-lock held-out beats-both \\
CIFAR-10.1 low-margin probe & MPS & \checkmark & \ref{tab:cifar101} & abstain-heavy; $0\%$ FA within-seed \\
iWildCam WILDS (ERM $f_0$, val grid) & MPS & \checkmark & \ref{tab:iwildcam} & diagnostic null; weak transfer \\
WILDS FMoW geoshift (Protocol L) & MPS & \checkmark & \ref{tab:fmow-L} & OOD test null; FA $37.5\%$ \\
WILDS PovertyMap (Protocol L) & MPS & \checkmark & --- & dev screen STOP; no test run \\
Multimodal KGA (Real-IAD, etc.) & CPU & \checkmark & \ref{tab:multimodal} & adapt/freeze/abstain all occur \\
Target-label-light probe (D24) & CPU & $\sim$ & \ref{tab:probe-k} & pooled Real-IAD; Camelyon/RxRx1 \textsc{tbd} \\
CPU anomaly routing (ELARA-U) & CPU & \checkmark & \ref{tab:rel}--\ref{tab:mixed} & $11\times$ harmful regret \\
ImageNet-C blur/weather (breadth) & MPS & --- & \textsc{tbd} & optional; noise grid sufficient \\
SAR official schedule (Protocol E) & MPS & --- & \textsc{tbd} & residual check \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table}[t]\centering\footnotesize
\caption{Camelyon17 conformal-radius bias--variance decomposition (Protocol B diagnostic).
Residual dominated by $\widehat{B}(Z)$ model error, not binomial measurement noise.}
\label{tab:bias-diag}
\begin{tabular}{lcc}
\toprule
Quantity & $n_{\mathrm{eval}}{=}256$ & $n_{\mathrm{eval}}{=}1024$ \\
\midrule
observed $\varepsilon$ & $0.112$ & $0.029$--$0.038$ \\
measurement floor $\varepsilon_{\mathrm{meas}}$ & $0.044$ & $0.022$ \\
obs./floor ratio & $2.55\times$ & $\approx\!1.0\times$ \\
frac.\ variance = measurement & $\approx\!16\%$ & $\approx\!16\%$ \\
Protocol B $1/\sqrt{n}$ prediction & --- & \textbf{fails} \\
\bottomrule
\end{tabular}
\end{table}

\section{Proof and experiment status}\label{app:status}
Theorem~\ref{thm:imp} is proved with a constructed witness and strengthened to a quantitative Le
Cam minimax floor (part (ii)); Lemma~\ref{thm:gate} is a plug-in regret
decomposition (exact identity) with a minimax floor; Theorem~\ref{thm:cert} is proved
\emph{conditional} on a valid label-free interval estimator and complemented by an anytime-valid
e-process (manuscript, App.~B); Corollary~\ref{thm:pos} proves the covariate case and
Lemma~\ref{lem:reduction} the binary, multiclass, and regression
sign-of-difference results. The multiclass/regression label-free \emph{bracketing} is closed under
a bounded calibration-transfer assumption (manuscript, App.~C); the \emph{unconditional}
bracketing (Conjecture~\ref{conj:gen}) is resolved as a testability dichotomy
(Theorem~\ref{thm:conj1-dichotomy}: no evidence-definable assumption identifies the
sign; the minimal untestable supplement is one bit, which suffices under falsifiable
structure). Every theorem ships a
numerical sanity-check in the release package; all were re-run
from a clean environment and pass: the Le Cam floor tracks the closed form, the regret identity
holds to ${\sim}10^{-17}$, the multiclass identity to $10^{-16}$ with $100\%$ sign agreement over
$4000$ trials, calibration-transfer sign-recovery is $100\%$ when the confidence gap exceeds
$2\eta$, the router generalization gap decays as ${\sim}n^{-0.66}$, and the drift-corrected anytime
false-adapt rate stays $\le\alpha$. Where multi-seed runs are available, experiments report means
$\pm$ std with paired $t$-tests; single-seed/scoping tracks are labeled explicitly in captions and
discussed as regime-classification evidence. We also report evidence/$\alpha$/batch ablations, a
regression covariate-shift track, and a clean
non-identifiability witness.

\input{kbound_frontier_appendix}

% ===== References =====
% [verified June 2026] = identity confirmed against live arXiv/OpenReview this session.
% [verified June 2026] = standard reference, identity from memory — confirm ID before submission.
\section{The weakest one-bit class: conditional resolution of Conjecture~\ref{conj:gen}}\label{app:weakest}
\input{paper/sections/weakest_class}

\input{paper/references_kbound_expanded}

\end{document}
