# K-Bound paper — core deps (CPU experiments + paper figures)
numpy
scipy
scikit-learn
matplotlib
# Optional, only for the GPU CIFAR-10 + Tent experiment (experiments/cifar_tent_mps.py):
#   torch
#   torchvision
# LaTeX (pdflatex) is required to compile the paper; install via your TeX distribution.
