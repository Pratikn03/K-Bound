"""Emit paper tables from results/. (Currently tables are inline in paper/kbound.tex;
this stub centralizes regeneration as sections are split out.)"""
print("Tables are presently authored inline in paper/kbound.tex. "
      "Use manifests/claim_result_map.csv as the source of truth when externalizing them.")
