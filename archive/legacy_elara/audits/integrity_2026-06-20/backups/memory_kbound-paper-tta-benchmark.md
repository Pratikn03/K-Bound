---
name: kbound-paper-tta-benchmark
description: "Pratik's K-Bound paper — safe test-time adaptation benchmark on ImageNet-C; run/results locations and key context"
metadata: 
  node_type: memory
  type: project
  originSessionId: fb5346af-9b66-40e0-a6a6-307d5a965354
---

Pratik (ML researcher) is writing the K-Bound paper: a safety method for test-time adaptation (TTA) that decides ADAPT/FREEZE/ABSTAIN to avoid harmful adaptation. Benchmark: frozen pretrained ResNet-50 (V2 weights) adapted by tent/eata/sar on ImageNet-C noise corruptions; 90 conditions (3 corruptions × severities 1–5 × 3 batch regimes × 2 aggressiveness), 4k images each.

Key locations: data and project live on his external SSD "T9" (mounted /Volumes/T9), under a `uav/` folder (includes AutoML_Flagship_V8). Runs launch via `kbtrain.sh` (e.g. `kbtrain.sh noise-full`) in VS Code's integrated terminal, wrapped in caffeinate, MPS/Metal backend. Results write only at run end to `experiments/kbound/results/<run_name>/` as `decisive_tta_results.json` + `decisive_tta_table.md` — silent terminal mid-run is normal. Full noise run launched 2026-06-07 2:12 PM, ~6h duration.

Smoke-run finding (2026-06-07): always-adapt is net harmful (−5.7 to −7.6 pts vs frozen 0.406; 50–62% harmful conditions, worst in bs=8 tiny-batch); K-Bound made zero false adapts and matched always-freeze/oracle. Open question for full run: does K-Bound beat both trivial policies on mixed streams. Other papers on T9: orius/gridpulse, ELARA, sentifargo.

He phrases things tersely (e.g. "training on T9" = the run whose data is on the T9 drive). His Claude desktop approval dialogs often time out — he's usually remote via Dispatch, so prefer desktop-commander/host processes over screen access.

SAR artifact finding (2026-06-09): the completed `experiments/kbound/results/imagenetc_noise/` run (generated 2026-06-08) showed SAR harmful on 31% of cells (vs Tent 8%, EATA 3%) and was the ONLY reason KGA "beats both" trivial policies there. Diagnosed as an implementation artifact, not a real harmful-win: the repo's `sar_adapt` had (1) a fake SAM step — restore used a *different* gradient than the perturbation, injecting a spurious displacement every step (≈0.0137/step vs 0 for correct SAM), diverges at any LR; (2) broken recovery — restored BN weights but not optimizer momentum, so it re-corrupted next step; (3) EMA tracked the wrong quantity; (4) reset threshold mis-scaled (0.2·ln K ≈1.38 instead of absolute 0.2). The `0.4·ln K` reliable-sample filter was the only correct piece. A second analysis pass also argued SAR was being run at Tent's hot LR (1e-3/2.5e-3) vs official ImageNet 2.5e-4 and adapting layer4 (official freezes it) — left as an open methodology choice. Faithful fix applied on git branch `fix/faithful-sar`, in the `docs/research/kbound/scripts/cifar_tent_mps_v2.py` copy (NOTE: two copies of the harness exist; that docs/research one is the ACTUAL runner — the `src/scripts/kbound/` copy still has the buggy SAR). Backup `cifar_tent_mps_v2.py.bak_presar_20260609`; CPU validation `test_sar_faithful.py`. Couldn't git-commit from sandbox (stale `.git/index.lock`); edits on disk only, tree already dirty with unrelated paper edits. Re-run REQUIRES a fresh `--out-results` dir (existing checkpoint.json resumes and skips the buggy SAR cells) and a GPU/MPS Mac (~8h). The bad SAR number is NOT yet in the PDF (Table 9 SAR/EATA rows = "(pending)"; CIFAR-10-C headline Table 7 already says KGA "ties the collapse-resistant SAR"). Do NOT promote the current SAR ImageNet-C number into Table 9 until the faithful re-run confirms it — expectation is the harmful-win over SAR vanishes, leaving an honest "KGA does no harm" tie.

Faithful-SAR re-run LAUNCHED 2026-06-09 on host MPS (~8h) via desktop-commander: `caffeinate -is python docs/research/kbound/scripts/cifar_tent_mps_v2.py --benchmarks imagenetc --imagenetc-root experiments/kbound/data/imagenet-c --corruptions gaussian_noise shot_noise impulse_noise --arch resnet50 --methods tent eata sar --out-results experiments/kbound/results/imagenetc_noise_sarfix` (worker PID 80329 at launch; matched-config fixed SAR, no LR change / no layer4 freeze; old `imagenetc_noise/` kept untouched for comparison). 36 cells. Monitor via `experiments/kbound/results/imagenetc_noise_sarfix/progress.log` + `checkpoint.json`; final tables `decisive_tta_table.md` write only at run end. Note: real ImageNet-C data lives on T9 at `experiments/kbound/data/imagenet-c`; host torch env is `~/.venv_wilds` (torch 2.5.1, MPS). Smoke test showed the FIXED SAR still collapsed on a tiny 16-image aggressive cell (dB −0.5) — too small to interpret; the full 4000/cell run decides whether any SAR harmful-win survives. When it hits 36/36, compare `imagenetc_noise_sarfix/decisive_tta_table.md` vs `imagenetc_noise/decisive_tta_table.md`.

v0.5 PROGRESS (2026-06-09):
PART 1 THEORY — COMPLETE. 1A `thm:multicand-residual` (multi-candidate sign-ID under bounded error correlation; relaxes Def 5; val_multicandidate_residual.py 9/9 PASS); 1B `thm:smooth-drift` (makes drift radius observable via B=Ld; val_smooth_drift.py 7/7); 1C `prop:reach-table-v05` (unifies 1A τ-route + 1B drift-route as one reach ρ; val_reach_unification.py 5/5). Fragments in docs/research/kbound/paper/sections/{multicandidate_residual,smooth_drift,reach_unification_v05}.tex; validators+JSON+figs in experiments/kbound/theory_validation/. kbound.tex WIRED 2026-06-09: 3 `\input` lines added to docs/research/kbound/kbound.tex appendix (app:theory-ext) — smooth_drift after regression_conjecture (1B), reach_unification_v05 after unification_reach (1C), multicandidate_residual after multicandidate (1A); timestamped backup kbound.tex.bak_preinput_*; pdflatex single-pass compiles exit 0, all 3 fragments load, 0 errors (only a PRE-EXISTING multiply-defined label thm:frontier, NOT from the new sections — the 8 new labels are all unique). NOTE kbound_submission.tex inlines its theory (no \input anchors) so it is NOT wired there. Full latexmk PDF build DONE 2026-06-09: kbound.pdf = 44 pages, compiles clean (cross-refs resolved on full build); a copy was delivered to Pratik. His "Prop 17/18" = Prop 19/20 in current build. Honest scope: relaxes Def 5 + known-drift-radius into observable moduli; does NOT close Conjecture 1; 1A tangential blind spot proves agreement-only stats can't.
PART 2 — STARTED (Camelyon17 debug only). SAR sweep was PAUSED at only 4/36 cells for the GPU handoff. Camelyon17 debug run LIVE on MPS: experiments/kbound/wilds/run_camelyon17_kbound.py → results experiments/kbound/results/wilds_kbound_debug_mps/ (PID 86973 at launch; 72 conditions = 4 seeds×3 domains×3 comps×small×mild+aggressive; τ*=0.52; ETA ~2–4.5h). KGA variant (c) smooth-drift wired only as a DIAGNOSTIC Brier-view surrogate (g_S≈f0 conservative; faithful classification g_S = future work). When done: read results/wilds_kbound_debug_mps/result_*.json → kbound_summary.classification + γ_S/γ_T + real τ. Smoke hinted mildly helpful-dominated but only mild cells ran; detectability strong (label-free harm-AUC 0.913 via marginal_KL).
CAMELYON17 RESULT — DONE 2026-06-09 (result_73add410.json, 72/72): classification = **mixed+detectable**; base-rate-harmful (B<0) = 28.7%; mean_B +0.039; detectability=detectable (best single-feature harm-AUC 0.78). BUT KGA ABSTAINED on ~all cells — real-data multicandidate τ mean 0.89 >> τ*=0.52, and smooth-drift assumption violated (|γT−γS|≈0.105 > δ=0.05, gamma_gap_within_delta=false). γS/γT are debug-scale proxies (not paper-final defs). So the Gap-B harmful+detectable regime EXISTS on natural shift (Camelyon17), but KGA's routing didn't catch it (abstained) → NOT a banked win until τ* is recalibrated on the stored per-condition τ. (Corrects the mid-run "helpful-dominated" preview, which was only the first ~45 easy cells.) GPU now FREE. SAR still paused at 4/36 (resume cmd above).
DATASETS (2026-06-09): downloaded to T9 experiments/kbound/data/ — rxrx1_v1.0 (33GB), imagenet-r (8.5GB), iwildcam_v2.0 (~12GB compressed, extracting). Redundant T9 camelyon17_v1.0 copy DELETED (~125GB freed; active run used internal ~/kbound_cam; T9 ~161GB free, was 96% full). rxrx1 & iwildcam need (a) an f0 ResNet-50 trained ourselves (wilds ships data only) and (b) a one-line numpy-writability patch to wilds 2.0.0 RxRx1Dataset/IWildCamDataset before their TTA sweeps. T9 is exFAT (128KB block) → many-small-file datasets bloat hugely on disk. Other (non-K-Bound) projects dominate T9: gridpulse ≥620GB, gridpulse quarantine 156GB. Full audit: experiments/kbound/wilds/T9_AUDIT.md.
PART 2 cont. (2026-06-09 eve): ImageNet-R FULL sweep RUNNING (PID 92943; results imagenetr_kbound_full_mps/; 72 conditions = 4 seeds×3 comps×3 batch-regimes×mild+aggr, n_eval=600; f0=ResNet-50 IMAGENET1K_V2 masked to 200 ImageNet-R classes; reads internal ~/kbound_inr; per-condition τ stored; τ*=0.52). COMPUTE-bound (not I/O), ETA ~10–15h overnight. Runner = experiments/kbound/wilds/run_imagenetr_kbound.py (multi-class; reuses tta_methods/analysis/aggregation; prob_mode=max-prob for route-c when >2 classes). RxRx1 f0 = GENUINE WILDS pretrained checkpoint, downloaded+verified (CodaLab bundle 0x7d33860545b64acca5047396d42c0ea0 → rxrx1_seed:0_epoch:best_model.pth → resnet50(num_classes=1139), 0 missing/unexpected keys; matches published id-test 35.9% / OOD 29.7%); stored ~/kbound_rxrx1_ckpt/; NO from-scratch training needed; RxRx1 TTA sweep QUEUED behind ImageNet-R (one MPS GPU at a time). iWildCam extraction still grinding (exFAT, 203k+ files, ~1h45m+, archive not yet cleaned). wilds 2.0.0 RxRx1/IWildCam loaders patched (.values→.values.copy()); ImageNet-R/iWildCam loaders must filter exFAT ._ AppleDouble files.
ImageNet-R SAGA + RESOLUTION (2026-06-09 late): full config (n_eval=600, episodic 50 steps) is MPS-INTRACTABLE (~multiple days); also got fully stalled when iWildCam extraction ate the 16GB unified memory (→~121MB free). FIX (confirmed working): paused iWildCam (kept archive.tar.gz, re-extract later), RECONFIGURED to n_eval=256 / episodic_steps=5 / episodic_batch=64 (full 72-cond grid + all 6 candidates + 3 routes preserved; episodic-step cap arguably more faithful), REORDERED grid light-cells-first (small/tiny before large_iid). CONFIRMED: cell [1/72] completed 128s, _partial.json written, process state S/healthy. Run now at PID-of-the-day; outputs internal ~/kbound_inr_results/imagenetr_kbound_full_mps/ (rsync to repo results/ at end). ETA ~12–14h overnight (48 light cells ~first 3h; 24 large_iid bs200-online cells ~10h tail). First cell: a0=0.415, best-aa=0.440 (+2.5%), KGA abstain (τ=1.67). Optional lever to cut tail: cap large_iid online batch 200→96. KEY LESSON: on this 16GB M-series MPS, "process alive + state U" is NOT proof of progress (MPS compute also shows state U) — only a COMPLETED condition counts; verify via _partial.json/[N/72] log lines, never process-liveness. iWildCam currently PAUSED (resume from preserved archive after ImageNet-R+RxRx1). RxRx1 f0 verified+ready.
IMAGENET-R ACTUALLY DONE — CORRECTION (2026-06-09 late): the COMPLETED run is `~/kbound_inr_results/imagenetr_kbound_light_mps_internal/` (manifest result_f4a1293b.json), NOT the `..._full_mps` one (which kept OOM-dying/got reset to 0 — I wrongly reported "died at 4/72" by checking that wrong dir; Pratik correctly knew it finished 48 cells). The "light" config = DROPPED the heavy large_iid batch-regime (kept small+tiny) → that's why it completed; n_eval=500, episodic_steps=5, episodic_batch=32, 4 seeds×2 batch×3 comp×2 aggr = 48 cells. RESULT: classification = **mixed+UNDETECTABLE**; base-rate-harmful 13.9%; mean_B +0.017; detectability WEAK (best single-feature harm-AUC 0.66); KGA abstains (real τ_mean 1.69). = the "honest-limit / abstain" regime — complements Camelyon17's mixed+DETECTABLE (AUC 0.78). LESSON: there are several imagenetr_* result dirs — verify by manifest + cell-count across ALL of them, never trust one run-name. ImageNet-C/SAR run (cifar_tent_mps_v2 → imagenetc_noise_sarfix, --cooldown 3) was intentionally (re)started by Pratik thinking ImageNet-R was done — it WAS; SAR now at 9/36, progressing solo (~20min/cell, machine swap-heavy).
SAR ImageNet-C FAITHFUL RE-RUN — DONE 2026-06-10, 36/36 (imagenetc_noise_sarfix/decisive_tta_table.md). RESULT: the harmful-win is REAL — it SURVIVED the faithful fix, so it is NOT an implementation artifact (OVERTURNS the earlier expectation that it would vanish). Faithful SAR harmful on 44% of cells (Tent 6%, EATA 3%); regret-to-oracle: always-adapt-SAR 0.1124, always-freeze 0.0267, K-Bound 0.0229 → K-Bound BEATS BOTH on the SAR stream (p*=0.2, alpha=0.1). On Tent/EATA K-Bound only ties always-adapt (insurance / does-no-harm). So SAR is a genuine "beats both" harmful-win — the empirical centerpiece alongside Camelyon17 (mixed+detectable). CAVEAT: run at the MATCHED/shared LR (apples-to-apples, mechanism-faithful SAR); SAR's OFFICIAL gentler LR 2.5e-4 + frozen-layer4 variant was NOT run — the one remaining reviewer-check. Remaining battery: RxRx1 (f0 verified ready) → iWildCam (extraction paused, archive saved) → OfficeHome (optional).
RxRx1 SWEEP LAUNCHED + CONFIRMED WORKING (2026-06-11): runner experiments/kbound/wilds/run_rxrx1_kbound.py; results ~/kbound_rxrx1_results/rxrx1_kbound_light_mps_internal/. Light config = 48 cells (large_iid dropped; 4 seeds×{small,tiny}×{iid,imbalanced,single_class}×{mild,aggressive}; n_eval=256, episodic_steps=5, episodic_batch=64, tau*=0.52). f0 = official WILDS ERM ckpt (~/kbound_rxrx1_ckpt, no training; probe id 0.332/OOD 0.285). Data = OOD test split (34,432 imgs) copied to internal ~/kbound_rxrx1_data. SURVIVAL HARNESS (the recipe that makes long MPS sweeps viable on 16GB): per-cell del+gc.collect+torch.mps.empty_cache+synchronize; atomic _partial.json resume (append-only, skip-completed); auto-restart supervisor supervise_rxrx1.sh (relaunches --resume on non-.done exit). Verified 3/48, memory HOLDING (66% free — no OOM), ~185s/cell, ETA ~2.5h. Cell 2 = real harmful cell (a0 0.270→aa 0.258, B=-0.012, KGA ABSTAIN→freeze).
RxRx1 RESULT — DONE 48/48 (2026-06-11, result_f6b268c7.json): classification = HARMFUL-DOMINATED; base-rate-harmful 74%; mean_B -0.060; detectability DETECTABLE (best harm-AUC 0.76); real τ_mean 1.65 (KGA abstains via τ-route). Run on OOD test split (14 experiments, 34,432 imgs). STRONGEST natural-shift case so far: adaptation mostly HURTS and it's detectable → the regime where K-Bound's FREEZE matters most. FULL NATURAL-SHIFT BATTERY now done: RxRx1 harmful-dominated+detectable · ImageNet-C/SAR mixed + K-Bound BEATS BOTH · Camelyon17 mixed+detectable (KGA abstains, τ* recal pending) · ImageNet-R mixed+undetectable (honest-limit abstain). Only iWildCam left (paused, archive saved). Survival harness (light config dropping large_iid + per-cell mem hygiene + auto-restart supervisor) is the proven recipe for long MPS sweeps on the 16GB Mac — used for both ImageNet-R and RxRx1. PAPER PDF updated 2026-06-11 (kbound.tex → kbound_results-integrated_2026-06-11.pdf, 18pp; SAR beats-both table + natural-shift subsection added; NOTE page-count mismatch — current kbound.tex builds 16-18pp vs an older 44pp build / kbound_submission.pdf 38pp — reconcile canonical file).
SAR RESUME CMD (run after Camelyon17 frees MPS): `cd /Volumes/T9/uav/AutoML_Flagship_V8 && PYTORCH_ENABLE_MPS_FALLBACK=1 caffeinate -is ~/.venv_wilds/bin/python docs/research/kbound/scripts/cifar_tent_mps_v2.py --benchmarks imagenetc --imagenetc-root experiments/kbound/data/imagenet-c --corruptions gaussian_noise shot_noise impulse_noise --arch resnet50 --methods tent eata sar --out-results experiments/kbound/results/imagenetc_noise_sarfix` (resumes from checkpoint, re-runs only the in-progress cell).
REMAINING: rest of WILDS battery (RxRx1, iWildCam, ImageNet-R, OfficeHome, Camelyon17-at-scale) + Part 3 (paper §8.2, 3 candidate abstracts, limitations rewrite, 1-page memo). Host torch env `~/.venv_wilds`; 4 trained f0 DenseNet-121 ckpts at results/wilds/f0_seed{0..3}.pt.

Dashboard (2026-06-09): built a React+TS+Vite+Tailwind+Recharts "K-Bound Knowability Control Center" demo dashboard on Pratik's exact MOCK data (KPIs, policy comparison, knowability phase map, decision stacked bars, benchmark table, "why decide" CI card, α-sweep, + Drilldown/Stress/Logs/Reproducibility pages). Compiles clean (0 TS errors). Delivered as single-file kbound_dashboard.html + kbound-dashboard-src.zip. Standalone (NOT in T9 repo); every data file commented with the experiments/kbound/results/*.json plug-in point for swapping in real runs later.

RxRx1 9+ (Protocol C) COMPLETE 2026-06-14 — all 3 seeds (`rxrx1_protocol_c_9plus_modelseed{0,1,2}`, `..._ALL.done`, 360 conditions / 2160 records, n_eval=512, ran via supervise_rxrx1_9plus.sh + survival harness, ~1 auto-restart, no work lost). Pooled: base-rate-harmful 81.5% (1760/2160), mean_B −0.063, certificate harm-AUC ~0.78–0.85, frozen 0.283 / oracle 0.288 / SAR-online collapse 0.024 / best-adapter 0.276; KGA ≈0% ADAPT (0 false-adapts) / 42% freeze / 58% abstain. Classification = HARMFUL-DOMINATED + DETECTABLE (matches the light run, slightly more harmful at scale). It is the SAFETY case: KGA ties always-freeze, beats always-adapt ~45×, cuts realized adaptation harm ~81%→0% — NOT a beats-both win. Integrated into the paper 2026-06-14.

CURRENT K-Bound paper (2026-06-14): canonical body = `docs/research/kbound/kbound.tex` (20pp, rebuilds clean via host latexmk). Strongest delivered = `K-Bound_paper_with_frontier_REBUILT.pdf` (26pp = 20pp body + 6pp frontier-validation appendix). Theory CONSOLIDATED into a 5-theorem frontier stack (`paper/sections/main_theory_5.tex`: impossibility, ADAPT/FREEZE/ABSTAIN certificate, exact benefit-sign frontier = main result, one-bit dichotomy, label-free rate); the v0.5 1A/1B/1C theorems are FOLDED IN, not separately labeled (section files archived but deliberately NOT `\input`). Two decisive beats-both wins in-paper: CIFAR-10-C (KGA regret 0.0016 vs 0.009/0.12) and ImageNet-C/SAR (0.023 vs 0.112/0.027); 4 natural-shift tracks across helpful/harmful/unknowable, no overclaim. CAVEAT: the 6pp frontier appendix has NO `.tex` source in the repo (only PDF pages survive) → 26pp not single-source reproducible. Honest ceiling: strong workshop / mid-conference.

CAMELYON17 EXTERNAL BEATS-BOTH — REFUTED 2026-06-14 (honest CPU re-analysis of `wilds_kbound_debug_mps/result_73add410.json`). Under a legitimately frozen Camelyon-FREE τ*=0.52 (same global constant as corruption + RxRx1), KGA regret-to-oracle 0.076 = WORST vs always-freeze 0.071 vs always-adapt 0.005 → beats_both=FALSE. Reasons: (1) Camelyon17 is HELPFUL-leaning (mean_B +0.039, adapt mostly helps) so abstain-by-default is the wrong move there; (2) τ̂ scale is DATASET-SPECIFIC and does NOT transfer — Camelyon17 τ̂ mean 0.89, RxRx1 1.51, τ*=0.52 fires on only 3% of Camelyon / 0% of RxRx1 conditions → near-total abstention on both real shifts. ⚠️ INTEGRITY ISSUE FOUND: the `with_frontier` appendix's "recalibrated K-Bound beats both" on Camelyon17 was produced by TUNING the band ON Camelyon17 itself (coverage 22%→57%) = test-set tuning on the external-validation set = INVALID; must be PULLED/relabeled before submission. Net: genuine beats-both wins are SYNTHETIC-corruption only (CIFAR-10-C, ImageNet-C/SAR); natural shifts are a safety/abstention story, NOT beats-both. Real path to a real-shift win = a scale-invariant / pre-registered normalized τ̂ (new work, uncertain) — never recalibrate τ* on the external set.

CLEANUP + SCALE-INVARIANT τ̂ TEST — DONE 2026-06-14. (A) Invalid Camelyon17 claim REMOVED: clean paper = `docs/research/kbound/K-Bound_paper_CLEAN.pdf` (20pp body only, latexmk exit 0). The bad "recalibrated beats-both / Fig A4 / coverage 22%→57%" lived in the source-less 6pp appendix → dropped the appendix (its one valid bit, ATC/DoC-committal-fails, already in body); corrected 6 body passages to the honest verdict. Legit results (CIFAR-10-C, ImageNet-C/SAR, RxRx1) retained. CANONICAL HONEST VERSION (2026-06-14) = `docs/research/kbound/K-Bound_paper_with_frontier_REBUILT.pdf` (21pp): 20pp body + REBUILT reproducible frontier appendix `kbound_frontier_appendix.tex` (\input by kbound.tex). Appendix A.1 CIFAR-10-C beats-both (KGA 0.0016/0.0015), A.2 ImageNet-C/SAR committal-heuristics-fail-where-KGA-abstains (ATC false-adapt 1.00 vs KGA 0.00), A.3 honest Camelyon "limitation not a win" — all JSON-traceable tables. The INVALID Camelyon "recalibrated beats both/Fig A4/22%→57%" is REMOVED. (The original 14:39 `K-Bound_paper_with_frontier.pdf` 26pp had the over-claim + 5 source-less full-page figures — SUPERSEDED; `K-Bound_paper_CLEAN.pdf` 20pp = body-only interim, also superseded.) Frontier figures NOW EMBEDDED (2026-06-14): A.1 CIFAR-10-C beats-both + A.2 ImageNet-C/SAR committal-vs-KGA, generated by NEW reproducible CPU script `docs/research/kbound/scripts/make_appendix_frontier_figs.py` (PNG-only, asserts every plotted value == appendix table number, no JSON writes) → `figures/app_frontier_{cifar10c,imagenetc}.png`. NOTE: `scripts/knowability_frontier_validation.py` is CPU-only and makes DIFFERENT figures (123-task ELARA-archive theorem-validation + subsumption bars, fig_kfrontier/fig_subsumption), NOT the appendix frontiers — don't use it for A.1/A.2. The 21pp K-Bound_paper_with_frontier_REBUILT.pdf now has both frontier figures + tables; invalid Camelyon figure excluded; fully reproducible via `latexmk kbound.tex`.

CAMELYON PROTOCOL F run (`camelyon17_richZ_F_v1`, full-scale 5-seed × n_eval=1024 × domains test/val/id_val × rich panel — the source-logging run for the scale-invariant test) was launched by Pratik 2026-06-14 and STOPPED EARLY 2026-06-15 at 15/90 conditions. Reason: a read-only early-stop peek showed the DEPLOYED adapter (sar_online, source-best) is helpful 0/6 harmful on the completed test conditions (B +0.15..+0.19, mean +0.17, incl. the SAR-hostile single_class/aggressive cell), and KGA can't fire (τ̂ range 0.58-1.81, never < τ*=0.52) → structurally cannot beats-both (helpful-leaning + scale-mismatch). 90 partial records preserved at the dir. Do NOT relaunch expecting a real-shift win — the goldilocks regime (deployed adapter flips helpful↔harmful detectably) is absent on Camelyon; that's the documented honest finding. K-Bound paper deemed integrity-clean + submission-ready (workshop / mid-conference) as of 2026-06-15.

ACDC GOLDILOCKS-TEST PIPELINE BUILT + VALIDATED 2026-06-15 (the hunt for a REAL-shift beats-both). Goal: test whether Cityscapes→ACDC (real adverse-weather seg: fog/night/rain/snow) is a "goldilocks" shift where the deployed adapter flips helpful↔harmful detectably (night hypothesized = collapse regime, per continual-TTA literature). Code at `experiments/kbound/acdc/`: seg_models.py (f0 = nvidia/segformer-b0-finetuned-cityscapes-1024-1024, real), seg_tta_methods.py (faithful Tent/EATA/SAR ported to segmentation, MPS-safe batch=1 — SegFormer decode-head BN/conv backward fails on MPS at bs≥2, so adapt at bs=1), seg_certificate.py (B=ΔmIoU, entropy detectability, adapt/freeze/abstain, source-calibrated τ — calibrate on Cityscapes/source NEVER on ACDC test), run_acdc_kbound.py, ingest_acdc.py, acdc_data.py. VALIDATED end-to-end on a synthetic random-GT fixture (37s: discovery→model→deployed-adapter-by-source-mIoU→source-calibrated routing→per-condition B→goldilocks+beats-both→manifest; correctly returned goldilocks=NULL/beats_both=False on degenerate fixture = no false-positive). AWAITING DATA: user downloading ACDC (needs free login at acdc.vision.ee.ethz.ch — 2 files: rgb_anon_trainvaltest.zip ~16GB + gt_trainval.zip; eval on VAL split, test GT withheld). RUN: `python experiments/kbound/acdc/ingest_acdc.py --extract <dl_folder>` (→ experiments/kbound/data/acdc/) then `python experiments/kbound/acdc/run_acdc_kbound.py --device mps`. T9 had 133GB free so NO datasets deleted. Honest odds: best candidate but a real bet (continual-collapse evidence may not transfer to episodic adapt); report null if so.
ACDC DRY RUN DONE 2026-06-15: user downloaded rgb_anon_trainvaltest.zip (16GB) + gt_trainval.zip to ~/Downloads. Internal disk only ~16GB free → extracted VAL SPLIT ONLY (406 img+gt pairs, ~768MB) to internal `~/kbound_acdc`. KEY: the clear-weather REFERENCE (source) images are BUNDLED in the rgb zip under `rgb_anon/{cond}/val_ref/*_rgb_ref_anon.png` (406) — NO separate download needed; moved them to `~/kbound_acdc/rgb_anon_ref/{cond}/val/` so discover_refs (--source acdc_ref) finds them. Cityscapes source not present (0). Dry run (n_eval=5, mild episodic, source=acdc_ref) RAN end-to-end 61s: real difficulty gradient CONFIRMED (source mIoU fog 0.52 / rain 0.39 / snow 0.40 / NIGHT 0.21 = hardest). Early read = deployed adapter (eata_episodic_mild) HARMFUL on all 4 conds (B −0.03..−0.07) → harmful-dominated, NOT goldilocks, beats_both=False (KGA 0.0148 beats adapt 0.0469, loses to freeze 0.0028). BUT NOT THE VERDICT: 5 imgs/cond too noisy + mild/episodic config doesn't test the aggressive/online COLLAPSE regime the goldilocks hypothesis needs. FULL RUN pending (all ~100/cond × aggressive+online configs, ~1-3h MPS) = the real answer. Dry results in experiments/kbound/results/acdc_dryrun_v2/ (NOT scientific — 5-img). DATA RELOCATED TO T9 2026-06-15 (user freed internal): val data now at `experiments/kbound/data/acdc/` (the pipeline DEFAULT --acdc-root) — fog100/night106/rain100/snow100 each w/ rgb_anon/{cond}/val + gt/{cond}/val + rgb_anon_ref/{cond}/val; source zips kept at `experiments/kbound/data/acdc_zips/` (rgb_anon 16GB + gt_trainval). Internal ~/kbound_acdc DELETED, internal freed 14→31GB. CLEANED 2932 exFAT ._ AppleDouble junk after the move (re-clean if data re-copied to exFAT). FULL RUN cmd: `cd /Volumes/T9/uav/AutoML_Flagship_V8 && PYTORCH_ENABLE_MPS_FALLBACK=1 ~/.venv_wilds/bin/python experiments/kbound/acdc/run_acdc_kbound.py --source acdc_ref --aggressiveness mild aggressive --device mps` (default acdc-root + n_eval now full; ~1-3h MPS). MULTI-SEED STRONG RUN LAUNCHED 2026-06-15 ~22:18 CDT: val data COPIED BACK to internal `~/kbound_acdc` (fast APFS reads; zip stays on T9), running seeds 0/1/2 seed-by-seed × aggressiveness mild+aggressive × n_eval 100 (FULL val, bumped from 50 at user's request for reliability — note: more data = reliability NOT a manufactured win; test split unusable = no public labels, val is the only labeled held-out eval) × n_stream 32, source=acdc_ref, caffeinated, log /tmp/acdc_full.log, results experiments/kbound/results/acdc_full_seed{0,1,2}/. ETA ~3-4h/seed ~10-12h total. AGGRESSIVE preset = steps=4 lr=5e-4 ("collapse-prone, surfaces harm") — the regime that could produce the goldilocks night-flip. 6 candidates = {tent,eata,sar}×{online,episodic}. ETA ~2-3h/seed (~6-9h total). PLAN: check after seed 0; if harmful-dominated/no-flip → early-stop (Camelyon wisdom); if night flips helpful↔harmful detectably → let all 3 finish = the real-shift WIN. Report straight either way.
ACDC RESULT — NULL (early-stopped at preview, full run NOT done) 2026-06-15: user asked for a quick aggressive-config preview before the 10-12h run. Preview (deployed=tent_online_aggressive by source entropy, n_eval 6, n_stream 12, seed 0, ~5.5min, source=acdc_ref) → deployed adapter HARMFUL on ALL 4 conditions: fog B−0.032 / night −0.042 / rain −0.050 / snow −0.014 (freeze mIoU fog 0.55/night 0.20/rain 0.40/snow 0.44). goldilocks=FALSE (benefit never positive, no flip), beats_both=FALSE (KGA regret 0.018 beats adapt 0.037, loses to freeze 0.0065). HARMFUL-DOMINATED like RxRx1 — even the easy condition (fog) is harmed, so longer streams only deepen harm, won't flip. → ACDC joins Camelyon/RxRx1/ImageNet-R as a real-shift SAFETY case, NOT a beats-both win. Full 10-12h run EARLY-STOPPED (Camelyon wisdom, saved the compute). CONCLUSION: the real-shift beats-both did NOT materialize on the best goldilocks candidate; K-Bound's honest characterization (decisive on synthetic corruption, safety/abstention on real shifts) STANDS and is reinforced. Paper level unchanged: strong workshop / mid-conference. (B) Scale-invariant τ̂ designed+tested (target-label-free: z=(τ̂−μ_s)/σ_s from source id_val; frozen z*=0; adapt to a fixed source-best candidate, never oracle): HONEST NEGATIVE — no real-shift beats-both. Camelyon17 KGA regret 0.039→0.0094 ties always-adapt (never beats); RxRx1 ties freeze; ImageNet-R abstains. SHARP STRUCTURAL REASON (key finding): a beats-both needs a real shift that is partly-helpful AND partly-harmful FOR THE DEPLOYED (source-best) adapter with the harm visible in τ̂ — none of the 3 has it: on Camelyon17 the strong adapter SAR-online helps 24/24 (0% harm, the 28.7% harm is candidate-averaged over weak adapters), RxRx1 is all-harm, ImageNet-R harm undetectable. Scale-invariance is NECESSARY-BUT-NOT-SUFFICIENT. Constructive breakthrough path (needs fresh run logging source-side τ̂): a real RGB+depth/industrial shift where the single best adapter itself flips helpful↔harmful — i.e. K-Bound × ELARA multimodal convergence. Breakthrough lever CLOSED on current datasets; honest paper = strong workshop/mid-conference.

CONJECTURE 1 (conj:gen) OPEN-PIECE RESOLVED — CONDITIONALLY 2026-06-15. The paper already resolved the *unconditional* label-free bracketing NEGATIVELY (Thm 4 dichotomy: no evidence-definable class identifies sign Δ; minimal untestable supplement = one bit), but Conjecture 1 left open verbatim "the weakest falsifiable class on which the bit still suffices." NOW CLOSED conditionally. Added to `paper/sections/main_theory_5.tex` (placed AFTER Thm 5 = ev-rate, BEFORE the consolidation table, so the five core theorems stay numbered 1–5 and Table II has no gap): Definition 3 (margin-monotone relative-calibration class C_mono: on D, a(x)=σ·c(x) with c(x)=w(x)(m(x)−m*) the OBSERVABLE relative-calibration field, m(x)=2s(x)−1 the observable margin, m* the observable neutral anchor, σ∈{±1} the single global orientation bit; falsifiable-but-not-verifiable), Assumption 1 (general position: free regions carry comparable, not-fully-E-pinned benefit mass; T_E≠0; no E-certifiable dominant region), Lemma 3 (structural sign-freedom = bit complexity: under GP, a b-bit falsifiable structural supplement pins sign Δ across the evidence fibre iff b ≥ K = #independently-signable regions of D), Theorem 6 (CONDITIONAL resolution: under GP, C_mono is the WEAKEST falsifiable class admitting a one-bit benefit-sign certificate — (i) sufficiency sign Δ = σ·sign(T_E), T_E=∫_D c dμ observable; (ii) necessity ±σ are evidence-identical [the D-local label complement of Thm 4] but give ±Δ ⇒ Le Cam ≥1 bit; (iii) minimality any falsifiable class strictly weaker than C_mono contains a non-margin-monotone law with K≥2 ⇒ ≥2 bits), Proposition 1 (explicit non-identifiability witness: two evidence-identical laws sharing any single structural bit's value but with OPPOSITE sign Δ), Remark 1 (candid unconditional gap). Also updated the conjecture's last sentence + two kbound.tex passages (limitation bullet 4, conclusion) to point to Thm 6 with the conditional framing; added `\newtheorem{assumption}` and `\newtheorem{remark}` to the kbound.tex preamble; softened "exactly five theorems" → "five core theorems" + a sixth-result sentence. GENERICITY CAVEAT (do NOT overclaim): this is NOT an unconditional closure — minimality (iii) holds only under the named general-position Assumption; if one region's benefit mass is E-certifiably dominant a single bit can suffice outside C_mono, so the boundary moves. The UNCONDITIONAL weakest falsifiable class REMAINS OPEN (stated in Remark 1). What's proven vs conditional: sufficiency + necessity of one bit on C_mono are unconditional (modulo T_E≠0); the "weakest/minimality" claim rests on general position. VALIDATOR `experiments/kbound/conj1_validator.py` RUNS & PASSES on host python3 (exit code 0, deterministic seed 20260615, numpy): Check A C_mono sufficiency+necessity 20000/20000 with closed-form identity |Δ−σ·T_E| = 0.0 EXACTLY; B1 two-region K=2 witness (region 2 uncalibrated → both candidate single bits sign a1, sign a2 defeated by evidence-identical opposite-Δ pairs); B2 non-monotone magnitude-calibrated K=3 equal-mass majority (sign Δ = majority(ε); all three cell-orientation bits defeated); C consistency (C_mono certificate is unsound off-class, wrong on exactly one of the pair); D general-position-necessity ablation 20000/20000 (a dominant-region class IS one-bit certifiable ⇒ confirms the conditional caveat is real, not vacuous). Results JSON: `experiments/kbound/results_conj1_validator.json` (+ copy in `experiments/kbound/theory_validation/`). PDF REBUILT clean on host via `latexmk -pdf kbound.tex`: `docs/research/kbound/kbound.tex` → `docs/research/kbound/K-Bound_paper_with_frontier_REBUILT.pdf` now 22pp (was 21pp; +1pp = the new block lands cleanly on p.6 with Table II), EXIT=0, no undefined refs, cross-refs stable; the NEW theorem block has ZERO overfull boxes (only the pre-existing 28.12pt box at the theory-input boundary + a trivial 2.25pt box in the intro paragraph remain). Backup of pre-edit source: `docs/research/kbound/kbound.tex.bak_pre_conj1_*`. Paper level unchanged (strong workshop / mid-conference); this adds a genuine, honestly-scoped conditional theorem closing the conjecture's last open piece without any fabricated numbers.

IWILDCAM (5th real-shift candidate) — NULL, then DETECTOR-IMPROVEMENT also NULL (2026-06-15). FIRST honest fix of a prior bug: the finder's f0 was a ResNet18 *head-only* model at CHANCE (id_val bal-acc ~0.10 on 182 classes) → its "harmful-dominated" finder verdicts were artifacts. Trained a PROPER f0 (`experiments/kbound/wilds/train_iwildcam_f0.py`, ResNet50 ERM full-finetune from ImageNet, 2 epochs, plain shuffle, model-selected on id_val macro-F1 so OOD val/test stay clean): **id_val macro-F1 0.408 / OOD val 0.260** (WILDS-grade) → ckpt `results/iwildcam_f0_erm/f0_resnet50_erm_seed0.pt`. Metric switched to WILDS macro-F1 (recomputed offline from stored per-sample preds+eval_y; spot-check matched EXACTLY). Pipeline: `run_iwildcam_kbound.py` (now stores macro-F1 + preds + eval_y) + source-calibrated analyzer `analyze_iwildcam_kbound.py` (SOURCE=id_val fits certificate Z→B, conformal eps, route-b τ*, AND picks deployed adapter; TARGET=val/test labels only score — leakage-free; data partly-extracted so LOAD_TRUNCATED_IMAGES + corrupt-skip added). ENTROPY-BASELINE FULL RESULT (val target, 144 conditions × 6 candidates × 2 seeds = 864 records; source id_val same size): classification = **mixed+UNDETECTABLE**, base-rate-harmful 58.9%, mean_B −0.027, B∈[−0.37,+0.15], frac-helpful 31.5% (genuinely mixed help/harm). Harm is ENTROPY-BLIND: best single label-free feature harm-AUC 0.62 (marginal_KL), all others ~0.50–0.57; the source-fit certificate ANTI-transfers to OOD (target harm-AUC **0.43 < chance**). Deployed adapter tent_online (source-best F1 0.207): KGA regret 0.0245 ≈ always-freeze 0.0233 < always-adapt 0.0517 → ties freeze, **beats_both=FALSE**, false-adapt 2; route-b (source-cal τ*=1.14) router regret 0.0417 > freeze 0.0310, **55 false-adapts**, beats_both=FALSE at EVERY τ* quantile {0.5,0.75,0.9,0.95} (not a τ artifact). Manifests: `results/iwildcam_full_idval/result_489da28f.json`, `results/iwildcam_full_val/result_f08e751c.json` + `VERDICT_val.json`; test target stopped at 65/144 (`results/iwildcam_full_test/PARTIAL_test_57cond.json`) for the pivot. DETECTOR-IMPROVEMENT EXPERIMENT (Pratik-approved pivot — diagnosis: null is a DETECTABILITY failure (confident-wrong collapse under per-location label shift), so give the certificate a STRONGER label-free detector than entropy, τ kept source-calibrated): implemented AETTA-style MC-dropout accuracy estimator (`aetta.py`: dropout on penultimate feat, M=8 p=0.4, harm = estAcc(adapted)−estAcc(frozen)) + frozen-ref disagreement; runner `run_iwildcam_aetta.py`; apples-to-apples analyzer `analyze_iwildcam_detector.py` (same certificate machinery, only the label-free INPUT changes; detector+threshold source-calibrated, NEVER on test). 1% PREVIEW (16 source + 16 target deployed-adapter conditions, seed 0, online cands): **AETTA Δacc is a PERFECT harm detector IN-SOURCE (CV harm-AUC = 1.0, vs entropy 0.67) but ANTI-TRANSFERS to the OOD target (harm-AUC 0.172 << chance, vs entropy 0.375)** → on OOD it predicts the harmful cases as helpful → 3 false-adapts → KGA regret 0.0324 > always-freeze 0.0253 (makes routing WORSE), beats_both=FALSE; aetta+Z augmented same (tgtAUC 0.234, 5 false-adapts, regret 0.0411). OUTCOME = **(3) impossibility reinforced**, sharpened: it is NOT that no label-free signal sees the harm (AETTA sees it perfectly in-distribution) — it is that the harm-signal relationship does NOT survive the source→OOD shift (same root cause as the prior "τ̂ scale doesn't transfer" finding). NOT a beats-both. CAVEAT: preview is small-n (srcAUC=1.0 inflated at n=16); direction is robust + consistent with the 864-record entropy baseline; a full 2-seed/6-candidate AETTA grid (~2h MPS) would tighten magnitudes but is very unlikely to flip the sign. INTEGRITY: nothing tuned to manufacture a pass; null reported straight. Consolidated manifest `results/iwildcam_kbound_RESULTS.json`. Net for the paper: iWildCam joins Camelyon17/RxRx1/ImageNet-R/ACDC as a real-shift SAFETY/abstention case, NOT a real-shift beats-both — and now with a sharper impossibility statement (detector is in-distribution-perfect yet non-transferable to OOD). K-Bound's honest characterization (decisive on synthetic corruption; safety/abstention on real shifts) STANDS, reinforced.

"MAKE ALL THEOREMS UNCONDITIONAL" — INVESTIGATED, ANSWERED HONESTLY 2026-06-15. User asked to remove conditionality from all theorems (believing "there should be a way"). Ran two parallel analysis agents (theorem-by-theorem conditionality audit + adversarial de-conditionalization of Thm6). HONEST VERDICT: you CANNOT make all theorems unconditional, and shouldn't — the paper's own impossibility theorems (Thm1 thm:imp, Thm4 thm:conj1-dichotomy parts i/iii) PROVE no assumption-free label-free benefit-sign certificate exists, so the positive/identifiability theorems are conditional BY NECESSITY (stripping their assumptions makes them FALSE and contradicts the impossibility core). Classification: ALREADY UNCONDITIONAL = Lemma lem:reduction, Lemma thm:gate, Thm1 thm:imp, Thm4(i)(iii)(iv) impossibility halves, Cor thm:pos(b), Prop prop:cmono-witness. CONDITIONAL-BY-NECESSITY (irremovable, some with explicit counterexamples e.g. stealth laws τ=0) = Thm4(ii) sufficiency-under-H, Thm5 thm:ev-rate (H + declared bit + margin regularity c_min/b_min whose constant provably diverges as agreement→chance), Cor thm:pos(a) covariate-shift premise, and the LABEL-FREE version of Thm2 thm:cert. UPGRADEABLE-BY-REFRAMING (free, no new math) = Thm3 thm:frontier (already an iff + necessity converse over ALL identifying classes; restate as unconditional statement about the family {C_β}) — NOT yet done, offered as option. NEEDS-CONSTRUCTION/GENUINELY-OPEN = Thm6(iii) minimality on General Position (asm:genpos) — NOT forced by impossibility, NOT mere reframing; making it unconditional needs a real new theorem (a bit-complexity count K_eff absorbing the E-dominant-mass escape hatch). Check D in conj1_validator.py is a genuine counterexample: without GP a dominant region's orientation certifies sign Δ outside C_mono, so "C_mono is THE weakest" is FALSE unconditionally. KEY INTEGRITY FIX MADE (the deep dive found a real overclaim in my own prior-turn Thm6): (a) Thm6 (i)Sufficiency + (ii)Necessity are ALREADY UNCONDITIONAL (never use GP) — now stated explicitly as "exactly one bit certifies sign Δ on C_mono with no further assumption"; (b) only (iii)Minimality uses Assumption 1 (General Position = the certificate being "domination-free"/minimax-sound over unobserved magnitudes); (c) DOWNGRADED "THE weakest" → "A weakest" everywhere, because even under GP C_mono is non-unique: any calibrated sign-aligned field {a=σh, h observable} is equally one-bit and incomparable to C_mono (Agent 2 verified |a|=2|c| works identically). Added validator CHECK E (two incomparable robustly-one-bit classes → "a weakest not the unique"); all SIX checks now PASS on host python3 (exit 0): A 20000/20000 |Δ−σT_E|=0 exact, B1, B2, C, D 20000/20000, E. Edited paper/sections/main_theory_5.tex (Thm6 statement: parts i/ii marked unconditional, iii under Asm1, "a weakest … not unique"; Remark 1 retitled "What is unconditional, and what rests on general position" + domination-free framing + σh non-uniqueness + "All six checks"; conjecture sentence + section-intro sentence softened to "a weakest" w/ unconditional certificate) and kbound.tex (limitation bullet 4 + conclusion → "a weakest", certificate unconditional). Rebuilt host latexmk EXIT=0, K-Bound_paper_with_frontier_REBUILT.pdf 22pp, page 6 renders clean (visually checked), no undefined refs, no new overfull. NET (honest, for the user): did NOT fake any unconditional theorem; instead (1) made Thm6's one-bit CERTIFICATE explicitly unconditional (a genuine gain), (2) fixed a real "THE weakest" overclaim down to "a weakest" (makes it MORE review-proof), (3) kept Thm6 minimality conditional on GP because Check D proves it must be. Thm3 unconditional-reframe still available as a free future edit. Paper level unchanged (strong workshop / mid-conference); now strictly more honest. Files: experiments/kbound/conj1_validator.py (now 6 checks), docs/research/kbound/paper/sections/main_theory_5.tex, docs/research/kbound/K-Bound_paper_with_frontier_REBUILT.pdf.

THEOREM 3 (thm:frontier) REFRAMED AS UNCONDITIONAL — DONE 2026-06-15 (the "free win" from the de-conditionalization audit; user approved). No content change to parts (i)-(iv); only made the framing explicit so it no longer reads as conditional. Edit in paper/sections/main_theory_5.tex: added lead-in "The statements below hold UNCONDITIONALLY: for every drift budget β≥0 and every target with μ(D)>0, with β a free parameter of the frontier rather than an assumption (and (iii) shows every identifying class is of this form)." This is honest because Thm3 was already an iff ("sign Δ identifiable over C_β iff |M|>β") PLUS a necessity converse (iii: every identifying class is a one-sided γ-constraint) — a complete characterization, not an assumption. CODE: extended experiments/kbound/theory_validation/val_frontier.py with CHECK T5 (unconditional family): non-circular test that "sign Δ identifiable over C_β" (= budget interval [M−β,M+β] excludes 0) EQUALS {|M|>β} exactly, and "unknowable" EQUALS {|M|≤β}, over a grid β∈{0,0.05,0.1,0.25,0.4}×200000 draws. RAN on host python3, ALL PASS exit 0: T1 impossibility, identity 20000/20000 exact, T2 frontier law 500000 draws, T3 tight region all β, T4 ATC=β=0 face (error==frontier-event to 1e-9), T5 unconditional family all β. results_frontier.json updated. Proof-sketch "Validated:" line updated to cite the T5 family check and "val_frontier.py checks T1--T5". PDF rebuilt host latexmk EXIT=0, 22pp, page 4 (Thm3) renders clean (visually verified), no undefined refs, no new overfull → K-Bound_paper_with_frontier_REBUILT.pdf (1808191 bytes). NET running tally of unconditional content after this session: Thm1 (impossibility), Thm3 (frontier, now explicit), Thm4(i)(iii)(iv) impossibility halves, Cor pos(b), Prop1 witness, Lemmas reduction+regret, AND Thm6(i)(ii) one-bit certificate = all UNCONDITIONAL. Remaining conditional-BY-NECESSITY (impossibility forbids removal): Thm2 label-free certificate, Thm4(ii) sufficiency-under-H, Thm5 rate (H+bit+margins), Cor pos(a) covariate shift, Thm6(iii) minimality (General Position). Nothing faked. Paper strictly more honest + one more theorem now reads unconditional.

KGA × ELARA "can it be 90+?" — BUILT & RAN FOR REAL 2026-06-15. experiments/kbound/kga_elara_demo.py wraps ELARA's reliability-gated multimodal fusion (the real _cw confidence-weighted + KS-drift reliability-gated formulas from src/scripts/elara_u/multimodal_reliability_test.py) on the REAL cached .npz score caches (experiments/fusion/{3d_adam,realiad_d3,mulsen,mvtec3d,realiad_natdeg}_score_cache/*.npz: Sval[n,M],yval,Stest[n,M],ytest,valauc[M]) and puts the ACTUAL kga/ package certificate on top (kga.certificate.empirical_bernstein + kga.policy.decide; alpha=0.10; benefit_range=2.0; benefit = per-sample placement of fusion minus best VAL-chosen single modality, pooled across cats). Host python3 3.9.6 numpy 2.0.2 scipy 1.13.1 sklearn 1.6.1; exit 0; results_kga_elara.json. REAL NUMBERS (fusion AUROC reproduces ELARA committed JSON to ±0.001): 3D-ADAM rel-gate 0.9367 / CW 0.9344 (best single 0.917) → KGA ABSTAIN (δ̂=+0.013, eps=0.065, LB=−0.052); Real-IAD-D3 rel-gate 0.9032 / CW 0.811 (best single 0.934!) → KGA FREEZE (δ̂=−0.124, LB=−0.160 — CW fusion HURTS vs single modality); MulSen CW 0.9335 / rel-gate 0.883 (best single 0.922) → KGA ABSTAIN (δ̂=+0.012, LB=−0.088); MVTec-3D rel-gate 0.882 / CW 0.866 (<90) → ABSTAIN; Real-IAD-NatDeg fusion 0.711 (best single 0.604) → KGA ADAPT (δ̂=+0.110, LB=+0.073 — the ONLY certified-fusion-helps track, but absolute only 0.71). HONEST VERDICT: 90+ IS reached on 3/5 multimodal tracks (3D-ADAM 0.94, MulSen 0.93, Real-IAD-D3 0.93) and is real/reproduced — BUT the fusion is NOT the source of the 90+: a single strong modality already is (best-single ≈0.92–0.93 on those tracks), and KGA's certificate CORRECTLY refuses to credit fusion there (ABSTAIN on 3D-ADAM/MulSen, FREEZE on Real-IAD-D3 where CW fusion underperforms the single modality). Fusion earns a certified ADAPT only where the base is weak (NatDeg) and there the ceiling is just 0.71. So "fusion-driven 90+" is NOT supported by these caches — the goldilocks complementarity (weak single modalities + fusion pushes ≥90) is absent, exactly the documented gap. What KGA×ELARA genuinely delivers: a certified SELECTOR/guard operating at the 90+ ceiling on complementary tracks (picks the strong option, won't over-credit fusion) + honest sub-90 flags. Selective abstention (risk-coverage) modestly raises AUROC on strong tracks (3D-ADAM 0.934→0.945@80%cov, MVTec 0.866→0.893@80%) but can't rescue weak ones (NatDeg 0.711→0.693). For a fusion-driven 90+ you need a track with NO single modality ≥90 AND fusion ≥90 — none of the 5 caches has it. Nothing faked; reported what the data shows.

OFFICE-HOME (6th real-shift candidate) — NULL, held-out beats_both=FALSE (2026-06-15). Properly powered run after a clean 1%-dry-run gate. SETUP: source = Real_World, fine-tuned ResNet-50 (ImageNet V2, 8 ep, fixed budget) → 86.1% RW-val acc; held-out discipline = τ*, candidate set, deployed-adapter, certificate ALL calibrated on source(Real_World/val)+target-VAL only, test evaluated ONCE. 14 candidates = {tent,eata,sar}×{online,episodic}×{mild,aggressive} (12) + NEW labelshift (MLLS prior-correction, no param update) + NEW conservative (damped+clipped prior-correction, no param update). Grid = 2 seeds × {iid,imbalanced,single_class} × {tiny=8,small=16}; n_eval=320 (~hundreds/cond, ~144 gradient cond/domain). Code: experiments/kbound/officehome/{oh_data,oh_candidates,oh_analyze,run_officehome_kbound,oh_report}.py + supervise_oh.sh. Manifests: results/officehome_full_{source,targetval,targettest,FINAL}; f0 results/officehome_f0/f0_resnet50_rw_seed0.pt; LOCKED splits results/officehome_kbound/splits.json; data ~/kbound_officehome (internal) + T9 experiments/kbound/data/office_home.
DECISIVE FINDING (gradient-TTA only, label-shift collapse EXCLUDED since it's trivially detectable): the goldilocks regime is structurally ABSENT because DETECTABILITY ⊥ MIXEDNESS across domains. VAL per-domain: Art helpful-dominated (9% harm, meanB +0.018, best-feat harm-AUC 0.90), Clipart helpful-dominated (3% harm, +0.034, AUC 0.99) — harm is detectable but there's almost none to avoid; Product MIXED (39% harm, meanB −0.006) but harm is NOT label-free detectable (best-feat 0.69, certificate-transfer 0.53≈chance). POOLED gradient-TTA: best single feature in-sample harm-AUC 0.81 (REAL at n=432, not the dry-run 2-condition artifact) BUT source-calibrated certificate-transfer-AUC 0.33 (ANTI-transfers) — same root cause as the iWildCam null (detector works in-distribution, dies on source→OOD transfer). HELD-OUT TEST (36 cond, 504 rec) reproduced it: pooled gradient helpful-dominated (12% harm, meanB +0.015), best-feat 0.79 / cert-transfer 0.49; Product mixed (23% harm) cert-transfer 0.63.
ROUTER: deployed (source-best) = eata_online_mild, which is NET-HELPFUL (route_a always-adapt regret 0.0009 val / 0.0000 test, vs KGA-freeze 0.028/0.027) → the safety/FREEZE play ties always-freeze but LOSES to always-adapt (Camelyon-style helpful-leaning failure). route_b multicandidate τ*=1.12 source-calibrated → ~all ABSTAIN/FREEZE (τ doesn't transfer; tgt τ mostly >τ*), regret = freeze >> best-fixed-adapt 0.0054. beats_both=FALSE on BOTH routes, BOTH val and held-out test, 0 false-adapts (safe, never a win). NEW candidates behaved as designed but didn't rescue it: labelshift catastrophic (meanB −0.47, 100% harm, harmful-dominated all domains); conservative inference-only was mildly HARMFUL not helpful (meanB −0.012, 81% harm) → gave the router NO safe-helpful option. OUTCOME = null + regime (helpful-leaning deployed adapter + harm-where-mixed label-free-undetectable-on-transfer). Office-Home joins Camelyon17/RxRx1/ImageNet-R/ACDC/iWildCam as a real-shift SAFETY/abstention case, NOT a beats-both. K-Bound's honest characterization (decisive on synthetic corruption; safety/abstention on real shifts) STANDS, now reinforced WITHIN a single covariate-shift dataset. OPS LESSON: the 16GB MPS wedged/throttled only when a CONCURRENT run_iwildcam_kbound ResNet-50 job shared the GPU (contention, not heat) — killing the other job cleared it instantly (the wedged cell s1/Clipart/test/iid/tiny then ran in 165s). Integrity: nothing tuned on test; one condition was --skip-resumed after the contention kill then back-filled to a full 36/36; null reported straight.
OFFICE-HOME INTEGRATED INTO PAPER 2026-06-15: added to docs/research/kbound/kbound.tex as the "Fifth natural-shift track: Office-Home" paragraph + Table tab:officehome (covariate-shift, detectability vs mixedness anti-correlate, certificate transfer-AUC 0.33 val/0.49 test, beats_both=no on val + held-out test, 0 false-adapts); synthesis paragraph updated (tau scale list now incl ~1.12 Office-Home; the structural-obstruction sentence cites Office-Home as the covariate-shift case). latexmk clean exit 0, kbound.pdf now 23pp (was 22), tab:officehome resolved, 0 undefined refs. Copied to K-Bound_paper_with_frontier_REBUILT.pdf (canonical delivered) + K-Bound_paper_officehome_20260615.pdf. Pre-edit backup kbound.tex.bak_pre_officehome_20260615_133823. Standalone report also exists at results/officehome_full_FINAL/OfficeHome_KBound_Report.pdf (5pp, 3 figs) but the PAPER integration is the canonical deliverable.
