# Archived ELARA Scenario C tests

These pytest modules locked the old **ELARA Family A/B/D / phase2** research
contract. They are **not** part of the K-Bound scientific claim or CI.

Moved out of repo-root `tests/` on 2026-07-15 so the default suite is K-Bound /
KGA only. Re-run locally only if you are auditing legacy ELARA provenance:

```bash
pytest archive/legacy_elara/tests -q
```
