# Gate P P15 — baseline load test (in-process TestClient)
#
#   python deploy/loadtest/run_baseline.py
#
# Optional Locust against live API (requires: pip install locust):
#
#   locust -f deploy/loadtest/locustfile.py --host http://127.0.0.1:8000
